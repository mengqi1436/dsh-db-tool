[32m18:21:32 (6764) INFO ProjectReader[39m Found 9 of 552 file(s) to be mutated.
[32m18:21:32 (6764) INFO Instrumenter[39m Instrumented 9 source file(s) with 1040 mutant(s)
[32m18:21:33 (6764) INFO ConcurrencyTokenProvider[39m Creating 4 test runner process(es).
[32m18:21:34 (6764) INFO BroadcastReporter[39m Detected that current console does not support the "progress" reporter, downgrading to "progress-append-only" reporter
[32m18:21:34 (6764) INFO DryRunExecutor[39m Starting initial test run (vitest test runner with "perTest" coverage analysis). This may take a while.
[32m18:21:40 (6764) INFO DryRunExecutor[39m Initial test run succeeded. Ran 98 tests in 5 seconds (net 590.896600000001 ms, overhead 4674.103399999999 ms).
Mutation testing 1% (elapsed: <1m, remaining: ~13m) 109/1040 tested (12 survived, 0 timed out)
Mutation testing 2% (elapsed: <1m, remaining: ~11m) 125/1040 tested (28 survived, 0 timed out)
Mutation testing 4% (elapsed: <1m, remaining: ~10m) 145/1040 tested (48 survived, 0 timed out)
Mutation testing 5% (elapsed: <1m, remaining: ~12m) 169/1040 tested (72 survived, 0 timed out)
Mutation testing 5% (elapsed: <1m, remaining: ~15m) 221/1040 tested (124 survived, 0 timed out)
Mutation testing 6% (elapsed: ~1m, remaining: ~15m) 251/1040 tested (154 survived, 0 timed out)
Mutation testing 8% (elapsed: ~1m, remaining: ~13m) 267/1040 tested (170 survived, 0 timed out)
Mutation testing 9% (elapsed: ~1m, remaining: ~13m) 292/1040 tested (195 survived, 0 timed out)
Mutation testing 12% (elapsed: ~1m, remaining: ~10m) 323/1040 tested (226 survived, 0 timed out)
Mutation testing 12% (elapsed: ~1m, remaining: ~11m) 355/1040 tested (258 survived, 0 timed out)
Mutation testing 13% (elapsed: ~1m, remaining: ~12m) 374/1040 tested (277 survived, 0 timed out)
Mutation testing 13% (elapsed: ~2m, remaining: ~12m) 395/1040 tested (298 survived, 0 timed out)
Mutation testing 15% (elapsed: ~2m, remaining: ~12m) 416/1040 tested (319 survived, 0 timed out)
Mutation testing 15% (elapsed: ~2m, remaining: ~12m) 441/1040 tested (344 survived, 0 timed out)
Mutation testing 17% (elapsed: ~2m, remaining: ~11m) 468/1040 tested (371 survived, 0 timed out)
Mutation testing 19% (elapsed: ~2m, remaining: ~10m) 493/1040 tested (396 survived, 0 timed out)
Mutation testing 21% (elapsed: ~2m, remaining: ~10m) 505/1040 tested (408 survived, 0 timed out)
Mutation testing 22% (elapsed: ~3m, remaining: ~10m) 525/1040 tested (428 survived, 0 timed out)
Mutation testing 23% (elapsed: ~3m, remaining: ~10m) 541/1040 tested (444 survived, 0 timed out)
Mutation testing 23% (elapsed: ~3m, remaining: ~10m) 549/1040 tested (452 survived, 0 timed out)
Mutation testing 25% (elapsed: ~3m, remaining: ~10m) 577/1040 tested (480 survived, 0 timed out)
Mutation testing 26% (elapsed: ~3m, remaining: ~10m) 606/1040 tested (509 survived, 0 timed out)
Mutation testing 26% (elapsed: ~3m, remaining: ~10m) 644/1040 tested (547 survived, 0 timed out)
Mutation testing 28% (elapsed: ~4m, remaining: ~10m) 657/1040 tested (560 survived, 0 timed out)
Mutation testing 28% (elapsed: ~4m, remaining: ~10m) 672/1040 tested (575 survived, 0 timed out)
Mutation testing 30% (elapsed: ~4m, remaining: ~9m) 688/1040 tested (591 survived, 0 timed out)
Mutation testing 33% (elapsed: ~4m, remaining: ~8m) 701/1040 tested (604 survived, 0 timed out)
Mutation testing 35% (elapsed: ~4m, remaining: ~8m) 713/1040 tested (616 survived, 0 timed out)
Mutation testing 37% (elapsed: ~4m, remaining: ~7m) 729/1040 tested (632 survived, 0 timed out)
Mutation testing 39% (elapsed: ~5m, remaining: ~7m) 740/1040 tested (643 survived, 0 timed out)
Mutation testing 41% (elapsed: ~5m, remaining: ~7m) 751/1040 tested (654 survived, 0 timed out)
Mutation testing 44% (elapsed: ~5m, remaining: ~6m) 763/1040 tested (666 survived, 0 timed out)
Mutation testing 47% (elapsed: ~5m, remaining: ~6m) 777/1040 tested (680 survived, 0 timed out)
Mutation testing 47% (elapsed: ~5m, remaining: ~6m) 802/1040 tested (705 survived, 0 timed out)
Mutation testing 48% (elapsed: ~5m, remaining: ~6m) 814/1040 tested (717 survived, 0 timed out)
Mutation testing 48% (elapsed: ~6m, remaining: ~6m) 824/1040 tested (727 survived, 0 timed out)
Mutation testing 49% (elapsed: ~6m, remaining: ~6m) 835/1040 tested (738 survived, 0 timed out)
Mutation testing 52% (elapsed: ~6m, remaining: ~5m) 853/1040 tested (756 survived, 0 timed out)
Mutation testing 54% (elapsed: ~6m, remaining: ~5m) 874/1040 tested (777 survived, 0 timed out)
Mutation testing 56% (elapsed: ~6m, remaining: ~5m) 888/1040 tested (791 survived, 0 timed out)
Mutation testing 57% (elapsed: ~6m, remaining: ~5m) 912/1040 tested (815 survived, 0 timed out)
Mutation testing 60% (elapsed: ~7m, remaining: ~4m) 924/1040 tested (827 survived, 0 timed out)
Mutation testing 64% (elapsed: ~7m, remaining: ~4m) 940/1040 tested (843 survived, 0 timed out)
Mutation testing 67% (elapsed: ~7m, remaining: ~3m) 955/1040 tested (858 survived, 0 timed out)
Mutation testing 70% (elapsed: ~7m, remaining: ~3m) 968/1040 tested (869 survived, 0 timed out)
Mutation testing 74% (elapsed: ~7m, remaining: ~2m) 977/1040 tested (876 survived, 0 timed out)
Mutation testing 78% (elapsed: ~7m, remaining: ~2m) 988/1040 tested (884 survived, 0 timed out)
Mutation testing 83% (elapsed: ~8m, remaining: ~1m) 999/1040 tested (893 survived, 0 timed out)
Mutation testing 86% (elapsed: ~8m, remaining: ~1m) 1007/1040 tested (899 survived, 0 timed out)
Mutation testing 89% (elapsed: ~8m, remaining: <1m) 1015/1040 tested (905 survived, 0 timed out)
Mutation testing 96% (elapsed: ~8m, remaining: <1m) 1031/1040 tested (915 survived, 0 timed out)

All tests
  guard
    challenge.spec.ts
      ~ ChallengeStore 一次性：consume 成功后立即失效 (covered 38)
      ~ ChallengeStore 绑定语句：换语句消费失败（重放改语句被拒） (covered 37)
      ~ ChallengeStore TTL 过期后 consume 失败 (covered 35)
      ~ ChallengeStore TTL 内正常消费 (covered 37)
      ~ ChallengeStore sweep 清理过期项 (covered 37)
      ✓ ChallengeStore dispose 后不可再用 (killed 2)
      ~ ChallengeStore id 形如 c_<hex> 且互不相同 (covered 25)
    classify.spec.ts
      ✓ classifyStatement: SQL 系 普通 SELECT 为 none (killed 3)
      ~ classifyStatement: SQL 系 DDL 为 danger，原因是 DDL 不可回滚 (covered 44)
      ~ classifyStatement: SQL 系 DML 在 execute 通道为 warning，在 query 通道为 danger (covered 54)
      ✓ classifyStatement: SQL 系 读语句内的危险片段升级为 danger (killed 3)
      ✓ classifyStatement: SQL 系 维护命令为 danger (killed 1)
      ~ classifyStatement: SQL 系 未知关键字：query 通道 danger，execute 通道 warning (covered 55)
      ~ classifyStatement: Redis 危险命令 danger（13 项清单） (covered 18)
      ~ classifyStatement: Redis 读命令 none、写命令 warning (covered 24)
      ~ classifyStatement: Redis JSON 数组形式同样解析 (covered 24)
      ~ classifyStatement: Redis 未知命令 warning (covered 21)
      ~ classifyStatement: MongoDB 危险操作 danger（8 项清单） (covered 33)
      ✓ classifyStatement: MongoDB 写操作 warning、读操作 none (killed 2)
      ~ classifyStatement: MongoDB 非法 JSON danger (covered 16)
      ~ statementHash trim 后哈希，稳定一致 (covered 5)
  http/api.spec.ts
    ~ fenced 校验 跨站 Origin → 403 FORBIDDEN (covered 161)
    ✘ fenced 校验 伪造 Host（DNS rebinding）→ 403 (covered 0)
    ✘ fenced 校验 loopback Origin 端口不一致 → 403 (covered 0)
    ~ fenced 校验 loopback Origin 同端口放行 (covered 20)
    ~ fenced 校验 无 Origin/Referer（本地脚本）放行 (covered 41)
    ✘ handleDbToolRequest 直调（prefix 挂载形态 + trust） 非 loopback Host → 403 FORBIDDEN (covered 0)
    ✘ handleDbToolRequest 直调（prefix 挂载形态 + trust） 恶意 Origin → 403 (covered 0)
    ~ handleDbToolRequest 直调（prefix 挂载形态 + trust） loopback 同端口 + prefix 路径 /dsh-db-tool/api/state → 200 (covered 41)
    ~ handleDbToolRequest 直调（prefix 挂载形态 + trust） trustedHosts 条目放行（非 loopback Host + 受信 Origin） (covered 41)
    ✘ handleDbToolRequest 直调（prefix 挂载形态 + trust） isTrustedRequest 纯函数：0.0.0.0 非 loopback；空 Host 拒绝 (covered 0)
    ✓ handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回 (killed 1)
    ~ 连接管理与授权路由 GET /api/connections 脱敏（无密码，含 ***） (covered 20)
    ~ 连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除 (covered 203)
    ~ 连接管理与授权路由 grants PUT/GET/DELETE (covered 95)
    ✘ 连接管理与授权路由 mode 非法 → INVALID_ARGUMENT (covered 0)
    ✓ 业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT (killed 1)
    ~ 业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview (covered 120)
    ~ 业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒 (covered 201)
    ~ 业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE (covered 191)
    ~ 业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应） (covered 248)
    ~ 业务路由与确认流程 GET /api/audit 与 /api/state (covered 95)
    ~ 业务路由与确认流程 未知路由 → 404；坏 JSON → INVALID_ARGUMENT (covered 1)
  store
    audit.spec.ts
      ~ AuditLog append 落盘为 JSONL，ts 省略时自动补当前时间 (covered 23)
      ~ AuditLog tail 返回最近 n 条且时间正序 (covered 49)
      ~ AuditLog 文件不存在时 tail 返回空数组 (covered 23)
      ~ AuditLog 单个损坏行被跳过，不影响其余历史 (covered 49)
    connections.spec.ts
      ✓ ConnectionStore create 返回脱敏 meta，密码不出现在 connections.json (killed 1)
      ~ ConnectionStore create 重复 id 抛错 (covered 88)
      ~ ConnectionStore list / get 返回脱敏数据，get 不存在返回 undefined (covered 119)
      ~ ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets (covered 156)
      ~ ConnectionStore testTarget 返回含机密的 ResolvedConnection；不存在抛错 (covered 156)
      ~ ConnectionStore remove 级联删除 secrets 与全部项目的 grants (covered 188)
      ~ ConnectionStore redactUrl 覆盖各类 URL 形态 (covered 38)
    grants.spec.ts
      ~ GrantStore grant 后 grantsFor/check 可见 (covered 65)
      ~ GrantStore 同连接重复授权更新模式（ro → rw） (covered 67)
      ~ GrantStore revoke 撤销后不可见；撤销不存在者静默 (covered 83)
      ~ GrantStore 同一连接多项目授权互不干扰 (covered 75)
      ~ GrantStore grantsFor 未授权项目返回空数组 (covered 28)
    io.spec.ts
      ~ io：原子写与损坏容错 writeJsonAtomic 落盘内容正确且无临时文件残留 (covered 11)
      ~ io：原子写与损坏容错 覆盖写不残留旧版本内容 (covered 11)
      ~ io：原子写与损坏容错 readJson：文件不存在返回 fallback (covered 4)
      ~ io：原子写与损坏容错 损坏 JSON 备份为 .bak 并返回 fallback（store 级验证） (covered 90)
      ~ io：原子写与损坏容错 secrets.json 损坏同样容错且重建后权限仍为 0600（POSIX） (covered 51)
      ~ io：原子写与损坏容错 目录权限尽力设为 0700（POSIX） (covered 18)
    normalize.spec.ts
      ✓ normalizeProjectKey 同一目录不同写法归一化为同一 key（分隔符/尾分隔符） (killed 5)
      ~ normalizeProjectKey 盘符大写归一为小写（路径段保留原大小写） (covered 14)
      ~ normalizeProjectKey 分隔符统一为 / 且去尾分隔符 (covered 14)
      ~ normalizeProjectKey 相对路径按 cwd 解析为绝对路径 (covered 14)
      ~ normalizeProjectKey 归一化是幂等的 (covered 14)
    secrets.spec.ts
      ~ SecretsBox set 合并写入并保留旧键，get/delete 正常 (covered 50)
      ~ SecretsBox get 返回拷贝，外部修改不污染内部状态 (covered 45)
      ~ SecretsBox 任何公开 API 返回值与元数据文件的序列化结果都不含密码 (covered 151)
      ~ SecretsBox secrets.json 写入后权限为 0600（POSIX；Windows 尽力而为跳过断言） (covered 18)
  tool/service.spec.ts
    ~ DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝 (covered 225)
    ~ DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND (covered 238)
    ~ DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂 (covered 347)
    ~ DbToolService: 授权与只读 rw 连接 mode=rw 透传，DML 直接执行 (covered 340)
    ✓ DbToolService: 危险确认与 challenge 危险语句首次 → NEEDS_CONFIRMATION；带 challengeId 重试成功 (killed 1)
    ~ DbToolService: 危险确认与 challenge challenge 一次性：重放同 id → INVALID_CHALLENGE (covered 384)
    ~ DbToolService: 危险确认与 challenge challenge 绑 hash：带 id 但语句已变更 → INVALID_CHALLENGE (covered 374)
    ~ DbToolService: 危险确认与 challenge query 通道的写语句同样需要确认；SELECT 直通 (covered 370)
    ~ DbToolService: 危险确认与 challenge ro 连接对危险语句也先报 READ_ONLY（授权优先于 guard） (covered 230)
    ~ DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50 (covered 302)
    ~ DbToolService: 浏览与审计 危险确认与执行都留审计 (covered 411)
    ~ DbToolService: 浏览与审计 INVALID_CHALLENGE 也留审计 (covered 410)
    ✓ DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回 (killed 1)
    ~ DbToolService: run_script 沙箱 沙箱内无 require/process/globalThis.fs 逃逸面 (covered 299)
    ~ DbToolService: run_script 沙箱 脚本内危险语句 → NEEDS_CONFIRMATION（challengeId 为空时生成新 challenge） (covered 395)
    ~ DbToolService: run_script 沙箱 脚本语法错误 → DRIVER_ERROR 而非崩溃 (covered 295)
    ~ DbToolService: 连接管理与 secrets connections.json 无密码（*** 脱敏），密码只进 secrets.json (covered 171)
    ~ DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND (covered 243)
    ~ handleToolAction（工具分发层） list_connections 返回脱敏 JSON 文本 (covered 187)
    ~ handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引 (covered 431)
    ~ handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出） (covered 257)
    ~ handleToolAction（工具分发层） 缺参数 / 未知 action → INVALID_ARGUMENT 错误 JSON (covered 204)
    ~ handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒 (covered 434)
    ✘ 连接 URL 常量 fixture 密码存在（供脱敏断言对照） (covered 0)

[NoCoverage] StringLiteral
lib/guard/index.ts:52:35
-     return m?.[1]?.toLowerCase() ?? '';
+     return m?.[1]?.toLowerCase() ?? "Stryker was here!";

[NoCoverage] ObjectLiteral
lib/guard/index.ts:75:27
-     if (head === '') return { level: 'danger', reason: '无法解析语句关键字，按危险语句处理' };
+     if (head === '') return {};

[NoCoverage] StringLiteral
lib/guard/index.ts:75:36
-     if (head === '') return { level: 'danger', reason: '无法解析语句关键字，按危险语句处理' };
+     if (head === '') return { level: "", reason: '无法解析语句关键字，按危险语句处理' };

[NoCoverage] StringLiteral
lib/guard/index.ts:75:54
-     if (head === '') return { level: 'danger', reason: '无法解析语句关键字，按危险语句处理' };
+     if (head === '') return { level: 'danger', reason: "" };

[NoCoverage] StringLiteral
lib/guard/index.ts:101:29
-     const head = (parts[0] ?? '').toUpperCase();
+     const head = (parts[0] ?? "Stryker was here!").toUpperCase();

[NoCoverage] ObjectLiteral
lib/guard/index.ts:102:27
-     if (head === '') return { level: 'danger', reason: '无法解析 Redis 命令，按危险命令处理' };
+     if (head === '') return {};

[NoCoverage] StringLiteral
lib/guard/index.ts:102:36
-     if (head === '') return { level: 'danger', reason: '无法解析 Redis 命令，按危险命令处理' };
+     if (head === '') return { level: "", reason: '无法解析 Redis 命令，按危险命令处理' };

[NoCoverage] StringLiteral
lib/guard/index.ts:102:54
-     if (head === '') return { level: 'danger', reason: '无法解析 Redis 命令，按危险命令处理' };
+     if (head === '') return { level: 'danger', reason: "" };

[NoCoverage] StringLiteral
lib/guard/index.ts:122:67
-         head = Object.keys(parsed as Record<string, unknown>)[0] ?? '';
+         head = Object.keys(parsed as Record<string, unknown>)[0] ?? "Stryker was here!";

[NoCoverage] ObjectLiteral
lib/guard/index.ts:127:27
-     if (head === '') return { level: 'danger', reason: '无法解析 MongoDB 命令，按危险命令处理' };
+     if (head === '') return {};

[NoCoverage] StringLiteral
lib/guard/index.ts:127:36
-     if (head === '') return { level: 'danger', reason: '无法解析 MongoDB 命令，按危险命令处理' };
+     if (head === '') return { level: "", reason: '无法解析 MongoDB 命令，按危险命令处理' };

[NoCoverage] StringLiteral
lib/guard/index.ts:127:54
-     if (head === '') return { level: 'danger', reason: '无法解析 MongoDB 命令，按危险命令处理' };
+     if (head === '') return { level: 'danger', reason: "" };

[NoCoverage] ArithmeticOperator
lib/guard/index.ts:170:47
-       const interval = opts?.sweepIntervalMs ?? 60 * 1000;
+       const interval = opts?.sweepIntervalMs ?? 60 / 1000;

[NoCoverage] BlockStatement
lib/guard/index.ts:171:23
-       if (interval > 0) {
-         this.timer = setInterval(() => this.sweep(), interval);
-         this.timer.unref?.();
-       }
+       if (interval > 0) {}

[NoCoverage] ArrowFunction
lib/guard/index.ts:172:32
-         this.timer = setInterval(() => this.sweep(), interval);
+         this.timer = setInterval(() => undefined, interval);

[NoCoverage] OptionalChaining
lib/guard/index.ts:173:7
-         this.timer.unref?.();
+         this.timer.unref();

[NoCoverage] BlockStatement
lib/guard/index.ts:210:21
-       if (this.timer) {
-         clearInterval(this.timer);
-         this.timer = undefined;
-       }
+       if (this.timer) {}

[NoCoverage] CallExpression
lib/guard/index.ts:211:7
-         clearInterval(this.timer);
+         ;

[Survived] Regex
lib/guard/index.ts:47:14
-       .replace(/\/\*[\s\S]*?\*\//g, ' ')
+       .replace(/\/\*[^\s\S]*?\*\//g, ' ')
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 19 more tests!


[Survived] MethodExpression
lib/guard/index.ts:46:13
-     const s = statement
-       .replace(/\/\*[\s\S]*?\*\//g, ' ')
-       .replace(/--[^\n]*/g, ' ')
-       .replace(/#[^\n]*/g, ' ')
-       .trim();
+     const s = statement.replace(/\/\*[\s\S]*?\*\//g, ' ').replace(/--[^\n]*/g, ' ').replace(/#[^\n]*/g, ' ');
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 19 more tests!


[Survived] BlockStatement
lib/guard/index.ts:45:52
-   export function sqlHead(statement: string): string {
-     const s = statement
-       .replace(/\/\*[\s\S]*?\*\//g, ' ')
-       .replace(/--[^\n]*/g, ' ')
-       .replace(/#[^\n]*/g, ' ')
-       .trim();
-     const m = /^[\s(]*([a-zA-Z_]+)/.exec(s);
-     return m?.[1]?.toLowerCase() ?? '';
-   }
+   export function sqlHead(statement: string): string {}
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 19 more tests!


[Survived] Regex
lib/guard/index.ts:47:14
-       .replace(/\/\*[\s\S]*?\*\//g, ' ')
+       .replace(/\/\*[\s\S]\*\//g, ' ')
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 19 more tests!


[Survived] Regex
lib/guard/index.ts:48:14
-       .replace(/--[^\n]*/g, ' ')
+       .replace(/--[^\n]/g, ' ')
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 19 more tests!


[Survived] Regex
lib/guard/index.ts:47:14
-       .replace(/\/\*[\s\S]*?\*\//g, ' ')
+       .replace(/\/\*[\S\S]*?\*\//g, ' ')
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 19 more tests!


[Survived] StringLiteral
lib/guard/index.ts:47:35
-       .replace(/\/\*[\s\S]*?\*\//g, ' ')
+       .replace(/\/\*[\s\S]*?\*\//g, "")
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 19 more tests!


[Survived] Regex
lib/guard/index.ts:47:14
-       .replace(/\/\*[\s\S]*?\*\//g, ' ')
+       .replace(/\/\*[\s\s]*?\*\//g, ' ')
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 19 more tests!


[Survived] Regex
lib/guard/index.ts:48:14
-       .replace(/--[^\n]*/g, ' ')
+       .replace(/--[\n]*/g, ' ')
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 19 more tests!


[Survived] Regex
lib/guard/index.ts:49:14
-       .replace(/#[^\n]*/g, ' ')
+       .replace(/#[^\n]/g, ' ')
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 19 more tests!


[Survived] StringLiteral
lib/guard/index.ts:48:27
-       .replace(/--[^\n]*/g, ' ')
+       .replace(/--[^\n]*/g, "")
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 19 more tests!


[Survived] Regex
lib/guard/index.ts:49:14
-       .replace(/#[^\n]*/g, ' ')
+       .replace(/#[\n]*/g, ' ')
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 19 more tests!


[Survived] StringLiteral
lib/guard/index.ts:49:26
-       .replace(/#[^\n]*/g, ' ')
+       .replace(/#[^\n]*/g, "")
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 19 more tests!


[Survived] Regex
lib/guard/index.ts:51:13
-     const m = /^[\s(]*([a-zA-Z_]+)/.exec(s);
+     const m = /[\s(]*([a-zA-Z_]+)/.exec(s);
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 19 more tests!


[Survived] Regex
lib/guard/index.ts:51:13
-     const m = /^[\s(]*([a-zA-Z_]+)/.exec(s);
+     const m = /^[\s(]([a-zA-Z_]+)/.exec(s);
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 19 more tests!


[Survived] Regex
lib/guard/index.ts:51:13
-     const m = /^[\s(]*([a-zA-Z_]+)/.exec(s);
+     const m = /^[^\s(]*([a-zA-Z_]+)/.exec(s);
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 19 more tests!


[Survived] Regex
lib/guard/index.ts:51:13
-     const m = /^[\s(]*([a-zA-Z_]+)/.exec(s);
+     const m = /^[\S(]*([a-zA-Z_]+)/.exec(s);
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 19 more tests!


[Survived] Regex
lib/guard/index.ts:51:13
-     const m = /^[\s(]*([a-zA-Z_]+)/.exec(s);
+     const m = /^[\s(]*([a-zA-Z_])/.exec(s);
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 19 more tests!


[Survived] Regex
lib/guard/index.ts:51:13
-     const m = /^[\s(]*([a-zA-Z_]+)/.exec(s);
+     const m = /^[\s(]*([^a-zA-Z_]+)/.exec(s);
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 19 more tests!


[Survived] LogicalOperator
lib/guard/index.ts:52:10
-     return m?.[1]?.toLowerCase() ?? '';
+     return m?.[1]?.toLowerCase() && '';
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 19 more tests!


[Survived] MethodExpression
lib/guard/index.ts:52:10
-     return m?.[1]?.toLowerCase() ?? '';
+     return m?.[1]?.toUpperCase() ?? '';
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 19 more tests!


[Survived] OptionalChaining
lib/guard/index.ts:52:10
-     return m?.[1]?.toLowerCase() ?? '';
+     return m?.[1].toLowerCase() ?? '';
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 19 more tests!


[Survived] OptionalChaining
lib/guard/index.ts:52:10
-     return m?.[1]?.toLowerCase() ?? '';
+     return m[1]?.toLowerCase() ?? '';
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 19 more tests!


[Survived] BlockStatement
lib/guard/index.ts:56:58
-   export function statementHash(statement: string): string {
-     return createHash('sha256').update(statement.trim(), 'utf8').digest('hex');
-   }
+   export function statementHash(statement: string): string {}
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 15 more tests!


[Survived] StringLiteral
lib/guard/index.ts:57:21
-     return createHash('sha256').update(statement.trim(), 'utf8').digest('hex');
+     return createHash("").update(statement.trim(), 'utf8').digest('hex');
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 15 more tests!


[Survived] MethodExpression
lib/guard/index.ts:57:38
-     return createHash('sha256').update(statement.trim(), 'utf8').digest('hex');
+     return createHash('sha256').update(statement, 'utf8').digest('hex');
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 15 more tests!


[Survived] StringLiteral
lib/guard/index.ts:57:56
-     return createHash('sha256').update(statement.trim(), 'utf8').digest('hex');
+     return createHash('sha256').update(statement.trim(), "").digest('hex');
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 15 more tests!


[Survived] StringLiteral
lib/guard/index.ts:57:71
-     return createHash('sha256').update(statement.trim(), 'utf8').digest('hex');
+     return createHash('sha256').update(statement.trim(), 'utf8').digest("");
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 15 more tests!


[Survived] BlockStatement
lib/guard/index.ts:65:107
-   export function classifyStatement(kind: DbKind, statement: string, op: 'query' | 'execute'): GuardVerdict {
-     if (kind === 'redis') return classifyRedis(statement);
-     if (kind === 'mongodb') return classifyMongo(statement);
-     return classifySql(statement, op);
-   }
+   export function classifyStatement(kind: DbKind, statement: string, op: 'query' | 'execute'): GuardVerdict {}
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 26 more tests!


[Survived] ConditionalExpression
lib/guard/index.ts:66:7
-     if (kind === 'redis') return classifyRedis(statement);
+     if (true) return classifyRedis(statement);
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 26 more tests!


[Survived] ConditionalExpression
lib/guard/index.ts:66:7
-     if (kind === 'redis') return classifyRedis(statement);
+     if (false) return classifyRedis(statement);
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 26 more tests!


[Survived] EqualityOperator
lib/guard/index.ts:66:7
-     if (kind === 'redis') return classifyRedis(statement);
+     if (kind !== 'redis') return classifyRedis(statement);
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 26 more tests!


[Survived] StringLiteral
lib/guard/index.ts:66:16
-     if (kind === 'redis') return classifyRedis(statement);
+     if (kind === "") return classifyRedis(statement);
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 26 more tests!


[Survived] ConditionalExpression
lib/guard/index.ts:67:7
-     if (kind === 'mongodb') return classifyMongo(statement);
+     if (false) return classifyMongo(statement);
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 22 more tests!


[Survived] ConditionalExpression
lib/guard/index.ts:67:7
-     if (kind === 'mongodb') return classifyMongo(statement);
+     if (true) return classifyMongo(statement);
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 22 more tests!


[Survived] EqualityOperator
lib/guard/index.ts:67:7
-     if (kind === 'mongodb') return classifyMongo(statement);
+     if (kind !== 'mongodb') return classifyMongo(statement);
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 22 more tests!


[Survived] StringLiteral
lib/guard/index.ts:67:16
-     if (kind === 'mongodb') return classifyMongo(statement);
+     if (kind === "") return classifyMongo(statement);
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 22 more tests!


[Survived] ConditionalExpression
lib/guard/index.ts:75:7
-     if (head === '') return { level: 'danger', reason: '无法解析语句关键字，按危险语句处理' };
+     if (true) return { level: 'danger', reason: '无法解析语句关键字，按危险语句处理' };
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 19 more tests!


[Survived] BlockStatement
lib/guard/index.ts:73:80
-   function classifySql(statement: string, op: 'query' | 'execute'): GuardVerdict {
-     const head = sqlHead(statement);
-     if (head === '') return { level: 'danger', reason: '无法解析语句关键字，按危险语句处理' };
-     const isRead = SQL_READ_HEADS.has(head);
-     if (isRead) {
-       if (SQL_HIDDEN_DANGER_RE.test(statement)) {
-         return { level: 'danger', reason: '读语句包含危险片段（多语句 / 文件读写 / 全局设置 / 锁读），需要确认' };
-       }
-       return { level: 'none' };
-     }
-     if (SQL_DDL_RE.test(statement)) return { level: 'danger', reason: 'DDL 不可回滚（隐式提交），可能破坏表结构或数据' };
-     if (SQL_DML_RE.test(statement)) {
-       if (op === 'query') return { level: 'danger', reason: '只读通道（query）出现写语句，需要确认' };
-       return { level: 'warning', reason: '写操作将修改数据' };
-     }
-     if (SQL_MAINT_RE.test(statement)) return { level: 'danger', reason: '维护/管理命令影响服务器状态，需要确认' };
-     // 未知关键字：execute 通道 warning（适配器只读兜底），query 通道 danger
-     if (op === 'query') return { level: 'danger', reason: `只读通道（query）出现非读取语句（${head}），需要确认` };
-     return { level: 'warning', reason: `未识别的语句类型（${head}），请确认后执行` };
-   }
+   function classifySql(statement: string, op: 'query' | 'execute'): GuardVerdict {}
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 19 more tests!


[Survived] ConditionalExpression
lib/guard/index.ts:75:7
-     if (head === '') return { level: 'danger', reason: '无法解析语句关键字，按危险语句处理' };
+     if (false) return { level: 'danger', reason: '无法解析语句关键字，按危险语句处理' };
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 19 more tests!


[Survived] EqualityOperator
lib/guard/index.ts:75:7
-     if (head === '') return { level: 'danger', reason: '无法解析语句关键字，按危险语句处理' };
+     if (head !== '') return { level: 'danger', reason: '无法解析语句关键字，按危险语句处理' };
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 19 more tests!


[Survived] StringLiteral
lib/guard/index.ts:75:16
-     if (head === '') return { level: 'danger', reason: '无法解析语句关键字，按危险语句处理' };
+     if (head === "Stryker was here!") return { level: 'danger', reason: '无法解析语句关键字，按危险语句处理' };
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 19 more tests!


[Survived] ConditionalExpression
lib/guard/index.ts:78:7
-     if (isRead) {
+     if (true) {
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 19 more tests!


[Survived] ConditionalExpression
lib/guard/index.ts:78:7
-     if (isRead) {
+     if (false) {
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 19 more tests!


[Survived] ConditionalExpression
lib/guard/index.ts:79:9
-       if (SQL_HIDDEN_DANGER_RE.test(statement)) {
+       if (false) {
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
  and 6 more tests!


[Survived] BlockStatement
lib/guard/index.ts:79:47
-       if (SQL_HIDDEN_DANGER_RE.test(statement)) {
-         return { level: 'danger', reason: '读语句包含危险片段（多语句 / 文件读写 / 全局设置 / 锁读），需要确认' };
-       }
+       if (SQL_HIDDEN_DANGER_RE.test(statement)) {}
Tests ran:
    classifyStatement: SQL 系 读语句内的危险片段升级为 danger


[Survived] BlockStatement
lib/guard/index.ts:78:15
-     if (isRead) {
-       if (SQL_HIDDEN_DANGER_RE.test(statement)) {
-         return { level: 'danger', reason: '读语句包含危险片段（多语句 / 文件读写 / 全局设置 / 锁读），需要确认' };
-       }
-       return { level: 'none' };
-     }
+     if (isRead) {}
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
  and 6 more tests!


[Survived] ConditionalExpression
lib/guard/index.ts:79:9
-       if (SQL_HIDDEN_DANGER_RE.test(statement)) {
+       if (true) {
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
  and 6 more tests!


[Survived] ObjectLiteral
lib/guard/index.ts:80:14
-         return { level: 'danger', reason: '读语句包含危险片段（多语句 / 文件读写 / 全局设置 / 锁读），需要确认' };
+         return {};
Tests ran:
    classifyStatement: SQL 系 读语句内的危险片段升级为 danger


[Survived] StringLiteral
lib/guard/index.ts:80:23
-         return { level: 'danger', reason: '读语句包含危险片段（多语句 / 文件读写 / 全局设置 / 锁读），需要确认' };
+         return { level: "", reason: '读语句包含危险片段（多语句 / 文件读写 / 全局设置 / 锁读），需要确认' };
Tests ran:
    classifyStatement: SQL 系 读语句内的危险片段升级为 danger


[Survived] StringLiteral
lib/guard/index.ts:80:41
-         return { level: 'danger', reason: '读语句包含危险片段（多语句 / 文件读写 / 全局设置 / 锁读），需要确认' };
+         return { level: 'danger', reason: "" };
Tests ran:
    classifyStatement: SQL 系 读语句内的危险片段升级为 danger


[Survived] ObjectLiteral
lib/guard/index.ts:82:12
-       return { level: 'none' };
+       return {};
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
  and 5 more tests!


[Survived] StringLiteral
lib/guard/index.ts:82:21
-       return { level: 'none' };
+       return { level: "" };
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
  and 5 more tests!


[Survived] ConditionalExpression
lib/guard/index.ts:85:7
-     if (SQL_DDL_RE.test(statement)) return { level: 'danger', reason: 'DDL 不可回滚（隐式提交），可能破坏表结构或数据' };
+     if (true) return { level: 'danger', reason: 'DDL 不可回滚（隐式提交），可能破坏表结构或数据' };
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 14 more tests!


[Survived] ConditionalExpression
lib/guard/index.ts:85:7
-     if (SQL_DDL_RE.test(statement)) return { level: 'danger', reason: 'DDL 不可回滚（隐式提交），可能破坏表结构或数据' };
+     if (false) return { level: 'danger', reason: 'DDL 不可回滚（隐式提交），可能破坏表结构或数据' };
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 14 more tests!


[Survived] ObjectLiteral
lib/guard/index.ts:85:42
-     if (SQL_DDL_RE.test(statement)) return { level: 'danger', reason: 'DDL 不可回滚（隐式提交），可能破坏表结构或数据' };
+     if (SQL_DDL_RE.test(statement)) return {};
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 8 more tests!


[Survived] ConditionalExpression
lib/guard/index.ts:86:7
-     if (SQL_DML_RE.test(statement)) {
+     if (true) {
Tests ran:
    DbToolService: 授权与只读 rw 连接 mode=rw 透传，DML 直接执行
    DbToolService: 危险确认与 challenge query 通道的写语句同样需要确认；SELECT 直通
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
  and 3 more tests!


[Survived] StringLiteral
lib/guard/index.ts:85:51
-     if (SQL_DDL_RE.test(statement)) return { level: 'danger', reason: 'DDL 不可回滚（隐式提交），可能破坏表结构或数据' };
+     if (SQL_DDL_RE.test(statement)) return { level: "", reason: 'DDL 不可回滚（隐式提交），可能破坏表结构或数据' };
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 8 more tests!


[Survived] StringLiteral
lib/guard/index.ts:85:69
-     if (SQL_DDL_RE.test(statement)) return { level: 'danger', reason: 'DDL 不可回滚（隐式提交），可能破坏表结构或数据' };
+     if (SQL_DDL_RE.test(statement)) return { level: 'danger', reason: "" };
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 8 more tests!


[Survived] ConditionalExpression
lib/guard/index.ts:86:7
-     if (SQL_DML_RE.test(statement)) {
+     if (false) {
Tests ran:
    DbToolService: 授权与只读 rw 连接 mode=rw 透传，DML 直接执行
    DbToolService: 危险确认与 challenge query 通道的写语句同样需要确认；SELECT 直通
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
  and 3 more tests!


[Survived] BlockStatement
lib/guard/index.ts:86:35
-     if (SQL_DML_RE.test(statement)) {
-       if (op === 'query') return { level: 'danger', reason: '只读通道（query）出现写语句，需要确认' };
-       return { level: 'warning', reason: '写操作将修改数据' };
-     }
+     if (SQL_DML_RE.test(statement)) {}
Tests ran:
    DbToolService: 授权与只读 rw 连接 mode=rw 透传，DML 直接执行
    DbToolService: 危险确认与 challenge query 通道的写语句同样需要确认；SELECT 直通
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
  and 1 more test!


[Survived] ConditionalExpression
lib/guard/index.ts:87:9
-       if (op === 'query') return { level: 'danger', reason: '只读通道（query）出现写语句，需要确认' };
+       if (true) return { level: 'danger', reason: '只读通道（query）出现写语句，需要确认' };
Tests ran:
    DbToolService: 授权与只读 rw 连接 mode=rw 透传，DML 直接执行
    DbToolService: 危险确认与 challenge query 通道的写语句同样需要确认；SELECT 直通
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
  and 1 more test!


[Survived] ConditionalExpression
lib/guard/index.ts:87:9
-       if (op === 'query') return { level: 'danger', reason: '只读通道（query）出现写语句，需要确认' };
+       if (false) return { level: 'danger', reason: '只读通道（query）出现写语句，需要确认' };
Tests ran:
    DbToolService: 授权与只读 rw 连接 mode=rw 透传，DML 直接执行
    DbToolService: 危险确认与 challenge query 通道的写语句同样需要确认；SELECT 直通
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
  and 1 more test!


[Survived] EqualityOperator
lib/guard/index.ts:87:9
-       if (op === 'query') return { level: 'danger', reason: '只读通道（query）出现写语句，需要确认' };
+       if (op !== 'query') return { level: 'danger', reason: '只读通道（query）出现写语句，需要确认' };
Tests ran:
    DbToolService: 授权与只读 rw 连接 mode=rw 透传，DML 直接执行
    DbToolService: 危险确认与 challenge query 通道的写语句同样需要确认；SELECT 直通
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
  and 1 more test!


[Survived] StringLiteral
lib/guard/index.ts:87:16
-       if (op === 'query') return { level: 'danger', reason: '只读通道（query）出现写语句，需要确认' };
+       if (op === "") return { level: 'danger', reason: '只读通道（query）出现写语句，需要确认' };
Tests ran:
    DbToolService: 授权与只读 rw 连接 mode=rw 透传，DML 直接执行
    DbToolService: 危险确认与 challenge query 通道的写语句同样需要确认；SELECT 直通
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
  and 1 more test!


[Survived] ObjectLiteral
lib/guard/index.ts:87:32
-       if (op === 'query') return { level: 'danger', reason: '只读通道（query）出现写语句，需要确认' };
+       if (op === 'query') return {};
Tests ran:
    DbToolService: 危险确认与 challenge query 通道的写语句同样需要确认；SELECT 直通
    classifyStatement: SQL 系 DML 在 execute 通道为 warning，在 query 通道为 danger


[Survived] StringLiteral
lib/guard/index.ts:87:41
-       if (op === 'query') return { level: 'danger', reason: '只读通道（query）出现写语句，需要确认' };
+       if (op === 'query') return { level: "", reason: '只读通道（query）出现写语句，需要确认' };
Tests ran:
    DbToolService: 危险确认与 challenge query 通道的写语句同样需要确认；SELECT 直通
    classifyStatement: SQL 系 DML 在 execute 通道为 warning，在 query 通道为 danger


[Survived] StringLiteral
lib/guard/index.ts:87:59
-       if (op === 'query') return { level: 'danger', reason: '只读通道（query）出现写语句，需要确认' };
+       if (op === 'query') return { level: 'danger', reason: "" };
Tests ran:
    DbToolService: 危险确认与 challenge query 通道的写语句同样需要确认；SELECT 直通
    classifyStatement: SQL 系 DML 在 execute 通道为 warning，在 query 通道为 danger


[Survived] ObjectLiteral
lib/guard/index.ts:88:12
-       return { level: 'warning', reason: '写操作将修改数据' };
+       return {};
Tests ran:
    DbToolService: 授权与只读 rw 连接 mode=rw 透传，DML 直接执行
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    classifyStatement: SQL 系 DML 在 execute 通道为 warning，在 query 通道为 danger


[Survived] ConditionalExpression
lib/guard/index.ts:90:7
-     if (SQL_MAINT_RE.test(statement)) return { level: 'danger', reason: '维护/管理命令影响服务器状态，需要确认' };
+     if (true) return { level: 'danger', reason: '维护/管理命令影响服务器状态，需要确认' };
Tests ran:
    classifyStatement: SQL 系 维护命令为 danger
    classifyStatement: SQL 系 未知关键字：query 通道 danger，execute 通道 warning


[Survived] StringLiteral
lib/guard/index.ts:88:21
-       return { level: 'warning', reason: '写操作将修改数据' };
+       return { level: "", reason: '写操作将修改数据' };
Tests ran:
    DbToolService: 授权与只读 rw 连接 mode=rw 透传，DML 直接执行
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    classifyStatement: SQL 系 DML 在 execute 通道为 warning，在 query 通道为 danger


[Survived] ConditionalExpression
lib/guard/index.ts:90:7
-     if (SQL_MAINT_RE.test(statement)) return { level: 'danger', reason: '维护/管理命令影响服务器状态，需要确认' };
+     if (false) return { level: 'danger', reason: '维护/管理命令影响服务器状态，需要确认' };
Tests ran:
    classifyStatement: SQL 系 维护命令为 danger
    classifyStatement: SQL 系 未知关键字：query 通道 danger，execute 通道 warning


[Survived] StringLiteral
lib/guard/index.ts:88:40
-       return { level: 'warning', reason: '写操作将修改数据' };
+       return { level: 'warning', reason: "" };
Tests ran:
    DbToolService: 授权与只读 rw 连接 mode=rw 透传，DML 直接执行
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    classifyStatement: SQL 系 DML 在 execute 通道为 warning，在 query 通道为 danger


[Survived] StringLiteral
lib/guard/index.ts:90:53
-     if (SQL_MAINT_RE.test(statement)) return { level: 'danger', reason: '维护/管理命令影响服务器状态，需要确认' };
+     if (SQL_MAINT_RE.test(statement)) return { level: "", reason: '维护/管理命令影响服务器状态，需要确认' };
Tests ran:
    classifyStatement: SQL 系 维护命令为 danger


[Survived] ObjectLiteral
lib/guard/index.ts:90:44
-     if (SQL_MAINT_RE.test(statement)) return { level: 'danger', reason: '维护/管理命令影响服务器状态，需要确认' };
+     if (SQL_MAINT_RE.test(statement)) return {};
Tests ran:
    classifyStatement: SQL 系 维护命令为 danger


[Survived] StringLiteral
lib/guard/index.ts:90:71
-     if (SQL_MAINT_RE.test(statement)) return { level: 'danger', reason: '维护/管理命令影响服务器状态，需要确认' };
+     if (SQL_MAINT_RE.test(statement)) return { level: 'danger', reason: "" };
Tests ran:
    classifyStatement: SQL 系 维护命令为 danger


[Survived] ConditionalExpression
lib/guard/index.ts:93:7
-     if (op === 'query') return { level: 'danger', reason: `只读通道（query）出现非读取语句（${head}），需要确认` };
+     if (true) return { level: 'danger', reason: `只读通道（query）出现非读取语句（${head}），需要确认` };
Tests ran:
    classifyStatement: SQL 系 未知关键字：query 通道 danger，execute 通道 warning


[Survived] EqualityOperator
lib/guard/index.ts:93:7
-     if (op === 'query') return { level: 'danger', reason: `只读通道（query）出现非读取语句（${head}），需要确认` };
+     if (op !== 'query') return { level: 'danger', reason: `只读通道（query）出现非读取语句（${head}），需要确认` };
Tests ran:
    classifyStatement: SQL 系 未知关键字：query 通道 danger，execute 通道 warning


[Survived] StringLiteral
lib/guard/index.ts:93:14
-     if (op === 'query') return { level: 'danger', reason: `只读通道（query）出现非读取语句（${head}），需要确认` };
+     if (op === "") return { level: 'danger', reason: `只读通道（query）出现非读取语句（${head}），需要确认` };
Tests ran:
    classifyStatement: SQL 系 未知关键字：query 通道 danger，execute 通道 warning


[Survived] ConditionalExpression
lib/guard/index.ts:93:7
-     if (op === 'query') return { level: 'danger', reason: `只读通道（query）出现非读取语句（${head}），需要确认` };
+     if (false) return { level: 'danger', reason: `只读通道（query）出现非读取语句（${head}），需要确认` };
Tests ran:
    classifyStatement: SQL 系 未知关键字：query 通道 danger，execute 通道 warning


[Survived] ObjectLiteral
lib/guard/index.ts:93:30
-     if (op === 'query') return { level: 'danger', reason: `只读通道（query）出现非读取语句（${head}），需要确认` };
+     if (op === 'query') return {};
Tests ran:
    classifyStatement: SQL 系 未知关键字：query 通道 danger，execute 通道 warning


[Survived] StringLiteral
lib/guard/index.ts:93:39
-     if (op === 'query') return { level: 'danger', reason: `只读通道（query）出现非读取语句（${head}），需要确认` };
+     if (op === 'query') return { level: "", reason: `只读通道（query）出现非读取语句（${head}），需要确认` };
Tests ran:
    classifyStatement: SQL 系 未知关键字：query 通道 danger，execute 通道 warning


[Survived] ObjectLiteral
lib/guard/index.ts:94:10
-     return { level: 'warning', reason: `未识别的语句类型（${head}），请确认后执行` };
+     return {};
Tests ran:
    classifyStatement: SQL 系 未知关键字：query 通道 danger，execute 通道 warning


[Survived] StringLiteral
lib/guard/index.ts:93:57
-     if (op === 'query') return { level: 'danger', reason: `只读通道（query）出现非读取语句（${head}），需要确认` };
+     if (op === 'query') return { level: 'danger', reason: `` };
Tests ran:
    classifyStatement: SQL 系 未知关键字：query 通道 danger，execute 通道 warning


[Survived] StringLiteral
lib/guard/index.ts:94:19
-     return { level: 'warning', reason: `未识别的语句类型（${head}），请确认后执行` };
+     return { level: "", reason: `未识别的语句类型（${head}），请确认后执行` };
Tests ran:
    classifyStatement: SQL 系 未知关键字：query 通道 danger，execute 通道 warning


[Survived] StringLiteral
lib/guard/index.ts:94:38
-     return { level: 'warning', reason: `未识别的语句类型（${head}），请确认后执行` };
+     return { level: 'warning', reason: `` };
Tests ran:
    classifyStatement: SQL 系 未知关键字：query 通道 danger，execute 通道 warning


[Survived] MethodExpression
lib/guard/index.ts:101:16
-     const head = (parts[0] ?? '').toUpperCase();
+     const head = (parts[0] ?? '').toLowerCase();
Tests ran:
    classifyStatement: Redis 危险命令 danger（13 项清单）
    classifyStatement: Redis 读命令 none、写命令 warning
    classifyStatement: Redis JSON 数组形式同样解析
  and 1 more test!


[Survived] BlockStatement
lib/guard/index.ts:99:57
-   function classifyRedis(statement: string): GuardVerdict {
-     const parts = parseRedisCommand(statement);
-     const head = (parts[0] ?? '').toUpperCase();
-     if (head === '') return { level: 'danger', reason: '无法解析 Redis 命令，按危险命令处理' };
-     if (DANGEROUS_COMMANDS.has(head)) {
-       return { level: 'danger', reason: `Redis 危险命令 ${head}，可能清空/重配置实例，需要确认` };
-     }
-     if (WRITE_COMMANDS.has(head)) {
-       return { level: 'warning', reason: `Redis 写命令 ${head} 将修改数据` };
-     }
-     if (READ_COMMANDS.has(head)) return { level: 'none' };
-     return { level: 'warning', reason: `未识别的 Redis 命令（${head}），请确认后执行` };
-   }
+   function classifyRedis(statement: string): GuardVerdict {}
Tests ran:
    classifyStatement: Redis 危险命令 danger（13 项清单）
    classifyStatement: Redis 读命令 none、写命令 warning
    classifyStatement: Redis JSON 数组形式同样解析
  and 1 more test!


[Survived] LogicalOperator
lib/guard/index.ts:101:17
-     const head = (parts[0] ?? '').toUpperCase();
+     const head = (parts[0] && '').toUpperCase();
Tests ran:
    classifyStatement: Redis 危险命令 danger（13 项清单）
    classifyStatement: Redis 读命令 none、写命令 warning
    classifyStatement: Redis JSON 数组形式同样解析
  and 1 more test!


[Survived] ConditionalExpression
lib/guard/index.ts:102:7
-     if (head === '') return { level: 'danger', reason: '无法解析 Redis 命令，按危险命令处理' };
+     if (true) return { level: 'danger', reason: '无法解析 Redis 命令，按危险命令处理' };
Tests ran:
    classifyStatement: Redis 危险命令 danger（13 项清单）
    classifyStatement: Redis 读命令 none、写命令 warning
    classifyStatement: Redis JSON 数组形式同样解析
  and 1 more test!


[Survived] ConditionalExpression
lib/guard/index.ts:102:7
-     if (head === '') return { level: 'danger', reason: '无法解析 Redis 命令，按危险命令处理' };
+     if (false) return { level: 'danger', reason: '无法解析 Redis 命令，按危险命令处理' };
Tests ran:
    classifyStatement: Redis 危险命令 danger（13 项清单）
    classifyStatement: Redis 读命令 none、写命令 warning
    classifyStatement: Redis JSON 数组形式同样解析
  and 1 more test!


[Survived] EqualityOperator
lib/guard/index.ts:102:7
-     if (head === '') return { level: 'danger', reason: '无法解析 Redis 命令，按危险命令处理' };
+     if (head !== '') return { level: 'danger', reason: '无法解析 Redis 命令，按危险命令处理' };
Tests ran:
    classifyStatement: Redis 危险命令 danger（13 项清单）
    classifyStatement: Redis 读命令 none、写命令 warning
    classifyStatement: Redis JSON 数组形式同样解析
  and 1 more test!


[Survived] StringLiteral
lib/guard/index.ts:102:16
-     if (head === '') return { level: 'danger', reason: '无法解析 Redis 命令，按危险命令处理' };
+     if (head === "Stryker was here!") return { level: 'danger', reason: '无法解析 Redis 命令，按危险命令处理' };
Tests ran:
    classifyStatement: Redis 危险命令 danger（13 项清单）
    classifyStatement: Redis 读命令 none、写命令 warning
    classifyStatement: Redis JSON 数组形式同样解析
  and 1 more test!


[Survived] ConditionalExpression
lib/guard/index.ts:104:7
-     if (DANGEROUS_COMMANDS.has(head)) {
+     if (true) {
Tests ran:
    classifyStatement: Redis 危险命令 danger（13 项清单）
    classifyStatement: Redis 读命令 none、写命令 warning
    classifyStatement: Redis JSON 数组形式同样解析
  and 1 more test!


[Survived] ConditionalExpression
lib/guard/index.ts:104:7
-     if (DANGEROUS_COMMANDS.has(head)) {
+     if (false) {
Tests ran:
    classifyStatement: Redis 危险命令 danger（13 项清单）
    classifyStatement: Redis 读命令 none、写命令 warning
    classifyStatement: Redis JSON 数组形式同样解析
  and 1 more test!


[Survived] BlockStatement
lib/guard/index.ts:104:37
-     if (DANGEROUS_COMMANDS.has(head)) {
-       return { level: 'danger', reason: `Redis 危险命令 ${head}，可能清空/重配置实例，需要确认` };
-     }
+     if (DANGEROUS_COMMANDS.has(head)) {}
Tests ran:
    classifyStatement: Redis 危险命令 danger（13 项清单）
    classifyStatement: Redis JSON 数组形式同样解析


[Survived] ObjectLiteral
lib/guard/index.ts:105:12
-       return { level: 'danger', reason: `Redis 危险命令 ${head}，可能清空/重配置实例，需要确认` };
+       return {};
Tests ran:
    classifyStatement: Redis 危险命令 danger（13 项清单）
    classifyStatement: Redis JSON 数组形式同样解析


[Survived] StringLiteral
lib/guard/index.ts:105:21
-       return { level: 'danger', reason: `Redis 危险命令 ${head}，可能清空/重配置实例，需要确认` };
+       return { level: "", reason: `Redis 危险命令 ${head}，可能清空/重配置实例，需要确认` };
Tests ran:
    classifyStatement: Redis 危险命令 danger（13 项清单）
    classifyStatement: Redis JSON 数组形式同样解析


[Survived] StringLiteral
lib/guard/index.ts:105:39
-       return { level: 'danger', reason: `Redis 危险命令 ${head}，可能清空/重配置实例，需要确认` };
+       return { level: 'danger', reason: `` };
Tests ran:
    classifyStatement: Redis 危险命令 danger（13 项清单）
    classifyStatement: Redis JSON 数组形式同样解析


[Survived] ConditionalExpression
lib/guard/index.ts:107:7
-     if (WRITE_COMMANDS.has(head)) {
+     if (true) {
Tests ran:
    classifyStatement: Redis 读命令 none、写命令 warning
    classifyStatement: Redis JSON 数组形式同样解析
    classifyStatement: Redis 未知命令 warning


[Survived] ConditionalExpression
lib/guard/index.ts:107:7
-     if (WRITE_COMMANDS.has(head)) {
+     if (false) {
Tests ran:
    classifyStatement: Redis 读命令 none、写命令 warning
    classifyStatement: Redis JSON 数组形式同样解析
    classifyStatement: Redis 未知命令 warning


[Survived] BlockStatement
lib/guard/index.ts:107:33
-     if (WRITE_COMMANDS.has(head)) {
-       return { level: 'warning', reason: `Redis 写命令 ${head} 将修改数据` };
-     }
+     if (WRITE_COMMANDS.has(head)) {}
Tests ran:
    classifyStatement: Redis 读命令 none、写命令 warning


[Survived] ObjectLiteral
lib/guard/index.ts:108:12
-       return { level: 'warning', reason: `Redis 写命令 ${head} 将修改数据` };
+       return {};
Tests ran:
    classifyStatement: Redis 读命令 none、写命令 warning


[Survived] StringLiteral
lib/guard/index.ts:108:21
-       return { level: 'warning', reason: `Redis 写命令 ${head} 将修改数据` };
+       return { level: "", reason: `Redis 写命令 ${head} 将修改数据` };
Tests ran:
    classifyStatement: Redis 读命令 none、写命令 warning


[Survived] StringLiteral
lib/guard/index.ts:108:40
-       return { level: 'warning', reason: `Redis 写命令 ${head} 将修改数据` };
+       return { level: 'warning', reason: `` };
Tests ran:
    classifyStatement: Redis 读命令 none、写命令 warning


[Survived] ConditionalExpression
lib/guard/index.ts:110:7
-     if (READ_COMMANDS.has(head)) return { level: 'none' };
+     if (true) return { level: 'none' };
Tests ran:
    classifyStatement: Redis 读命令 none、写命令 warning
    classifyStatement: Redis JSON 数组形式同样解析
    classifyStatement: Redis 未知命令 warning


[Survived] ConditionalExpression
lib/guard/index.ts:110:7
-     if (READ_COMMANDS.has(head)) return { level: 'none' };
+     if (false) return { level: 'none' };
Tests ran:
    classifyStatement: Redis 读命令 none、写命令 warning
    classifyStatement: Redis JSON 数组形式同样解析
    classifyStatement: Redis 未知命令 warning


[Survived] ObjectLiteral
lib/guard/index.ts:110:39
-     if (READ_COMMANDS.has(head)) return { level: 'none' };
+     if (READ_COMMANDS.has(head)) return {};
Tests ran:
    classifyStatement: Redis 读命令 none、写命令 warning
    classifyStatement: Redis JSON 数组形式同样解析


[Survived] StringLiteral
lib/guard/index.ts:110:48
-     if (READ_COMMANDS.has(head)) return { level: 'none' };
+     if (READ_COMMANDS.has(head)) return { level: "" };
Tests ran:
    classifyStatement: Redis 读命令 none、写命令 warning
    classifyStatement: Redis JSON 数组形式同样解析


[Survived] ObjectLiteral
lib/guard/index.ts:111:10
-     return { level: 'warning', reason: `未识别的 Redis 命令（${head}），请确认后执行` };
+     return {};
Tests ran:
    classifyStatement: Redis 未知命令 warning


[Survived] StringLiteral
lib/guard/index.ts:111:38
-     return { level: 'warning', reason: `未识别的 Redis 命令（${head}），请确认后执行` };
+     return { level: 'warning', reason: `` };
Tests ran:
    classifyStatement: Redis 未知命令 warning


[Survived] StringLiteral
lib/guard/index.ts:111:19
-     return { level: 'warning', reason: `未识别的 Redis 命令（${head}），请确认后执行` };
+     return { level: "", reason: `未识别的 Redis 命令（${head}），请确认后执行` };
Tests ran:
    classifyStatement: Redis 未知命令 warning


[Survived] BlockStatement
lib/guard/index.ts:117:57
-   function classifyMongo(statement: string): GuardVerdict {
-     let head = '';
-     try {
-       const parsed: unknown = JSON.parse(statement);
-       if (parsed && typeof parsed === 'object' && !Array.isArray(parsed)) {
-         head = Object.keys(parsed as Record<string, unknown>)[0] ?? '';
-       }
-     } catch {
-       return { level: 'danger', reason: 'MongoDB 命令不是合法 JSON 对象，按危险命令处理' };
-     }
-     if (head === '') return { level: 'danger', reason: '无法解析 MongoDB 命令，按危险命令处理' };
-     if (DANGEROUS_OPS.has(head)) {
-       return { level: 'danger', reason: `MongoDB 危险操作 ${head}，可能删库/删集合，需要确认` };
-     }
-     if (MONGO_WRITE_OPS.has(head)) {
-       return { level: 'warning', reason: `MongoDB 写操作 ${head} 将修改数据` };
-     }
-     return { level: 'none' };
-   }
+   function classifyMongo(statement: string): GuardVerdict {}
Tests ran:
    classifyStatement: MongoDB 危险操作 danger（8 项清单）
    classifyStatement: MongoDB 写操作 warning、读操作 none
    classifyStatement: MongoDB 非法 JSON danger


[Survived] StringLiteral
lib/guard/index.ts:118:14
-     let head = '';
+     let head = "Stryker was here!";
Tests ran:
    classifyStatement: MongoDB 危险操作 danger（8 项清单）
    classifyStatement: MongoDB 写操作 warning、读操作 none
    classifyStatement: MongoDB 非法 JSON danger


[Survived] BlockStatement
lib/guard/index.ts:119:7
-     try {
-       const parsed: unknown = JSON.parse(statement);
-       if (parsed && typeof parsed === 'object' && !Array.isArray(parsed)) {
-         head = Object.keys(parsed as Record<string, unknown>)[0] ?? '';
-       }
-     } catch {
+     try {} catch {
Tests ran:
    classifyStatement: MongoDB 危险操作 danger（8 项清单）
    classifyStatement: MongoDB 写操作 warning、读操作 none
    classifyStatement: MongoDB 非法 JSON danger


[Survived] ConditionalExpression
lib/guard/index.ts:121:9
-       if (parsed && typeof parsed === 'object' && !Array.isArray(parsed)) {
+       if (true) {
Tests ran:
    classifyStatement: MongoDB 危险操作 danger（8 项清单）
    classifyStatement: MongoDB 写操作 warning、读操作 none


[Survived] ConditionalExpression
lib/guard/index.ts:121:9
-       if (parsed && typeof parsed === 'object' && !Array.isArray(parsed)) {
+       if (false) {
Tests ran:
    classifyStatement: MongoDB 危险操作 danger（8 项清单）
    classifyStatement: MongoDB 写操作 warning、读操作 none


[Survived] LogicalOperator
lib/guard/index.ts:121:9
-       if (parsed && typeof parsed === 'object' && !Array.isArray(parsed)) {
+       if (parsed && typeof parsed === 'object' || !Array.isArray(parsed)) {
Tests ran:
    classifyStatement: MongoDB 危险操作 danger（8 项清单）
    classifyStatement: MongoDB 写操作 warning、读操作 none


[Survived] ConditionalExpression
lib/guard/index.ts:121:9
-       if (parsed && typeof parsed === 'object' && !Array.isArray(parsed)) {
+       if (true && !Array.isArray(parsed)) {
Tests ran:
    classifyStatement: MongoDB 危险操作 danger（8 项清单）
    classifyStatement: MongoDB 写操作 warning、读操作 none


[Survived] LogicalOperator
lib/guard/index.ts:121:9
-       if (parsed && typeof parsed === 'object' && !Array.isArray(parsed)) {
+       if (parsed || typeof parsed === 'object' && !Array.isArray(parsed)) {
Tests ran:
    classifyStatement: MongoDB 危险操作 danger（8 项清单）
    classifyStatement: MongoDB 写操作 warning、读操作 none


[Survived] ConditionalExpression
lib/guard/index.ts:121:19
-       if (parsed && typeof parsed === 'object' && !Array.isArray(parsed)) {
+       if (parsed && true && !Array.isArray(parsed)) {
Tests ran:
    classifyStatement: MongoDB 危险操作 danger（8 项清单）
    classifyStatement: MongoDB 写操作 warning、读操作 none


[Survived] EqualityOperator
lib/guard/index.ts:121:19
-       if (parsed && typeof parsed === 'object' && !Array.isArray(parsed)) {
+       if (parsed && typeof parsed !== 'object' && !Array.isArray(parsed)) {
Tests ran:
    classifyStatement: MongoDB 危险操作 danger（8 项清单）
    classifyStatement: MongoDB 写操作 warning、读操作 none


[Survived] StringLiteral
lib/guard/index.ts:121:37
-       if (parsed && typeof parsed === 'object' && !Array.isArray(parsed)) {
+       if (parsed && typeof parsed === "" && !Array.isArray(parsed)) {
Tests ran:
    classifyStatement: MongoDB 危险操作 danger（8 项清单）
    classifyStatement: MongoDB 写操作 warning、读操作 none


[Survived] BooleanLiteral
lib/guard/index.ts:121:49
-       if (parsed && typeof parsed === 'object' && !Array.isArray(parsed)) {
+       if (parsed && typeof parsed === 'object' && Array.isArray(parsed)) {
Tests ran:
    classifyStatement: MongoDB 危险操作 danger（8 项清单）
    classifyStatement: MongoDB 写操作 warning、读操作 none


[Survived] BlockStatement
lib/guard/index.ts:124:11
-     } catch {
-       return { level: 'danger', reason: 'MongoDB 命令不是合法 JSON 对象，按危险命令处理' };
-     }
+     } catch {}
Tests ran:
    classifyStatement: MongoDB 非法 JSON danger


[Survived] BlockStatement
lib/guard/index.ts:121:73
-       if (parsed && typeof parsed === 'object' && !Array.isArray(parsed)) {
-         head = Object.keys(parsed as Record<string, unknown>)[0] ?? '';
-       }
+       if (parsed && typeof parsed === 'object' && !Array.isArray(parsed)) {}
Tests ran:
    classifyStatement: MongoDB 危险操作 danger（8 项清单）
    classifyStatement: MongoDB 写操作 warning、读操作 none


[Survived] LogicalOperator
lib/guard/index.ts:122:14
-         head = Object.keys(parsed as Record<string, unknown>)[0] ?? '';
+         head = Object.keys(parsed as Record<string, unknown>)[0] && '';
Tests ran:
    classifyStatement: MongoDB 危险操作 danger（8 项清单）
    classifyStatement: MongoDB 写操作 warning、读操作 none


[Survived] ObjectLiteral
lib/guard/index.ts:125:12
-       return { level: 'danger', reason: 'MongoDB 命令不是合法 JSON 对象，按危险命令处理' };
+       return {};
Tests ran:
    classifyStatement: MongoDB 非法 JSON danger


[Survived] StringLiteral
lib/guard/index.ts:125:21
-       return { level: 'danger', reason: 'MongoDB 命令不是合法 JSON 对象，按危险命令处理' };
+       return { level: "", reason: 'MongoDB 命令不是合法 JSON 对象，按危险命令处理' };
Tests ran:
    classifyStatement: MongoDB 非法 JSON danger


[Survived] ConditionalExpression
lib/guard/index.ts:127:7
-     if (head === '') return { level: 'danger', reason: '无法解析 MongoDB 命令，按危险命令处理' };
+     if (true) return { level: 'danger', reason: '无法解析 MongoDB 命令，按危险命令处理' };
Tests ran:
    classifyStatement: MongoDB 危险操作 danger（8 项清单）
    classifyStatement: MongoDB 写操作 warning、读操作 none


[Survived] StringLiteral
lib/guard/index.ts:125:39
-       return { level: 'danger', reason: 'MongoDB 命令不是合法 JSON 对象，按危险命令处理' };
+       return { level: 'danger', reason: "" };
Tests ran:
    classifyStatement: MongoDB 非法 JSON danger


[Survived] ConditionalExpression
lib/guard/index.ts:127:7
-     if (head === '') return { level: 'danger', reason: '无法解析 MongoDB 命令，按危险命令处理' };
+     if (false) return { level: 'danger', reason: '无法解析 MongoDB 命令，按危险命令处理' };
Tests ran:
    classifyStatement: MongoDB 危险操作 danger（8 项清单）
    classifyStatement: MongoDB 写操作 warning、读操作 none


[Survived] StringLiteral
lib/guard/index.ts:127:16
-     if (head === '') return { level: 'danger', reason: '无法解析 MongoDB 命令，按危险命令处理' };
+     if (head === "Stryker was here!") return { level: 'danger', reason: '无法解析 MongoDB 命令，按危险命令处理' };
Tests ran:
    classifyStatement: MongoDB 危险操作 danger（8 项清单）
    classifyStatement: MongoDB 写操作 warning、读操作 none


[Survived] EqualityOperator
lib/guard/index.ts:127:7
-     if (head === '') return { level: 'danger', reason: '无法解析 MongoDB 命令，按危险命令处理' };
+     if (head !== '') return { level: 'danger', reason: '无法解析 MongoDB 命令，按危险命令处理' };
Tests ran:
    classifyStatement: MongoDB 危险操作 danger（8 项清单）
    classifyStatement: MongoDB 写操作 warning、读操作 none


[Survived] ConditionalExpression
lib/guard/index.ts:129:7
-     if (DANGEROUS_OPS.has(head)) {
+     if (true) {
Tests ran:
    classifyStatement: MongoDB 危险操作 danger（8 项清单）
    classifyStatement: MongoDB 写操作 warning、读操作 none


[Survived] ConditionalExpression
lib/guard/index.ts:129:7
-     if (DANGEROUS_OPS.has(head)) {
+     if (false) {
Tests ran:
    classifyStatement: MongoDB 危险操作 danger（8 项清单）
    classifyStatement: MongoDB 写操作 warning、读操作 none


[Survived] BlockStatement
lib/guard/index.ts:129:32
-     if (DANGEROUS_OPS.has(head)) {
-       return { level: 'danger', reason: `MongoDB 危险操作 ${head}，可能删库/删集合，需要确认` };
-     }
+     if (DANGEROUS_OPS.has(head)) {}
Tests ran:
    classifyStatement: MongoDB 危险操作 danger（8 项清单）


[Survived] ObjectLiteral
lib/guard/index.ts:130:12
-       return { level: 'danger', reason: `MongoDB 危险操作 ${head}，可能删库/删集合，需要确认` };
+       return {};
Tests ran:
    classifyStatement: MongoDB 危险操作 danger（8 项清单）


[Survived] StringLiteral
lib/guard/index.ts:130:21
-       return { level: 'danger', reason: `MongoDB 危险操作 ${head}，可能删库/删集合，需要确认` };
+       return { level: "", reason: `MongoDB 危险操作 ${head}，可能删库/删集合，需要确认` };
Tests ran:
    classifyStatement: MongoDB 危险操作 danger（8 项清单）


[Survived] StringLiteral
lib/guard/index.ts:130:39
-       return { level: 'danger', reason: `MongoDB 危险操作 ${head}，可能删库/删集合，需要确认` };
+       return { level: 'danger', reason: `` };
Tests ran:
    classifyStatement: MongoDB 危险操作 danger（8 项清单）


[Survived] ConditionalExpression
lib/guard/index.ts:132:7
-     if (MONGO_WRITE_OPS.has(head)) {
+     if (true) {
Tests ran:
    classifyStatement: MongoDB 写操作 warning、读操作 none


[Survived] ConditionalExpression
lib/guard/index.ts:132:7
-     if (MONGO_WRITE_OPS.has(head)) {
+     if (false) {
Tests ran:
    classifyStatement: MongoDB 写操作 warning、读操作 none


[Survived] BlockStatement
lib/guard/index.ts:132:34
-     if (MONGO_WRITE_OPS.has(head)) {
-       return { level: 'warning', reason: `MongoDB 写操作 ${head} 将修改数据` };
-     }
+     if (MONGO_WRITE_OPS.has(head)) {}
Tests ran:
    classifyStatement: MongoDB 写操作 warning、读操作 none


[Survived] ObjectLiteral
lib/guard/index.ts:133:12
-       return { level: 'warning', reason: `MongoDB 写操作 ${head} 将修改数据` };
+       return {};
Tests ran:
    classifyStatement: MongoDB 写操作 warning、读操作 none


[Survived] StringLiteral
lib/guard/index.ts:133:21
-       return { level: 'warning', reason: `MongoDB 写操作 ${head} 将修改数据` };
+       return { level: "", reason: `MongoDB 写操作 ${head} 将修改数据` };
Tests ran:
    classifyStatement: MongoDB 写操作 warning、读操作 none


[Survived] ObjectLiteral
lib/guard/index.ts:135:10
-     return { level: 'none' };
+     return {};
Tests ran:
    classifyStatement: MongoDB 写操作 warning、读操作 none


[Survived] StringLiteral
lib/guard/index.ts:133:40
-       return { level: 'warning', reason: `MongoDB 写操作 ${head} 将修改数据` };
+       return { level: 'warning', reason: `` };
Tests ran:
    classifyStatement: MongoDB 写操作 warning、读操作 none


[Survived] StringLiteral
lib/guard/index.ts:135:19
-     return { level: 'none' };
+     return { level: "" };
Tests ran:
    classifyStatement: MongoDB 写操作 warning、读操作 none


[Survived] LogicalOperator
lib/guard/index.ts:168:16
-       this.ttl = opts?.ttlMs ?? 5 * 60 * 1000;
+       this.ttl = opts?.ttlMs && 5 * 60 * 1000;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 28 more tests!


[Survived] BlockStatement
lib/guard/index.ts:167:45
-     constructor(opts?: ChallengeStoreOptions) {
-       this.ttl = opts?.ttlMs ?? 5 * 60 * 1000;
-       this.nowFn = opts?.now ?? (() => Date.now());
-       const interval = opts?.sweepIntervalMs ?? 60 * 1000;
-       if (interval > 0) {
-         this.timer = setInterval(() => this.sweep(), interval);
-         this.timer.unref?.();
-       }
-     }
+     constructor(opts?: ChallengeStoreOptions) {}
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 28 more tests!


[Survived] BooleanLiteral
lib/guard/index.ts:165:22
-     private disposed = false;
+     private disposed = true;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 28 more tests!


[Survived] OptionalChaining
lib/guard/index.ts:168:16
-       this.ttl = opts?.ttlMs ?? 5 * 60 * 1000;
+       this.ttl = opts.ttlMs ?? 5 * 60 * 1000;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 28 more tests!


[Survived] ArithmeticOperator
lib/guard/index.ts:168:31
-       this.ttl = opts?.ttlMs ?? 5 * 60 * 1000;
+       this.ttl = opts?.ttlMs ?? 5 * 60 / 1000;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 21 more tests!


[Survived] ArithmeticOperator
lib/guard/index.ts:168:31
-       this.ttl = opts?.ttlMs ?? 5 * 60 * 1000;
+       this.ttl = opts?.ttlMs ?? 5 / 60 * 1000;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 21 more tests!


[Survived] LogicalOperator
lib/guard/index.ts:169:18
-       this.nowFn = opts?.now ?? (() => Date.now());
+       this.nowFn = opts?.now && (() => Date.now());
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 28 more tests!


[Survived] OptionalChaining
lib/guard/index.ts:169:18
-       this.nowFn = opts?.now ?? (() => Date.now());
+       this.nowFn = opts.now ?? (() => Date.now());
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 28 more tests!


[Survived] ArrowFunction
lib/guard/index.ts:169:32
-       this.nowFn = opts?.now ?? (() => Date.now());
+       this.nowFn = opts?.now ?? (() => undefined);
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 21 more tests!


[Survived] LogicalOperator
lib/guard/index.ts:170:22
-       const interval = opts?.sweepIntervalMs ?? 60 * 1000;
+       const interval = opts?.sweepIntervalMs && 60 * 1000;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 28 more tests!


[Survived] OptionalChaining
lib/guard/index.ts:170:22
-       const interval = opts?.sweepIntervalMs ?? 60 * 1000;
+       const interval = opts.sweepIntervalMs ?? 60 * 1000;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 28 more tests!


[Survived] ConditionalExpression
lib/guard/index.ts:171:9
-       if (interval > 0) {
+       if (true) {
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 28 more tests!


[Survived] ConditionalExpression
lib/guard/index.ts:171:9
-       if (interval > 0) {
+       if (false) {
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 28 more tests!


[Survived] EqualityOperator
lib/guard/index.ts:171:9
-       if (interval > 0) {
+       if (interval >= 0) {
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 28 more tests!


[Survived] BlockStatement
lib/guard/index.ts:178:40
-     create(statement: string): Challenge {
-       if (this.disposed) throw new Error('ChallengeStore 已关闭');
-       const now = this.nowFn();
-       const ch: Challenge = {
-         id: 'c_' + randomBytes(12).toString('hex'),
-         statementHash: statementHash(statement),
-         createdAt: now,
-         expiresAt: now + this.ttl,
-       };
-       this.map.set(ch.id, ch);
-       return ch;
-     }
+     create(statement: string): Challenge {}
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 15 more tests!


[Survived] EqualityOperator
lib/guard/index.ts:171:9
-       if (interval > 0) {
+       if (interval <= 0) {
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 28 more tests!


[Survived] StringLiteral
lib/guard/index.ts:179:40
-       if (this.disposed) throw new Error('ChallengeStore 已关闭');
+       if (this.disposed) throw new Error("");
Tests ran:
    ChallengeStore dispose 后不可再用


[Survived] ConditionalExpression
lib/guard/index.ts:179:9
-       if (this.disposed) throw new Error('ChallengeStore 已关闭');
+       if (true) throw new Error('ChallengeStore 已关闭');
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 15 more tests!


[Survived] ConditionalExpression
lib/guard/index.ts:179:9
-       if (this.disposed) throw new Error('ChallengeStore 已关闭');
+       if (false) throw new Error('ChallengeStore 已关闭');
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 15 more tests!


[Survived] ObjectLiteral
lib/guard/index.ts:181:27
-       const ch: Challenge = {
-         id: 'c_' + randomBytes(12).toString('hex'),
-         statementHash: statementHash(statement),
-         createdAt: now,
-         expiresAt: now + this.ttl,
-       };
+       const ch: Challenge = {};
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 14 more tests!


[Survived] StringLiteral
lib/guard/index.ts:182:11
-         id: 'c_' + randomBytes(12).toString('hex'),
+         id: "" + randomBytes(12).toString('hex'),
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 14 more tests!


[Survived] StringLiteral
lib/guard/index.ts:182:43
-         id: 'c_' + randomBytes(12).toString('hex'),
+         id: 'c_' + randomBytes(12).toString(""),
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 14 more tests!


[Survived] ArithmeticOperator
lib/guard/index.ts:185:18
-         expiresAt: now + this.ttl,
+         expiresAt: now - this.ttl,
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 14 more tests!


[Survived] CallExpression
lib/guard/index.ts:187:5
-       this.map.set(ch.id, ch);
+       ;
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 14 more tests!


[Survived] BlockStatement
lib/guard/index.ts:192:51
-     consume(id: string, statement: string): boolean {
-       const ch = this.map.get(id);
-       if (!ch) return false;
-       this.map.delete(id); // 无论成败都取走，防重放探测
-       if (this.nowFn() > ch.expiresAt) return false;
-       return ch.statementHash === statementHash(statement);
-     }
+     consume(id: string, statement: string): boolean {}
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 11 more tests!


[Survived] BooleanLiteral
lib/guard/index.ts:194:9
-       if (!ch) return false;
+       if (ch) return false;
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 11 more tests!


[Survived] ConditionalExpression
lib/guard/index.ts:194:9
-       if (!ch) return false;
+       if (true) return false;
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 11 more tests!


[Survived] ConditionalExpression
lib/guard/index.ts:194:9
-       if (!ch) return false;
+       if (false) return false;
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 11 more tests!


[Survived] BooleanLiteral
lib/guard/index.ts:194:21
-       if (!ch) return false;
+       if (!ch) return true;
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    DbToolService: 危险确认与 challenge challenge 一次性：重放同 id → INVALID_CHALLENGE
    ChallengeStore 一次性：consume 成功后立即失效
  and 2 more tests!


[Survived] CallExpression
lib/guard/index.ts:195:5
-       this.map.delete(id); // 无论成败都取走，防重放探测
+       ; // 无论成败都取走，防重放探测
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 9 more tests!


[Survived] ConditionalExpression
lib/guard/index.ts:196:9
-       if (this.nowFn() > ch.expiresAt) return false;
+       if (true) return false;
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 9 more tests!


[Survived] BooleanLiteral
lib/guard/index.ts:196:45
-       if (this.nowFn() > ch.expiresAt) return false;
+       if (this.nowFn() > ch.expiresAt) return true;
Tests ran:
    ChallengeStore TTL 过期后 consume 失败


[Survived] ConditionalExpression
lib/guard/index.ts:196:9
-       if (this.nowFn() > ch.expiresAt) return false;
+       if (false) return false;
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 9 more tests!


[Survived] EqualityOperator
lib/guard/index.ts:196:9
-       if (this.nowFn() > ch.expiresAt) return false;
+       if (this.nowFn() <= ch.expiresAt) return false;
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 9 more tests!


[Survived] EqualityOperator
lib/guard/index.ts:196:9
-       if (this.nowFn() > ch.expiresAt) return false;
+       if (this.nowFn() >= ch.expiresAt) return false;
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 9 more tests!


[Survived] BlockStatement
lib/guard/index.ts:201:17
-     sweep(): void {
-       const now = this.nowFn();
-       for (const [id, ch] of this.map) {
-         if (now > ch.expiresAt) this.map.delete(id);
-       }
-     }
+     sweep(): void {}
Tests ran:
    ChallengeStore sweep 清理过期项


[Survived] BlockStatement
lib/guard/index.ts:203:38
-       for (const [id, ch] of this.map) {
-         if (now > ch.expiresAt) this.map.delete(id);
-       }
+       for (const [id, ch] of this.map) {}
Tests ran:
    ChallengeStore sweep 清理过期项


[Survived] ConditionalExpression
lib/guard/index.ts:197:12
-       return ch.statementHash === statementHash(statement);
+       return true;
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 8 more tests!


[Survived] ConditionalExpression
lib/guard/index.ts:197:12
-       return ch.statementHash === statementHash(statement);
+       return false;
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 8 more tests!


[Survived] ConditionalExpression
lib/guard/index.ts:204:11
-         if (now > ch.expiresAt) this.map.delete(id);
+         if (true) this.map.delete(id);
Tests ran:
    ChallengeStore sweep 清理过期项


[Survived] ConditionalExpression
lib/guard/index.ts:204:11
-         if (now > ch.expiresAt) this.map.delete(id);
+         if (false) this.map.delete(id);
Tests ran:
    ChallengeStore sweep 清理过期项


[Survived] EqualityOperator
lib/guard/index.ts:197:12
-       return ch.statementHash === statementHash(statement);
+       return ch.statementHash !== statementHash(statement);
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 8 more tests!


[Survived] EqualityOperator
lib/guard/index.ts:204:11
-         if (now > ch.expiresAt) this.map.delete(id);
+         if (now >= ch.expiresAt) this.map.delete(id);
Tests ran:
    ChallengeStore sweep 清理过期项


[Survived] EqualityOperator
lib/guard/index.ts:204:11
-         if (now > ch.expiresAt) this.map.delete(id);
+         if (now <= ch.expiresAt) this.map.delete(id);
Tests ran:
    ChallengeStore sweep 清理过期项


[Survived] CallExpression
lib/guard/index.ts:204:31
-         if (now > ch.expiresAt) this.map.delete(id);
+         if (now > ch.expiresAt) ;
Tests ran:
    ChallengeStore sweep 清理过期项


[Survived] StringLiteral
lib/guard/index.ts:23:13
-     'select', 'with', 'show', 'describe', 'desc', 'explain', 'use', 'set', 'help', 'table',
+     'select', "", 'show', 'describe', 'desc', 'explain', 'use', 'set', 'help', 'table',
Ran all tests for this mutant.

[Survived] StringLiteral
lib/guard/index.ts:23:21
-     'select', 'with', 'show', 'describe', 'desc', 'explain', 'use', 'set', 'help', 'table',
+     'select', 'with', "", 'describe', 'desc', 'explain', 'use', 'set', 'help', 'table',
Ran all tests for this mutant.

[Survived] StringLiteral
lib/guard/index.ts:23:29
-     'select', 'with', 'show', 'describe', 'desc', 'explain', 'use', 'set', 'help', 'table',
+     'select', 'with', 'show', "", 'desc', 'explain', 'use', 'set', 'help', 'table',
Ran all tests for this mutant.

[Survived] StringLiteral
lib/guard/index.ts:23:41
-     'select', 'with', 'show', 'describe', 'desc', 'explain', 'use', 'set', 'help', 'table',
+     'select', 'with', 'show', 'describe', "", 'explain', 'use', 'set', 'help', 'table',
Ran all tests for this mutant.

[Survived] StringLiteral
lib/guard/index.ts:23:60
-     'select', 'with', 'show', 'describe', 'desc', 'explain', 'use', 'set', 'help', 'table',
+     'select', 'with', 'show', 'describe', 'desc', 'explain', "", 'set', 'help', 'table',
Ran all tests for this mutant.

[Survived] StringLiteral
lib/guard/index.ts:23:67
-     'select', 'with', 'show', 'describe', 'desc', 'explain', 'use', 'set', 'help', 'table',
+     'select', 'with', 'show', 'describe', 'desc', 'explain', 'use', "", 'help', 'table',
Ran all tests for this mutant.

[Survived] StringLiteral
lib/guard/index.ts:23:74
-     'select', 'with', 'show', 'describe', 'desc', 'explain', 'use', 'set', 'help', 'table',
+     'select', 'with', 'show', 'describe', 'desc', 'explain', 'use', 'set', "", 'table',
Ran all tests for this mutant.

[Survived] StringLiteral
lib/guard/index.ts:23:82
-     'select', 'with', 'show', 'describe', 'desc', 'explain', 'use', 'set', 'help', 'table',
+     'select', 'with', 'show', 'describe', 'desc', 'explain', 'use', 'set', 'help', "",
Ran all tests for this mutant.

[Survived] Regex
lib/guard/index.ts:27:20
-   const SQL_DDL_RE = /^\s*(drop|truncate|alter|create|rename|comment|grant|revoke|flashback|purge)\b/i;
+   const SQL_DDL_RE = /\s*(drop|truncate|alter|create|rename|comment|grant|revoke|flashback|purge)\b/i;
Ran all tests for this mutant.

[Survived] Regex
lib/guard/index.ts:27:20
-   const SQL_DDL_RE = /^\s*(drop|truncate|alter|create|rename|comment|grant|revoke|flashback|purge)\b/i;
+   const SQL_DDL_RE = /^\S*(drop|truncate|alter|create|rename|comment|grant|revoke|flashback|purge)\b/i;
Ran all tests for this mutant.

[Survived] Regex
lib/guard/index.ts:29:20
-   const SQL_DML_RE = /^\s*(insert|update|delete|merge|replace|upsert|call|do)\b/i;
+   const SQL_DML_RE = /\s*(insert|update|delete|merge|replace|upsert|call|do)\b/i;
Ran all tests for this mutant.

[Survived] Regex
lib/guard/index.ts:29:20
-   const SQL_DML_RE = /^\s*(insert|update|delete|merge|replace|upsert|call|do)\b/i;
+   const SQL_DML_RE = /^\s(insert|update|delete|merge|replace|upsert|call|do)\b/i;
Ran all tests for this mutant.

[Survived] Regex
lib/guard/index.ts:29:20
-   const SQL_DML_RE = /^\s*(insert|update|delete|merge|replace|upsert|call|do)\b/i;
+   const SQL_DML_RE = /^\S*(insert|update|delete|merge|replace|upsert|call|do)\b/i;
Ran all tests for this mutant.

[Survived] Regex
lib/guard/index.ts:31:22
-   const SQL_MAINT_RE = /^\s*(shutdown|startup|archive\s+log|recover|analyze|optimize|check\s+table|repair|lock|unlock|kill|flush)\b/i;
+   const SQL_MAINT_RE = /\s*(shutdown|startup|archive\s+log|recover|analyze|optimize|check\s+table|repair|lock|unlock|kill|flush)\b/i;
Ran all tests for this mutant.

[Survived] Regex
lib/guard/index.ts:31:22
-   const SQL_MAINT_RE = /^\s*(shutdown|startup|archive\s+log|recover|analyze|optimize|check\s+table|repair|lock|unlock|kill|flush)\b/i;
+   const SQL_MAINT_RE = /^\S*(shutdown|startup|archive\s+log|recover|analyze|optimize|check\s+table|repair|lock|unlock|kill|flush)\b/i;
Ran all tests for this mutant.

[Survived] Regex
lib/guard/index.ts:31:22
-   const SQL_MAINT_RE = /^\s*(shutdown|startup|archive\s+log|recover|analyze|optimize|check\s+table|repair|lock|unlock|kill|flush)\b/i;
+   const SQL_MAINT_RE = /^\s*(shutdown|startup|archive\slog|recover|analyze|optimize|check\s+table|repair|lock|unlock|kill|flush)\b/i;
Ran all tests for this mutant.

[Survived] Regex
lib/guard/index.ts:31:22
-   const SQL_MAINT_RE = /^\s*(shutdown|startup|archive\s+log|recover|analyze|optimize|check\s+table|repair|lock|unlock|kill|flush)\b/i;
+   const SQL_MAINT_RE = /^\s*(shutdown|startup|archive\S+log|recover|analyze|optimize|check\s+table|repair|lock|unlock|kill|flush)\b/i;
Ran all tests for this mutant.

[Survived] Regex
lib/guard/index.ts:31:22
-   const SQL_MAINT_RE = /^\s*(shutdown|startup|archive\s+log|recover|analyze|optimize|check\s+table|repair|lock|unlock|kill|flush)\b/i;
+   const SQL_MAINT_RE = /^\s*(shutdown|startup|archive\s+log|recover|analyze|optimize|check\stable|repair|lock|unlock|kill|flush)\b/i;
Ran all tests for this mutant.

[Survived] Regex
lib/guard/index.ts:31:22
-   const SQL_MAINT_RE = /^\s*(shutdown|startup|archive\s+log|recover|analyze|optimize|check\s+table|repair|lock|unlock|kill|flush)\b/i;
+   const SQL_MAINT_RE = /^\s*(shutdown|startup|archive\s+log|recover|analyze|optimize|check\S+table|repair|lock|unlock|kill|flush)\b/i;
Ran all tests for this mutant.

[Survived] Regex
lib/guard/index.ts:34:30
-   const SQL_HIDDEN_DANGER_RE = /[;]\s*\S|into\s+(outfile|dumpfile)|load_file\s*\(|set\s+(global|persist)|select\s+.*\bfor\s+update\b/i;
+   const SQL_HIDDEN_DANGER_RE = /[;]\s\S|into\s+(outfile|dumpfile)|load_file\s*\(|set\s+(global|persist)|select\s+.*\bfor\s+update\b/i;
Ran all tests for this mutant.

[Survived] Regex
lib/guard/index.ts:34:30
-   const SQL_HIDDEN_DANGER_RE = /[;]\s*\S|into\s+(outfile|dumpfile)|load_file\s*\(|set\s+(global|persist)|select\s+.*\bfor\s+update\b/i;
+   const SQL_HIDDEN_DANGER_RE = /[;]\s*\s|into\s+(outfile|dumpfile)|load_file\s*\(|set\s+(global|persist)|select\s+.*\bfor\s+update\b/i;
Ran all tests for this mutant.

[Survived] Regex
lib/guard/index.ts:34:30
-   const SQL_HIDDEN_DANGER_RE = /[;]\s*\S|into\s+(outfile|dumpfile)|load_file\s*\(|set\s+(global|persist)|select\s+.*\bfor\s+update\b/i;
+   const SQL_HIDDEN_DANGER_RE = /[;]\s*\S|into\s(outfile|dumpfile)|load_file\s*\(|set\s+(global|persist)|select\s+.*\bfor\s+update\b/i;
Ran all tests for this mutant.

[Survived] Regex
lib/guard/index.ts:34:30
-   const SQL_HIDDEN_DANGER_RE = /[;]\s*\S|into\s+(outfile|dumpfile)|load_file\s*\(|set\s+(global|persist)|select\s+.*\bfor\s+update\b/i;
+   const SQL_HIDDEN_DANGER_RE = /[;]\s*\S|into\s+(outfile|dumpfile)|load_file\s\(|set\s+(global|persist)|select\s+.*\bfor\s+update\b/i;
Ran all tests for this mutant.

[Survived] Regex
lib/guard/index.ts:34:30
-   const SQL_HIDDEN_DANGER_RE = /[;]\s*\S|into\s+(outfile|dumpfile)|load_file\s*\(|set\s+(global|persist)|select\s+.*\bfor\s+update\b/i;
+   const SQL_HIDDEN_DANGER_RE = /[;]\s*\S|into\s+(outfile|dumpfile)|load_file\S*\(|set\s+(global|persist)|select\s+.*\bfor\s+update\b/i;
Ran all tests for this mutant.

[Survived] Regex
lib/guard/index.ts:34:30
-   const SQL_HIDDEN_DANGER_RE = /[;]\s*\S|into\s+(outfile|dumpfile)|load_file\s*\(|set\s+(global|persist)|select\s+.*\bfor\s+update\b/i;
+   const SQL_HIDDEN_DANGER_RE = /[;]\s*\S|into\s+(outfile|dumpfile)|load_file\s*\(|set\s(global|persist)|select\s+.*\bfor\s+update\b/i;
Ran all tests for this mutant.

[Survived] Regex
lib/guard/index.ts:34:30
-   const SQL_HIDDEN_DANGER_RE = /[;]\s*\S|into\s+(outfile|dumpfile)|load_file\s*\(|set\s+(global|persist)|select\s+.*\bfor\s+update\b/i;
+   const SQL_HIDDEN_DANGER_RE = /[;]\s*\S|into\s+(outfile|dumpfile)|load_file\s*\(|set\s+(global|persist)|select\s.*\bfor\s+update\b/i;
Ran all tests for this mutant.

[Survived] Regex
lib/guard/index.ts:34:30
-   const SQL_HIDDEN_DANGER_RE = /[;]\s*\S|into\s+(outfile|dumpfile)|load_file\s*\(|set\s+(global|persist)|select\s+.*\bfor\s+update\b/i;
+   const SQL_HIDDEN_DANGER_RE = /[;]\s*\S|into\s+(outfile|dumpfile)|load_file\s*\(|set\s+(global|persist)|select\S+.*\bfor\s+update\b/i;
Ran all tests for this mutant.

[Survived] Regex
lib/guard/index.ts:34:30
-   const SQL_HIDDEN_DANGER_RE = /[;]\s*\S|into\s+(outfile|dumpfile)|load_file\s*\(|set\s+(global|persist)|select\s+.*\bfor\s+update\b/i;
+   const SQL_HIDDEN_DANGER_RE = /[;]\s*\S|into\s+(outfile|dumpfile)|load_file\s*\(|set\s+(global|persist)|select\s+.\bfor\s+update\b/i;
Ran all tests for this mutant.

[Survived] Regex
lib/guard/index.ts:34:30
-   const SQL_HIDDEN_DANGER_RE = /[;]\s*\S|into\s+(outfile|dumpfile)|load_file\s*\(|set\s+(global|persist)|select\s+.*\bfor\s+update\b/i;
+   const SQL_HIDDEN_DANGER_RE = /[;]\s*\S|into\s+(outfile|dumpfile)|load_file\s*\(|set\s+(global|persist)|select\s+.*\bfor\supdate\b/i;
Ran all tests for this mutant.

[Survived] Regex
lib/guard/index.ts:34:30
-   const SQL_HIDDEN_DANGER_RE = /[;]\s*\S|into\s+(outfile|dumpfile)|load_file\s*\(|set\s+(global|persist)|select\s+.*\bfor\s+update\b/i;
+   const SQL_HIDDEN_DANGER_RE = /[;]\s*\S|into\s+(outfile|dumpfile)|load_file\s*\(|set\s+(global|persist)|select\s+.*\bfor\S+update\b/i;
Ran all tests for this mutant.

[Survived] StringLiteral
lib/guard/index.ts:38:13
-     'insert', 'insertOne', 'insertMany',
+     'insert', "", 'insertMany',
Ran all tests for this mutant.

[Survived] StringLiteral
lib/guard/index.ts:38:26
-     'insert', 'insertOne', 'insertMany',
+     'insert', 'insertOne', "",
Ran all tests for this mutant.

[Survived] StringLiteral
lib/guard/index.ts:39:3
-     'updateOne', 'updateMany', 'replaceOne', 'findOneAndUpdate', 'bulkWrite',
+     "", 'updateMany', 'replaceOne', 'findOneAndUpdate', 'bulkWrite',
Ran all tests for this mutant.

[Survived] StringLiteral
lib/guard/index.ts:39:16
-     'updateOne', 'updateMany', 'replaceOne', 'findOneAndUpdate', 'bulkWrite',
+     'updateOne', "", 'replaceOne', 'findOneAndUpdate', 'bulkWrite',
Ran all tests for this mutant.

[Survived] StringLiteral
lib/guard/index.ts:39:30
-     'updateOne', 'updateMany', 'replaceOne', 'findOneAndUpdate', 'bulkWrite',
+     'updateOne', 'updateMany', "", 'findOneAndUpdate', 'bulkWrite',
Ran all tests for this mutant.

[Survived] StringLiteral
lib/guard/index.ts:39:44
-     'updateOne', 'updateMany', 'replaceOne', 'findOneAndUpdate', 'bulkWrite',
+     'updateOne', 'updateMany', 'replaceOne', "", 'bulkWrite',
Ran all tests for this mutant.

[Survived] StringLiteral
lib/guard/index.ts:39:64
-     'updateOne', 'updateMany', 'replaceOne', 'findOneAndUpdate', 'bulkWrite',
+     'updateOne', 'updateMany', 'replaceOne', 'findOneAndUpdate', "",
Ran all tests for this mutant.

[Survived] StringLiteral
lib/guard/index.ts:40:3
-     'deleteOne',
+     "",
Ran all tests for this mutant.

[Survived] StringLiteral
lib/guard/index.ts:41:3
-     'createCollection', 'renameCollection',
+     "", 'renameCollection',
Ran all tests for this mutant.

[Survived] StringLiteral
lib/guard/index.ts:41:23
-     'createCollection', 'renameCollection',
+     'createCollection', "",
Ran all tests for this mutant.

[Survived] ConditionalExpression
lib/guard/index.ts:210:9
-       if (this.timer) {
+       if (true) {
Ran all tests for this mutant.

[Survived] ConditionalExpression
lib/guard/index.ts:210:9
-       if (this.timer) {
+       if (false) {
Ran all tests for this mutant.

[Survived] CallExpression
lib/guard/index.ts:214:5
-       this.map.clear();
+       ;
Ran all tests for this mutant.

[Survived] BlockStatement
lib/store/audit.ts:35:46
-     append(entry: AuditEntryInput): AuditEntry {
-       const full: AuditEntry = { ...entry, ts: entry.ts ?? new Date().toISOString() };
-       fs.appendFileSync(this.file, JSON.stringify(full) + '\n', 'utf8');
-       return full;
-     }
+     append(entry: AuditEntryInput): AuditEntry {}
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    连接管理与授权路由 grants PUT/GET/DELETE
  and 24 more tests!


[Survived] ObjectLiteral
lib/store/audit.ts:36:30
-       const full: AuditEntry = { ...entry, ts: entry.ts ?? new Date().toISOString() };
+       const full: AuditEntry = {};
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    连接管理与授权路由 grants PUT/GET/DELETE
  and 24 more tests!


[Survived] BlockStatement
lib/store/audit.ts:30:28
-     constructor(dir: string) {
-       this.file = path.join(dir, 'audit.jsonl');
-     }
+     constructor(dir: string) {}
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 44 more tests!


[Survived] StringLiteral
lib/store/audit.ts:31:32
-       this.file = path.join(dir, 'audit.jsonl');
+       this.file = path.join(dir, "");
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 44 more tests!


[Survived] LogicalOperator
lib/store/audit.ts:36:46
-       const full: AuditEntry = { ...entry, ts: entry.ts ?? new Date().toISOString() };
+       const full: AuditEntry = { ...entry, ts: entry.ts && new Date().toISOString() };
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    连接管理与授权路由 grants PUT/GET/DELETE
  and 24 more tests!


[Survived] StringLiteral
lib/store/audit.ts:37:57
-       fs.appendFileSync(this.file, JSON.stringify(full) + '\n', 'utf8');
+       fs.appendFileSync(this.file, JSON.stringify(full) + "", 'utf8');
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    连接管理与授权路由 grants PUT/GET/DELETE
  and 24 more tests!


[Survived] BlockStatement
lib/store/audit.ts:42:33
-     tail(n: number): AuditEntry[] {
-       if (!fs.existsSync(this.file)) return [];
-       const lines = fs.readFileSync(this.file, 'utf8').split('\n');
-       const out: AuditEntry[] = [];
-       for (let i = lines.length - 1; i >= 0 && out.length < n; i--) {
-         const line = lines[i]?.trim();
-         if (!line) continue;
-         try {
-           out.push(JSON.parse(line) as AuditEntry);
-         } catch {
-           // 跳过损坏行
-         }
-       }
-       return out.reverse();
-     }
+     tail(n: number): AuditEntry[] {}
Tests ran:
    fenced 校验 无 Origin/Referer（本地脚本）放行
    handleDbToolRequest 直调（prefix 挂载形态 + trust） loopback 同端口 + prefix 路径 /dsh-db-tool/api/state → 200
    handleDbToolRequest 直调（prefix 挂载形态 + trust） trustedHosts 条目放行（非 loopback Host + 受信 Origin）
  and 6 more tests!


[Survived] StringLiteral
lib/store/audit.ts:37:63
-       fs.appendFileSync(this.file, JSON.stringify(full) + '\n', 'utf8');
+       fs.appendFileSync(this.file, JSON.stringify(full) + '\n', "");
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    连接管理与授权路由 grants PUT/GET/DELETE
  and 24 more tests!


[Survived] ArrayDeclaration
lib/store/audit.ts:43:43
-       if (!fs.existsSync(this.file)) return [];
+       if (!fs.existsSync(this.file)) return ["Stryker was here"];
Tests ran:
    fenced 校验 无 Origin/Referer（本地脚本）放行
    handleDbToolRequest 直调（prefix 挂载形态 + trust） loopback 同端口 + prefix 路径 /dsh-db-tool/api/state → 200
    handleDbToolRequest 直调（prefix 挂载形态 + trust） trustedHosts 条目放行（非 loopback Host + 受信 Origin）
  and 1 more test!


[Survived] ConditionalExpression
lib/store/audit.ts:43:9
-       if (!fs.existsSync(this.file)) return [];
+       if (true) return [];
Tests ran:
    fenced 校验 无 Origin/Referer（本地脚本）放行
    handleDbToolRequest 直调（prefix 挂载形态 + trust） loopback 同端口 + prefix 路径 /dsh-db-tool/api/state → 200
    handleDbToolRequest 直调（prefix 挂载形态 + trust） trustedHosts 条目放行（非 loopback Host + 受信 Origin）
  and 6 more tests!


[Survived] BooleanLiteral
lib/store/audit.ts:43:9
-       if (!fs.existsSync(this.file)) return [];
+       if (fs.existsSync(this.file)) return [];
Tests ran:
    fenced 校验 无 Origin/Referer（本地脚本）放行
    handleDbToolRequest 直调（prefix 挂载形态 + trust） loopback 同端口 + prefix 路径 /dsh-db-tool/api/state → 200
    handleDbToolRequest 直调（prefix 挂载形态 + trust） trustedHosts 条目放行（非 loopback Host + 受信 Origin）
  and 6 more tests!


[Survived] ConditionalExpression
lib/store/audit.ts:43:9
-       if (!fs.existsSync(this.file)) return [];
+       if (false) return [];
Tests ran:
    fenced 校验 无 Origin/Referer（本地脚本）放行
    handleDbToolRequest 直调（prefix 挂载形态 + trust） loopback 同端口 + prefix 路径 /dsh-db-tool/api/state → 200
    handleDbToolRequest 直调（prefix 挂载形态 + trust） trustedHosts 条目放行（非 loopback Host + 受信 Origin）
  and 6 more tests!


[Survived] StringLiteral
lib/store/audit.ts:44:46
-       const lines = fs.readFileSync(this.file, 'utf8').split('\n');
+       const lines = fs.readFileSync(this.file, "").split('\n');
Tests ran:
    业务路由与确认流程 GET /api/audit 与 /api/state
    DbToolService: 浏览与审计 危险确认与执行都留审计
    DbToolService: 浏览与审计 INVALID_CHALLENGE 也留审计
  and 2 more tests!


[Survived] StringLiteral
lib/store/audit.ts:44:60
-       const lines = fs.readFileSync(this.file, 'utf8').split('\n');
+       const lines = fs.readFileSync(this.file, 'utf8').split("");
Tests ran:
    业务路由与确认流程 GET /api/audit 与 /api/state
    DbToolService: 浏览与审计 危险确认与执行都留审计
    DbToolService: 浏览与审计 INVALID_CHALLENGE 也留审计
  and 2 more tests!


[Survived] ArrayDeclaration
lib/store/audit.ts:45:31
-       const out: AuditEntry[] = [];
+       const out: AuditEntry[] = ["Stryker was here"];
Tests ran:
    业务路由与确认流程 GET /api/audit 与 /api/state
    DbToolService: 浏览与审计 危险确认与执行都留审计
    DbToolService: 浏览与审计 INVALID_CHALLENGE 也留审计
  and 2 more tests!


[Survived] ArithmeticOperator
lib/store/audit.ts:46:18
-       for (let i = lines.length - 1; i >= 0 && out.length < n; i--) {
+       for (let i = lines.length + 1; i >= 0 && out.length < n; i--) {
Tests ran:
    业务路由与确认流程 GET /api/audit 与 /api/state
    DbToolService: 浏览与审计 危险确认与执行都留审计
    DbToolService: 浏览与审计 INVALID_CHALLENGE 也留审计
  and 2 more tests!


[Survived] ConditionalExpression
lib/store/audit.ts:46:36
-       for (let i = lines.length - 1; i >= 0 && out.length < n; i--) {
+       for (let i = lines.length - 1; false; i--) {
Tests ran:
    业务路由与确认流程 GET /api/audit 与 /api/state
    DbToolService: 浏览与审计 危险确认与执行都留审计
    DbToolService: 浏览与审计 INVALID_CHALLENGE 也留审计
  and 2 more tests!


[Survived] LogicalOperator
lib/store/audit.ts:46:36
-       for (let i = lines.length - 1; i >= 0 && out.length < n; i--) {
+       for (let i = lines.length - 1; i >= 0 || out.length < n; i--) {
Tests ran:
    业务路由与确认流程 GET /api/audit 与 /api/state
    DbToolService: 浏览与审计 危险确认与执行都留审计
    DbToolService: 浏览与审计 INVALID_CHALLENGE 也留审计
  and 2 more tests!


[Survived] ConditionalExpression
lib/store/audit.ts:46:36
-       for (let i = lines.length - 1; i >= 0 && out.length < n; i--) {
+       for (let i = lines.length - 1; true && out.length < n; i--) {
Tests ran:
    业务路由与确认流程 GET /api/audit 与 /api/state
    DbToolService: 浏览与审计 危险确认与执行都留审计
    DbToolService: 浏览与审计 INVALID_CHALLENGE 也留审计
  and 2 more tests!


[Survived] EqualityOperator
lib/store/audit.ts:46:36
-       for (let i = lines.length - 1; i >= 0 && out.length < n; i--) {
+       for (let i = lines.length - 1; i > 0 && out.length < n; i--) {
Tests ran:
    业务路由与确认流程 GET /api/audit 与 /api/state
    DbToolService: 浏览与审计 危险确认与执行都留审计
    DbToolService: 浏览与审计 INVALID_CHALLENGE 也留审计
  and 2 more tests!


[Survived] EqualityOperator
lib/store/audit.ts:46:36
-       for (let i = lines.length - 1; i >= 0 && out.length < n; i--) {
+       for (let i = lines.length - 1; i < 0 && out.length < n; i--) {
Tests ran:
    业务路由与确认流程 GET /api/audit 与 /api/state
    DbToolService: 浏览与审计 危险确认与执行都留审计
    DbToolService: 浏览与审计 INVALID_CHALLENGE 也留审计
  and 2 more tests!


[Survived] ConditionalExpression
lib/store/audit.ts:46:46
-       for (let i = lines.length - 1; i >= 0 && out.length < n; i--) {
+       for (let i = lines.length - 1; i >= 0 && true; i--) {
Tests ran:
    业务路由与确认流程 GET /api/audit 与 /api/state
    DbToolService: 浏览与审计 危险确认与执行都留审计
    DbToolService: 浏览与审计 INVALID_CHALLENGE 也留审计
  and 2 more tests!


[Survived] EqualityOperator
lib/store/audit.ts:46:46
-       for (let i = lines.length - 1; i >= 0 && out.length < n; i--) {
+       for (let i = lines.length - 1; i >= 0 && out.length >= n; i--) {
Tests ran:
    业务路由与确认流程 GET /api/audit 与 /api/state
    DbToolService: 浏览与审计 危险确认与执行都留审计
    DbToolService: 浏览与审计 INVALID_CHALLENGE 也留审计
  and 2 more tests!


[Survived] EqualityOperator
lib/store/audit.ts:46:46
-       for (let i = lines.length - 1; i >= 0 && out.length < n; i--) {
+       for (let i = lines.length - 1; i >= 0 && out.length <= n; i--) {
Tests ran:
    业务路由与确认流程 GET /api/audit 与 /api/state
    DbToolService: 浏览与审计 危险确认与执行都留审计
    DbToolService: 浏览与审计 INVALID_CHALLENGE 也留审计
  and 2 more tests!


[Survived] UpdateOperator
lib/store/audit.ts:46:62
-       for (let i = lines.length - 1; i >= 0 && out.length < n; i--) {
+       for (let i = lines.length - 1; i >= 0 && out.length < n; i++) {
Tests ran:
    业务路由与确认流程 GET /api/audit 与 /api/state
    DbToolService: 浏览与审计 危险确认与执行都留审计
    DbToolService: 浏览与审计 INVALID_CHALLENGE 也留审计
  and 2 more tests!


[Survived] BlockStatement
lib/store/audit.ts:46:67
-       for (let i = lines.length - 1; i >= 0 && out.length < n; i--) {
-         const line = lines[i]?.trim();
-         if (!line) continue;
-         try {
-           out.push(JSON.parse(line) as AuditEntry);
-         } catch {
-           // 跳过损坏行
-         }
-       }
+       for (let i = lines.length - 1; i >= 0 && out.length < n; i--) {}
Tests ran:
    业务路由与确认流程 GET /api/audit 与 /api/state
    DbToolService: 浏览与审计 危险确认与执行都留审计
    DbToolService: 浏览与审计 INVALID_CHALLENGE 也留审计
  and 2 more tests!


[Survived] MethodExpression
lib/store/audit.ts:47:20
-         const line = lines[i]?.trim();
+         const line = lines[i];
Tests ran:
    业务路由与确认流程 GET /api/audit 与 /api/state
    DbToolService: 浏览与审计 危险确认与执行都留审计
    DbToolService: 浏览与审计 INVALID_CHALLENGE 也留审计
  and 2 more tests!


[Survived] OptionalChaining
lib/store/audit.ts:47:20
-         const line = lines[i]?.trim();
+         const line = lines[i].trim();
Tests ran:
    业务路由与确认流程 GET /api/audit 与 /api/state
    DbToolService: 浏览与审计 危险确认与执行都留审计
    DbToolService: 浏览与审计 INVALID_CHALLENGE 也留审计
  and 2 more tests!


[Survived] BooleanLiteral
lib/store/audit.ts:48:11
-         if (!line) continue;
+         if (line) continue;
Tests ran:
    业务路由与确认流程 GET /api/audit 与 /api/state
    DbToolService: 浏览与审计 危险确认与执行都留审计
    DbToolService: 浏览与审计 INVALID_CHALLENGE 也留审计
  and 2 more tests!


[Survived] ConditionalExpression
lib/store/audit.ts:48:11
-         if (!line) continue;
+         if (true) continue;
Tests ran:
    业务路由与确认流程 GET /api/audit 与 /api/state
    DbToolService: 浏览与审计 危险确认与执行都留审计
    DbToolService: 浏览与审计 INVALID_CHALLENGE 也留审计
  and 2 more tests!


[Survived] ConditionalExpression
lib/store/audit.ts:48:11
-         if (!line) continue;
+         if (false) continue;
Tests ran:
    业务路由与确认流程 GET /api/audit 与 /api/state
    DbToolService: 浏览与审计 危险确认与执行都留审计
    DbToolService: 浏览与审计 INVALID_CHALLENGE 也留审计
  and 2 more tests!


[Survived] BlockStatement
lib/store/audit.ts:49:11
-         try {
-           out.push(JSON.parse(line) as AuditEntry);
-         } catch {
+         try {} catch {
Tests ran:
    业务路由与确认流程 GET /api/audit 与 /api/state
    DbToolService: 浏览与审计 危险确认与执行都留审计
    DbToolService: 浏览与审计 INVALID_CHALLENGE 也留审计
  and 2 more tests!


[Survived] CallExpression
lib/store/audit.ts:50:9
-           out.push(JSON.parse(line) as AuditEntry);
+           ;
Tests ran:
    业务路由与确认流程 GET /api/audit 与 /api/state
    DbToolService: 浏览与审计 危险确认与执行都留审计
    DbToolService: 浏览与审计 INVALID_CHALLENGE 也留审计
  and 2 more tests!


[Survived] MethodExpression
lib/store/audit.ts:55:12
-       return out.reverse();
+       return out;
Tests ran:
    业务路由与确认流程 GET /api/audit 与 /api/state
    DbToolService: 浏览与审计 危险确认与执行都留审计
    DbToolService: 浏览与审计 INVALID_CHALLENGE 也留审计
  and 2 more tests!


[NoCoverage] StringLiteral
lib/store/connections.ts:56:25
-     const creds = m[2] ?? '';
+     const creds = m[2] ?? "Stryker was here!";

[NoCoverage] ObjectLiteral
lib/store/connections.ts:156:56
-         if (password !== undefined) this.secrets.set(id, { password });
+         if (password !== undefined) this.secrets.set(id, {});

[Survived] BlockStatement
lib/store/connections.ts:53:48
-   export function redactUrl(url: string): string {
-     const m = url.match(/^([a-z][a-z0-9+.-]*:\/\/)([^/]*)@/i);
-     if (!m) return url;
-     const creds = m[2] ?? '';
-     const idx = creds.lastIndexOf(':');
-     if (idx < 0) return url; // 无密码段
-     return `${m[1]}${creds.slice(0, idx + 1)}***@${url.slice(m[0].length)}`;
-   }
+   export function redactUrl(url: string): string {}
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 29 more tests!


[Survived] Regex
lib/store/connections.ts:54:23
-     const m = url.match(/^([a-z][a-z0-9+.-]*:\/\/)([^/]*)@/i);
+     const m = url.match(/([a-z][a-z0-9+.-]*:\/\/)([^/]*)@/i);
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 29 more tests!


[Survived] Regex
lib/store/connections.ts:54:23
-     const m = url.match(/^([a-z][a-z0-9+.-]*:\/\/)([^/]*)@/i);
+     const m = url.match(/^([^a-z][a-z0-9+.-]*:\/\/)([^/]*)@/i);
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 29 more tests!


[Survived] Regex
lib/store/connections.ts:54:23
-     const m = url.match(/^([a-z][a-z0-9+.-]*:\/\/)([^/]*)@/i);
+     const m = url.match(/^([a-z][a-z0-9+.-]:\/\/)([^/]*)@/i);
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 29 more tests!


[Survived] Regex
lib/store/connections.ts:54:23
-     const m = url.match(/^([a-z][a-z0-9+.-]*:\/\/)([^/]*)@/i);
+     const m = url.match(/^([a-z][^a-z0-9+.-]*:\/\/)([^/]*)@/i);
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 29 more tests!


[Survived] Regex
lib/store/connections.ts:54:23
-     const m = url.match(/^([a-z][a-z0-9+.-]*:\/\/)([^/]*)@/i);
+     const m = url.match(/^([a-z][a-z0-9+.-]*:\/\/)([^/])@/i);
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 29 more tests!


[Survived] Regex
lib/store/connections.ts:54:23
-     const m = url.match(/^([a-z][a-z0-9+.-]*:\/\/)([^/]*)@/i);
+     const m = url.match(/^([a-z][a-z0-9+.-]*:\/\/)([/]*)@/i);
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 29 more tests!


[Survived] BooleanLiteral
lib/store/connections.ts:55:7
-     if (!m) return url;
+     if (m) return url;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 29 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:55:7
-     if (!m) return url;
+     if (true) return url;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 29 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:55:7
-     if (!m) return url;
+     if (false) return url;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 29 more tests!


[Survived] LogicalOperator
lib/store/connections.ts:56:17
-     const creds = m[2] ?? '';
+     const creds = m[2] && '';
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 28 more tests!


[Survived] StringLiteral
lib/store/connections.ts:57:33
-     const idx = creds.lastIndexOf(':');
+     const idx = creds.lastIndexOf("");
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 28 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:58:7
-     if (idx < 0) return url; // 无密码段
+     if (true) return url; // 无密码段
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 28 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:58:7
-     if (idx < 0) return url; // 无密码段
+     if (false) return url; // 无密码段
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 28 more tests!


[Survived] EqualityOperator
lib/store/connections.ts:58:7
-     if (idx < 0) return url; // 无密码段
+     if (idx <= 0) return url; // 无密码段
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 28 more tests!


[Survived] EqualityOperator
lib/store/connections.ts:58:7
-     if (idx < 0) return url; // 无密码段
+     if (idx >= 0) return url; // 无密码段
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 28 more tests!


[Survived] StringLiteral
lib/store/connections.ts:59:10
-     return `${m[1]}${creds.slice(0, idx + 1)}***@${url.slice(m[0].length)}`;
+     return ``;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 28 more tests!


[Survived] MethodExpression
lib/store/connections.ts:59:20
-     return `${m[1]}${creds.slice(0, idx + 1)}***@${url.slice(m[0].length)}`;
+     return `${m[1]}${creds}***@${url.slice(m[0].length)}`;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 28 more tests!


[Survived] ArithmeticOperator
lib/store/connections.ts:59:35
-     return `${m[1]}${creds.slice(0, idx + 1)}***@${url.slice(m[0].length)}`;
+     return `${m[1]}${creds.slice(0, idx - 1)}***@${url.slice(m[0].length)}`;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 28 more tests!


[Survived] MethodExpression
lib/store/connections.ts:59:50
-     return `${m[1]}${creds.slice(0, idx + 1)}***@${url.slice(m[0].length)}`;
+     return `${m[1]}${creds.slice(0, idx + 1)}***@${url}`;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 28 more tests!


[Survived] BlockStatement
lib/store/connections.ts:65:3
-   } {
-     if (!fields || typeof fields[PASSWORD_KEY] !== 'string') {
-       return { clean: fields, password: undefined };
-     }
-     const { [PASSWORD_KEY]: password, ...clean } = fields;
-     return { clean, password: password as string };
-   }
+   } {}
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 30 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:66:7
-     if (!fields || typeof fields[PASSWORD_KEY] !== 'string') {
+     if (true) {
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 30 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:66:18
-     if (!fields || typeof fields[PASSWORD_KEY] !== 'string') {
+     if (!fields || false) {
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    ConnectionStore create 返回脱敏 meta，密码不出现在 connections.json
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets
  and 2 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:66:7
-     if (!fields || typeof fields[PASSWORD_KEY] !== 'string') {
+     if (false) {
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 30 more tests!


[Survived] LogicalOperator
lib/store/connections.ts:66:7
-     if (!fields || typeof fields[PASSWORD_KEY] !== 'string') {
+     if (!fields && typeof fields[PASSWORD_KEY] !== 'string') {
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 30 more tests!


[Survived] BooleanLiteral
lib/store/connections.ts:66:7
-     if (!fields || typeof fields[PASSWORD_KEY] !== 'string') {
+     if (fields || typeof fields[PASSWORD_KEY] !== 'string') {
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 30 more tests!


[Survived] EqualityOperator
lib/store/connections.ts:66:18
-     if (!fields || typeof fields[PASSWORD_KEY] !== 'string') {
+     if (!fields || typeof fields[PASSWORD_KEY] === 'string') {
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    ConnectionStore create 返回脱敏 meta，密码不出现在 connections.json
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets
  and 2 more tests!


[Survived] StringLiteral
lib/store/connections.ts:66:50
-     if (!fields || typeof fields[PASSWORD_KEY] !== 'string') {
+     if (!fields || typeof fields[PASSWORD_KEY] !== "") {
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    ConnectionStore create 返回脱敏 meta，密码不出现在 connections.json
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets
  and 2 more tests!


[Survived] ObjectLiteral
lib/store/connections.ts:70:10
-     return { clean, password: password as string };
+     return {};
Tests ran:
    ConnectionStore create 返回脱敏 meta，密码不出现在 connections.json
    ConnectionStore testTarget 返回含机密的 ResolvedConnection；不存在抛错
    SecretsBox 任何公开 API 返回值与元数据文件的序列化结果都不含密码


[Survived] BlockStatement
lib/store/connections.ts:66:60
-     if (!fields || typeof fields[PASSWORD_KEY] !== 'string') {
-       return { clean: fields, password: undefined };
-     }
+     if (!fields || typeof fields[PASSWORD_KEY] !== 'string') {}
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 27 more tests!


[Survived] ObjectLiteral
lib/store/connections.ts:67:12
-       return { clean: fields, password: undefined };
+       return {};
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 27 more tests!


[Survived] BlockStatement
lib/store/connections.ts:74:50
-   function toMeta(rec: ConnRecord): ConnectionMeta {
-     const meta: ConnectionMeta = { id: rec.id, kind: rec.kind };
-     if (rec.name !== undefined) meta.name = rec.name;
-     if (rec.urlSafe !== undefined) meta.safeUrl = rec.urlSafe;
-     if (rec.fields) {
-       const { host, port, database } = rec.fields;
-       if (typeof host === 'string') meta.host = host;
-       if (typeof port === 'number') meta.port = port;
-       if (typeof database === 'string') meta.database = database;
-     }
-     return meta;
-   }
+   function toMeta(rec: ConnRecord): ConnectionMeta {}
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    fenced 校验 loopback Origin 同端口放行
    fenced 校验 无 Origin/Referer（本地脚本）放行
  and 37 more tests!


[Survived] ObjectLiteral
lib/store/connections.ts:75:32
-     const meta: ConnectionMeta = { id: rec.id, kind: rec.kind };
+     const meta: ConnectionMeta = {};
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    fenced 校验 loopback Origin 同端口放行
    fenced 校验 无 Origin/Referer（本地脚本）放行
  and 37 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:76:7
-     if (rec.name !== undefined) meta.name = rec.name;
+     if (true) meta.name = rec.name;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    fenced 校验 loopback Origin 同端口放行
    fenced 校验 无 Origin/Referer（本地脚本）放行
  and 37 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:76:7
-     if (rec.name !== undefined) meta.name = rec.name;
+     if (false) meta.name = rec.name;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    fenced 校验 loopback Origin 同端口放行
    fenced 校验 无 Origin/Referer（本地脚本）放行
  and 37 more tests!


[Survived] EqualityOperator
lib/store/connections.ts:76:7
-     if (rec.name !== undefined) meta.name = rec.name;
+     if (rec.name === undefined) meta.name = rec.name;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    fenced 校验 loopback Origin 同端口放行
    fenced 校验 无 Origin/Referer（本地脚本）放行
  and 37 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:77:7
-     if (rec.urlSafe !== undefined) meta.safeUrl = rec.urlSafe;
+     if (true) meta.safeUrl = rec.urlSafe;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    fenced 校验 loopback Origin 同端口放行
    fenced 校验 无 Origin/Referer（本地脚本）放行
  and 37 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:77:7
-     if (rec.urlSafe !== undefined) meta.safeUrl = rec.urlSafe;
+     if (false) meta.safeUrl = rec.urlSafe;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    fenced 校验 loopback Origin 同端口放行
    fenced 校验 无 Origin/Referer（本地脚本）放行
  and 37 more tests!


[Survived] EqualityOperator
lib/store/connections.ts:77:7
-     if (rec.urlSafe !== undefined) meta.safeUrl = rec.urlSafe;
+     if (rec.urlSafe === undefined) meta.safeUrl = rec.urlSafe;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    fenced 校验 loopback Origin 同端口放行
    fenced 校验 无 Origin/Referer（本地脚本）放行
  and 37 more tests!


[Survived] BlockStatement
lib/store/connections.ts:78:19
-     if (rec.fields) {
-       const { host, port, database } = rec.fields;
-       if (typeof host === 'string') meta.host = host;
-       if (typeof port === 'number') meta.port = port;
-       if (typeof database === 'string') meta.database = database;
-     }
+     if (rec.fields) {}
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    ConnectionStore create 返回脱敏 meta，密码不出现在 connections.json
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets
  and 2 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:78:7
-     if (rec.fields) {
+     if (false) {
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    fenced 校验 loopback Origin 同端口放行
    fenced 校验 无 Origin/Referer（本地脚本）放行
  and 37 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:78:7
-     if (rec.fields) {
+     if (true) {
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    fenced 校验 loopback Origin 同端口放行
    fenced 校验 无 Origin/Referer（本地脚本）放行
  and 37 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:80:9
-       if (typeof host === 'string') meta.host = host;
+       if (true) meta.host = host;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    ConnectionStore create 返回脱敏 meta，密码不出现在 connections.json
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets
  and 2 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:80:9
-       if (typeof host === 'string') meta.host = host;
+       if (false) meta.host = host;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    ConnectionStore create 返回脱敏 meta，密码不出现在 connections.json
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets
  and 2 more tests!


[Survived] EqualityOperator
lib/store/connections.ts:80:9
-       if (typeof host === 'string') meta.host = host;
+       if (typeof host !== 'string') meta.host = host;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    ConnectionStore create 返回脱敏 meta，密码不出现在 connections.json
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets
  and 2 more tests!


[Survived] StringLiteral
lib/store/connections.ts:80:25
-       if (typeof host === 'string') meta.host = host;
+       if (typeof host === "") meta.host = host;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    ConnectionStore create 返回脱敏 meta，密码不出现在 connections.json
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets
  and 2 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:81:9
-       if (typeof port === 'number') meta.port = port;
+       if (true) meta.port = port;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    ConnectionStore create 返回脱敏 meta，密码不出现在 connections.json
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets
  and 2 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:81:9
-       if (typeof port === 'number') meta.port = port;
+       if (false) meta.port = port;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    ConnectionStore create 返回脱敏 meta，密码不出现在 connections.json
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets
  and 2 more tests!


[Survived] EqualityOperator
lib/store/connections.ts:81:9
-       if (typeof port === 'number') meta.port = port;
+       if (typeof port !== 'number') meta.port = port;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    ConnectionStore create 返回脱敏 meta，密码不出现在 connections.json
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets
  and 2 more tests!


[Survived] StringLiteral
lib/store/connections.ts:81:25
-       if (typeof port === 'number') meta.port = port;
+       if (typeof port === "") meta.port = port;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    ConnectionStore create 返回脱敏 meta，密码不出现在 connections.json
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets
  and 2 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:82:9
-       if (typeof database === 'string') meta.database = database;
+       if (true) meta.database = database;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    ConnectionStore create 返回脱敏 meta，密码不出现在 connections.json
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets
  and 2 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:82:9
-       if (typeof database === 'string') meta.database = database;
+       if (false) meta.database = database;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    ConnectionStore create 返回脱敏 meta，密码不出现在 connections.json
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets
  and 2 more tests!


[Survived] EqualityOperator
lib/store/connections.ts:82:9
-       if (typeof database === 'string') meta.database = database;
+       if (typeof database !== 'string') meta.database = database;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    ConnectionStore create 返回脱敏 meta，密码不出现在 connections.json
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets
  and 2 more tests!


[Survived] StringLiteral
lib/store/connections.ts:82:29
-       if (typeof database === 'string') meta.database = database;
+       if (typeof database === "") meta.database = database;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    ConnectionStore create 返回脱敏 meta，密码不出现在 connections.json
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets
  and 2 more tests!


[Survived] BlockStatement
lib/store/connections.ts:94:5
-     ) {
-       this.file = path.join(dir, 'connections.json');
-     }
+     ) {}
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 44 more tests!


[Survived] ObjectLiteral
lib/store/connections.ts:99:49
-       return readJson<ConnectionsFile>(this.file, { connections: [] });
+       return readJson<ConnectionsFile>(this.file, {});
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    fenced 校验 loopback Origin 同端口放行
    fenced 校验 无 Origin/Referer（本地脚本）放行
  and 37 more tests!


[Survived] BlockStatement
lib/store/connections.ts:98:35
-     private load(): ConnectionsFile {
-       return readJson<ConnectionsFile>(this.file, { connections: [] });
-     }
+     private load(): ConnectionsFile {}
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    fenced 校验 loopback Origin 同端口放行
    fenced 校验 无 Origin/Referer（本地脚本）放行
  and 37 more tests!


[Survived] StringLiteral
lib/store/connections.ts:95:32
-       this.file = path.join(dir, 'connections.json');
+       this.file = path.join(dir, "");
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 44 more tests!


[Survived] ArrayDeclaration
lib/store/connections.ts:99:64
-       return readJson<ConnectionsFile>(this.file, { connections: [] });
+       return readJson<ConnectionsFile>(this.file, { connections: ["Stryker was here"] });
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    fenced 校验 loopback Origin 同端口放行
    fenced 校验 无 Origin/Referer（本地脚本）放行
  and 37 more tests!


[Survived] BlockStatement
lib/store/connections.ts:102:45
-     private save(data: ConnectionsFile): void {
-       writeJsonAtomic(this.file, data);
-     }
+     private save(data: ConnectionsFile): void {}
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 30 more tests!


[Survived] CallExpression
lib/store/connections.ts:103:5
-       writeJsonAtomic(this.file, data);
+       ;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 30 more tests!


[Survived] BlockStatement
lib/store/connections.ts:106:78
-     private findRec(data: ConnectionsFile, id: string): ConnRecord | undefined {
-       return data.connections.find((c) => c.id === id);
-     }
+     private findRec(data: ConnectionsFile, id: string): ConnRecord | undefined {}
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
  and 31 more tests!


[Survived] ArrowFunction
lib/store/connections.ts:107:34
-       return data.connections.find((c) => c.id === id);
+       return data.connections.find(() => undefined);
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
  and 31 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:107:41
-       return data.connections.find((c) => c.id === id);
+       return data.connections.find((c) => false);
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
  and 19 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:107:41
-       return data.connections.find((c) => c.id === id);
+       return data.connections.find((c) => true);
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
  and 19 more tests!


[Survived] EqualityOperator
lib/store/connections.ts:107:41
-       return data.connections.find((c) => c.id === id);
+       return data.connections.find((c) => c.id !== id);
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
  and 19 more tests!


[Survived] BlockStatement
lib/store/connections.ts:114:47
-     get(id: string): ConnectionMeta | undefined {
-       const rec = this.findRec(this.load(), id);
-       return rec ? toMeta(rec) : undefined;
-     }
+     get(id: string): ConnectionMeta | undefined {}
Tests ran:
    ConnectionStore list / get 返回脱敏数据，get 不存在返回 undefined
    ConnectionStore remove 级联删除 secrets 与全部项目的 grants
    SecretsBox 任何公开 API 返回值与元数据文件的序列化结果都不含密码


[Survived] BlockStatement
lib/store/connections.ts:110:28
-     list(): ConnectionMeta[] {
-       return this.load().connections.map(toMeta);
-     }
+     list(): ConnectionMeta[] {}
Tests ran:
    fenced 校验 loopback Origin 同端口放行
    fenced 校验 无 Origin/Referer（本地脚本）放行
    handleDbToolRequest 直调（prefix 挂载形态 + trust） loopback 同端口 + prefix 路径 /dsh-db-tool/api/state → 200
  and 8 more tests!


[Survived] BlockStatement
lib/store/connections.ts:122:39
-       if (this.findRec(data, input.id)) {
-         throw new Error(`连接已存在: ${input.id}`);
-       }
+       if (this.findRec(data, input.id)) {}
Tests ran:
    ConnectionStore create 重复 id 抛错


[Survived] BlockStatement
lib/store/connections.ts:120:56
-     create(input: ConnectionCreateInput): ConnectionMeta {
-       const data = this.load();
-       if (this.findRec(data, input.id)) {
-         throw new Error(`连接已存在: ${input.id}`);
-       }
-       const rec: ConnRecord = { id: input.id, kind: input.kind };
-       if (input.name !== undefined) rec.name = input.name;
-       if (input.ssl !== undefined) rec.ssl = input.ssl;
-       const { clean, password } = splitPassword(input.fields);
-       if (clean !== undefined) rec.fields = clean;
-       const secretPatch: Record<string, string> = {};
-       if (input.url !== undefined) {
-         rec.urlSafe = redactUrl(input.url);
-         secretPatch.url = input.url;
-       }
-       if (password !== undefined) secretPatch.password = password;
-       if (Object.keys(secretPatch).length > 0) this.secrets.set(input.id, secretPatch);
-       data.connections.push(rec);
-       this.save(data);
-       return toMeta(rec);
-     }
+     create(input: ConnectionCreateInput): ConnectionMeta {}
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 30 more tests!


[Survived] StringLiteral
lib/store/connections.ts:123:23
-         throw new Error(`连接已存在: ${input.id}`);
+         throw new Error(``);
Tests ran:
    ConnectionStore create 重复 id 抛错


[Survived] ConditionalExpression
lib/store/connections.ts:122:9
-       if (this.findRec(data, input.id)) {
+       if (true) {
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 30 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:122:9
-       if (this.findRec(data, input.id)) {
+       if (false) {
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 30 more tests!


[Survived] ObjectLiteral
lib/store/connections.ts:125:29
-       const rec: ConnRecord = { id: input.id, kind: input.kind };
+       const rec: ConnRecord = {};
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 30 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:126:9
-       if (input.name !== undefined) rec.name = input.name;
+       if (true) rec.name = input.name;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 30 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:126:9
-       if (input.name !== undefined) rec.name = input.name;
+       if (false) rec.name = input.name;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 30 more tests!


[Survived] EqualityOperator
lib/store/connections.ts:126:9
-       if (input.name !== undefined) rec.name = input.name;
+       if (input.name === undefined) rec.name = input.name;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 30 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:127:9
-       if (input.ssl !== undefined) rec.ssl = input.ssl;
+       if (true) rec.ssl = input.ssl;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 30 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:127:9
-       if (input.ssl !== undefined) rec.ssl = input.ssl;
+       if (false) rec.ssl = input.ssl;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 30 more tests!


[Survived] EqualityOperator
lib/store/connections.ts:127:9
-       if (input.ssl !== undefined) rec.ssl = input.ssl;
+       if (input.ssl === undefined) rec.ssl = input.ssl;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 30 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:130:9
-       if (clean !== undefined) rec.fields = clean;
+       if (true) rec.fields = clean;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 30 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:130:9
-       if (clean !== undefined) rec.fields = clean;
+       if (false) rec.fields = clean;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 30 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:133:9
-       if (input.url !== undefined) {
+       if (true) {
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 30 more tests!


[Survived] EqualityOperator
lib/store/connections.ts:130:9
-       if (clean !== undefined) rec.fields = clean;
+       if (clean === undefined) rec.fields = clean;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 30 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:133:9
-       if (input.url !== undefined) {
+       if (false) {
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 30 more tests!


[Survived] BlockStatement
lib/store/connections.ts:133:34
-       if (input.url !== undefined) {
-         rec.urlSafe = redactUrl(input.url);
-         secretPatch.url = input.url;
-       }
+       if (input.url !== undefined) {}
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 28 more tests!


[Survived] EqualityOperator
lib/store/connections.ts:133:9
-       if (input.url !== undefined) {
+       if (input.url === undefined) {
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 30 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:137:9
-       if (password !== undefined) secretPatch.password = password;
+       if (true) secretPatch.password = password;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 30 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:137:9
-       if (password !== undefined) secretPatch.password = password;
+       if (false) secretPatch.password = password;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 30 more tests!


[Survived] EqualityOperator
lib/store/connections.ts:137:9
-       if (password !== undefined) secretPatch.password = password;
+       if (password === undefined) secretPatch.password = password;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 30 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:138:9
-       if (Object.keys(secretPatch).length > 0) this.secrets.set(input.id, secretPatch);
+       if (true) this.secrets.set(input.id, secretPatch);
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 30 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:138:9
-       if (Object.keys(secretPatch).length > 0) this.secrets.set(input.id, secretPatch);
+       if (false) this.secrets.set(input.id, secretPatch);
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 30 more tests!


[Survived] EqualityOperator
lib/store/connections.ts:138:9
-       if (Object.keys(secretPatch).length > 0) this.secrets.set(input.id, secretPatch);
+       if (Object.keys(secretPatch).length >= 0) this.secrets.set(input.id, secretPatch);
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 30 more tests!


[Survived] CallExpression
lib/store/connections.ts:138:46
-       if (Object.keys(secretPatch).length > 0) this.secrets.set(input.id, secretPatch);
+       if (Object.keys(secretPatch).length > 0) ;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 28 more tests!


[Survived] EqualityOperator
lib/store/connections.ts:138:9
-       if (Object.keys(secretPatch).length > 0) this.secrets.set(input.id, secretPatch);
+       if (Object.keys(secretPatch).length <= 0) this.secrets.set(input.id, secretPatch);
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 30 more tests!


[Survived] CallExpression
lib/store/connections.ts:140:5
-       data.connections.push(rec);
+       ;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 30 more tests!


[Survived] CallExpression
lib/store/connections.ts:141:5
-       this.save(data);
+       ;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 30 more tests!


[Survived] BlockStatement
lib/store/connections.ts:146:80
-     update(id: string, patch: ConnectionUpdateInput): ConnectionMeta | undefined {
-       const data = this.load();
-       const rec = this.findRec(data, id);
-       if (!rec) return undefined;
-       if (patch.name !== undefined) rec.name = patch.name;
-       if (patch.ssl !== undefined) rec.ssl = patch.ssl;
-       if (patch.fields !== undefined) {
-         const { clean, password } = splitPassword(patch.fields);
-         rec.fields = clean;
-         if (password !== undefined) this.secrets.set(id, { password });
-       }
-       if (patch.url !== undefined) {
-         rec.urlSafe = redactUrl(patch.url);
-         this.secrets.set(id, { url: patch.url });
-       }
-       this.save(data);
-       return toMeta(rec);
-     }
+     update(id: string, patch: ConnectionUpdateInput): ConnectionMeta | undefined {}
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets


[Survived] BooleanLiteral
lib/store/connections.ts:149:9
-       if (!rec) return undefined;
+       if (rec) return undefined;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets


[Survived] ConditionalExpression
lib/store/connections.ts:149:9
-       if (!rec) return undefined;
+       if (true) return undefined;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets


[Survived] ConditionalExpression
lib/store/connections.ts:149:9
-       if (!rec) return undefined;
+       if (false) return undefined;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets


[Survived] ConditionalExpression
lib/store/connections.ts:151:9
-       if (patch.name !== undefined) rec.name = patch.name;
+       if (true) rec.name = patch.name;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets


[Survived] ConditionalExpression
lib/store/connections.ts:151:9
-       if (patch.name !== undefined) rec.name = patch.name;
+       if (false) rec.name = patch.name;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets


[Survived] EqualityOperator
lib/store/connections.ts:151:9
-       if (patch.name !== undefined) rec.name = patch.name;
+       if (patch.name === undefined) rec.name = patch.name;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets


[Survived] ConditionalExpression
lib/store/connections.ts:152:9
-       if (patch.ssl !== undefined) rec.ssl = patch.ssl;
+       if (true) rec.ssl = patch.ssl;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets


[Survived] ConditionalExpression
lib/store/connections.ts:152:9
-       if (patch.ssl !== undefined) rec.ssl = patch.ssl;
+       if (false) rec.ssl = patch.ssl;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets


[Survived] EqualityOperator
lib/store/connections.ts:152:9
-       if (patch.ssl !== undefined) rec.ssl = patch.ssl;
+       if (patch.ssl === undefined) rec.ssl = patch.ssl;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets


[Survived] BlockStatement
lib/store/connections.ts:153:37
-       if (patch.fields !== undefined) {
-         const { clean, password } = splitPassword(patch.fields);
-         rec.fields = clean;
-         if (password !== undefined) this.secrets.set(id, { password });
-       }
+       if (patch.fields !== undefined) {}
Tests ran:
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets


[Survived] ConditionalExpression
lib/store/connections.ts:156:11
-         if (password !== undefined) this.secrets.set(id, { password });
+         if (true) this.secrets.set(id, { password });
Tests ran:
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets


[Survived] ConditionalExpression
lib/store/connections.ts:153:9
-       if (patch.fields !== undefined) {
+       if (true) {
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets


[Survived] ConditionalExpression
lib/store/connections.ts:153:9
-       if (patch.fields !== undefined) {
+       if (false) {
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets


[Survived] EqualityOperator
lib/store/connections.ts:153:9
-       if (patch.fields !== undefined) {
+       if (patch.fields === undefined) {
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets


[Survived] ConditionalExpression
lib/store/connections.ts:156:11
-         if (password !== undefined) this.secrets.set(id, { password });
+         if (false) this.secrets.set(id, { password });
Tests ran:
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets


[Survived] EqualityOperator
lib/store/connections.ts:156:11
-         if (password !== undefined) this.secrets.set(id, { password });
+         if (password === undefined) this.secrets.set(id, { password });
Tests ran:
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets


[Survived] BlockStatement
lib/store/connections.ts:158:34
-       if (patch.url !== undefined) {
-         rec.urlSafe = redactUrl(patch.url);
-         this.secrets.set(id, { url: patch.url });
-       }
+       if (patch.url !== undefined) {}
Tests ran:
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets


[Survived] ObjectLiteral
lib/store/connections.ts:160:28
-         this.secrets.set(id, { url: patch.url });
+         this.secrets.set(id, {});
Tests ran:
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets


[Survived] ConditionalExpression
lib/store/connections.ts:158:9
-       if (patch.url !== undefined) {
+       if (true) {
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets


[Survived] EqualityOperator
lib/store/connections.ts:158:9
-       if (patch.url !== undefined) {
+       if (patch.url === undefined) {
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets


[Survived] ConditionalExpression
lib/store/connections.ts:158:9
-       if (patch.url !== undefined) {
+       if (false) {
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets


[Survived] CallExpression
lib/store/connections.ts:162:5
-       this.save(data);
+       ;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    ConnectionStore update 更新 name/ssl/fields/url 并同步 secrets


[Survived] BlockStatement
lib/store/connections.ts:167:31
-     remove(id: string): boolean {
-       const data = this.load();
-       const idx = data.connections.findIndex((c) => c.id === id);
-       if (idx < 0) return false;
-       data.connections.splice(idx, 1);
-       this.save(data);
-       this.secrets.delete(id);
-       this.grants.removeConn(id);
-       return true;
-     }
+     remove(id: string): boolean {}
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
  and 1 more test!


[Survived] ConditionalExpression
lib/store/connections.ts:169:51
-       const idx = data.connections.findIndex((c) => c.id === id);
+       const idx = data.connections.findIndex((c) => true);
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
  and 1 more test!


[Survived] ArrowFunction
lib/store/connections.ts:169:44
-       const idx = data.connections.findIndex((c) => c.id === id);
+       const idx = data.connections.findIndex(() => undefined);
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
  and 1 more test!


[Survived] ConditionalExpression
lib/store/connections.ts:169:51
-       const idx = data.connections.findIndex((c) => c.id === id);
+       const idx = data.connections.findIndex((c) => false);
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
  and 1 more test!


[Survived] EqualityOperator
lib/store/connections.ts:169:51
-       const idx = data.connections.findIndex((c) => c.id === id);
+       const idx = data.connections.findIndex((c) => c.id !== id);
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
  and 1 more test!


[Survived] ConditionalExpression
lib/store/connections.ts:170:9
-       if (idx < 0) return false;
+       if (true) return false;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
  and 1 more test!


[Survived] ConditionalExpression
lib/store/connections.ts:170:9
-       if (idx < 0) return false;
+       if (false) return false;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
  and 1 more test!


[Survived] EqualityOperator
lib/store/connections.ts:170:9
-       if (idx < 0) return false;
+       if (idx <= 0) return false;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
  and 1 more test!


[Survived] BooleanLiteral
lib/store/connections.ts:170:25
-       if (idx < 0) return false;
+       if (idx < 0) return true;
Tests ran:
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
    ConnectionStore remove 级联删除 secrets 与全部项目的 grants


[Survived] EqualityOperator
lib/store/connections.ts:170:9
-       if (idx < 0) return false;
+       if (idx >= 0) return false;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
  and 1 more test!


[Survived] CallExpression
lib/store/connections.ts:171:5
-       data.connections.splice(idx, 1);
+       ;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
    ConnectionStore remove 级联删除 secrets 与全部项目的 grants


[Survived] CallExpression
lib/store/connections.ts:172:5
-       this.save(data);
+       ;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
    ConnectionStore remove 级联删除 secrets 与全部项目的 grants


[Survived] CallExpression
lib/store/connections.ts:173:5
-       this.secrets.delete(id);
+       ;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
    ConnectionStore remove 级联删除 secrets 与全部项目的 grants


[Survived] CallExpression
lib/store/connections.ts:174:5
-       this.grants.removeConn(id);
+       ;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
    ConnectionStore remove 级联删除 secrets 与全部项目的 grants


[Survived] BooleanLiteral
lib/store/connections.ts:175:12
-       return true;
+       return false;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
    ConnectionStore remove 级联删除 secrets 与全部项目的 grants


[Survived] BlockStatement
lib/store/connections.ts:182:46
-     testTarget(id: string): ResolvedConnection {
-       const rec = this.findRec(this.load(), id);
-       if (!rec) throw new Error(`连接不存在: ${id}`);
-       const meta = toMeta(rec);
-       const rc: ResolvedConnection = { meta };
-       const sec = this.secrets.get(id);
-       if (sec?.url !== undefined) rc.url = sec.url;
-       let fields = rec.fields ? { ...rec.fields } : undefined;
-       if (sec?.password !== undefined) (fields ??= {}).password = sec.password;
-       if (fields !== undefined) rc.fields = fields;
-       if (rec.ssl !== undefined) rc.ssl = rec.ssl;
-       return rc;
-     }
+     testTarget(id: string): ResolvedConnection {}
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
    DbToolService: 授权与只读 rw 连接 mode=rw 透传，DML 直接执行
  and 14 more tests!


[Survived] StringLiteral
lib/store/connections.ts:184:31
-       if (!rec) throw new Error(`连接不存在: ${id}`);
+       if (!rec) throw new Error(``);
Tests ran:
    ConnectionStore testTarget 返回含机密的 ResolvedConnection；不存在抛错


[Survived] BooleanLiteral
lib/store/connections.ts:184:9
-       if (!rec) throw new Error(`连接不存在: ${id}`);
+       if (rec) throw new Error(`连接不存在: ${id}`);
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
    DbToolService: 授权与只读 rw 连接 mode=rw 透传，DML 直接执行
  and 14 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:184:9
-       if (!rec) throw new Error(`连接不存在: ${id}`);
+       if (true) throw new Error(`连接不存在: ${id}`);
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
    DbToolService: 授权与只读 rw 连接 mode=rw 透传，DML 直接执行
  and 14 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:184:9
-       if (!rec) throw new Error(`连接不存在: ${id}`);
+       if (false) throw new Error(`连接不存在: ${id}`);
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
    DbToolService: 授权与只读 rw 连接 mode=rw 透传，DML 直接执行
  and 14 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:188:9
-       if (sec?.url !== undefined) rc.url = sec.url;
+       if (true) rc.url = sec.url;
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
    DbToolService: 授权与只读 rw 连接 mode=rw 透传，DML 直接执行
  and 14 more tests!


[Survived] ObjectLiteral
lib/store/connections.ts:186:36
-       const rc: ResolvedConnection = { meta };
+       const rc: ResolvedConnection = {};
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
    DbToolService: 授权与只读 rw 连接 mode=rw 透传，DML 直接执行
  and 14 more tests!


[Survived] ObjectLiteral
lib/store/connections.ts:189:31
-       let fields = rec.fields ? { ...rec.fields } : undefined;
+       let fields = rec.fields ? {} : undefined;
Tests ran:
    ConnectionStore testTarget 返回含机密的 ResolvedConnection；不存在抛错


[Survived] ConditionalExpression
lib/store/connections.ts:188:9
-       if (sec?.url !== undefined) rc.url = sec.url;
+       if (false) rc.url = sec.url;
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
    DbToolService: 授权与只读 rw 连接 mode=rw 透传，DML 直接执行
  and 14 more tests!


[Survived] EqualityOperator
lib/store/connections.ts:188:9
-       if (sec?.url !== undefined) rc.url = sec.url;
+       if (sec?.url === undefined) rc.url = sec.url;
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
    DbToolService: 授权与只读 rw 连接 mode=rw 透传，DML 直接执行
  and 14 more tests!


[Survived] OptionalChaining
lib/store/connections.ts:188:9
-       if (sec?.url !== undefined) rc.url = sec.url;
+       if (sec.url !== undefined) rc.url = sec.url;
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
    DbToolService: 授权与只读 rw 连接 mode=rw 透传，DML 直接执行
  and 14 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:190:9
-       if (sec?.password !== undefined) (fields ??= {}).password = sec.password;
+       if (true) (fields ??= {}).password = sec.password;
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
    DbToolService: 授权与只读 rw 连接 mode=rw 透传，DML 直接执行
  and 14 more tests!


[Survived] EqualityOperator
lib/store/connections.ts:190:9
-       if (sec?.password !== undefined) (fields ??= {}).password = sec.password;
+       if (sec?.password === undefined) (fields ??= {}).password = sec.password;
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
    DbToolService: 授权与只读 rw 连接 mode=rw 透传，DML 直接执行
  and 14 more tests!


[Survived] AssignmentOperator
lib/store/connections.ts:190:39
-       if (sec?.password !== undefined) (fields ??= {}).password = sec.password;
+       if (sec?.password !== undefined) (fields &&= {}).password = sec.password;
Tests ran:
    ConnectionStore testTarget 返回含机密的 ResolvedConnection；不存在抛错


[Survived] ConditionalExpression
lib/store/connections.ts:190:9
-       if (sec?.password !== undefined) (fields ??= {}).password = sec.password;
+       if (false) (fields ??= {}).password = sec.password;
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
    DbToolService: 授权与只读 rw 连接 mode=rw 透传，DML 直接执行
  and 14 more tests!


[Survived] OptionalChaining
lib/store/connections.ts:190:9
-       if (sec?.password !== undefined) (fields ??= {}).password = sec.password;
+       if (sec.password !== undefined) (fields ??= {}).password = sec.password;
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
    DbToolService: 授权与只读 rw 连接 mode=rw 透传，DML 直接执行
  and 14 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:191:9
-       if (fields !== undefined) rc.fields = fields;
+       if (true) rc.fields = fields;
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
    DbToolService: 授权与只读 rw 连接 mode=rw 透传，DML 直接执行
  and 14 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:191:9
-       if (fields !== undefined) rc.fields = fields;
+       if (false) rc.fields = fields;
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
    DbToolService: 授权与只读 rw 连接 mode=rw 透传，DML 直接执行
  and 14 more tests!


[Survived] EqualityOperator
lib/store/connections.ts:191:9
-       if (fields !== undefined) rc.fields = fields;
+       if (fields === undefined) rc.fields = fields;
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
    DbToolService: 授权与只读 rw 连接 mode=rw 透传，DML 直接执行
  and 14 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:192:9
-       if (rec.ssl !== undefined) rc.ssl = rec.ssl;
+       if (true) rc.ssl = rec.ssl;
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
    DbToolService: 授权与只读 rw 连接 mode=rw 透传，DML 直接执行
  and 14 more tests!


[Survived] ConditionalExpression
lib/store/connections.ts:192:9
-       if (rec.ssl !== undefined) rc.ssl = rec.ssl;
+       if (false) rc.ssl = rec.ssl;
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
    DbToolService: 授权与只读 rw 连接 mode=rw 透传，DML 直接执行
  and 14 more tests!


[Survived] EqualityOperator
lib/store/connections.ts:192:9
-       if (rec.ssl !== undefined) rc.ssl = rec.ssl;
+       if (rec.ssl === undefined) rc.ssl = rec.ssl;
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
    DbToolService: 授权与只读 rw 连接 mode=rw 透传，DML 直接执行
  and 14 more tests!


[Survived] BlockStatement
lib/store/grants.ts:22:28
-     constructor(dir: string) {
-       this.file = path.join(dir, 'grants.json');
-     }
+     constructor(dir: string) {}
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 44 more tests!


[Survived] BlockStatement
lib/store/grants.ts:26:30
-     private load(): GrantsFile {
-       return readJson<GrantsFile>(this.file, { grants: {} });
-     }
+     private load(): GrantsFile {}
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
  and 37 more tests!


[Survived] ObjectLiteral
lib/store/grants.ts:27:44
-       return readJson<GrantsFile>(this.file, { grants: {} });
+       return readJson<GrantsFile>(this.file, {});
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
  and 37 more tests!


[Survived] StringLiteral
lib/store/grants.ts:23:32
-       this.file = path.join(dir, 'grants.json');
+       this.file = path.join(dir, "");
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 44 more tests!


[Survived] BlockStatement
lib/store/grants.ts:35:73
-     grantsFor(projectKey: string): { connId: string; mode: AccessMode }[] {
-       return (this.load().grants[projectKey] ?? []).map(({ connId, mode }) => ({ connId, mode }));
-     }
+     grantsFor(projectKey: string): { connId: string; mode: AccessMode }[] {}
Tests ran:
    连接管理与授权路由 grants PUT/GET/DELETE
    业务路由与确认流程 GET /api/audit 与 /api/state
    GrantStore grant 后 grantsFor/check 可见
  and 3 more tests!


[Survived] LogicalOperator
lib/store/grants.ts:36:13
-       return (this.load().grants[projectKey] ?? []).map(({ connId, mode }) => ({ connId, mode }));
+       return (this.load().grants[projectKey] && []).map(({ connId, mode }) => ({ connId, mode }));
Tests ran:
    连接管理与授权路由 grants PUT/GET/DELETE
    业务路由与确认流程 GET /api/audit 与 /api/state
    GrantStore grant 后 grantsFor/check 可见
  and 3 more tests!


[Survived] BlockStatement
lib/store/grants.ts:30:40
-     private save(data: GrantsFile): void {
-       writeJsonAtomic(this.file, data);
-     }
+     private save(data: GrantsFile): void {}
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 grants PUT/GET/DELETE
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 28 more tests!


[Survived] ArrayDeclaration
lib/store/grants.ts:36:47
-       return (this.load().grants[projectKey] ?? []).map(({ connId, mode }) => ({ connId, mode }));
+       return (this.load().grants[projectKey] ?? ["Stryker was here"]).map(({ connId, mode }) => ({ connId, mode }));
Tests ran:
    GrantStore grantsFor 未授权项目返回空数组


[Survived] ArrowFunction
lib/store/grants.ts:36:55
-       return (this.load().grants[projectKey] ?? []).map(({ connId, mode }) => ({ connId, mode }));
+       return (this.load().grants[projectKey] ?? []).map(() => undefined);
Tests ran:
    连接管理与授权路由 grants PUT/GET/DELETE
    业务路由与确认流程 GET /api/audit 与 /api/state
    GrantStore grant 后 grantsFor/check 可见
  and 3 more tests!


[Survived] CallExpression
lib/store/grants.ts:31:5
-       writeJsonAtomic(this.file, data);
+       ;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 grants PUT/GET/DELETE
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 28 more tests!


[Survived] ObjectLiteral
lib/store/grants.ts:36:78
-       return (this.load().grants[projectKey] ?? []).map(({ connId, mode }) => ({ connId, mode }));
+       return (this.load().grants[projectKey] ?? []).map(({ connId, mode }) => ({}));
Tests ran:
    连接管理与授权路由 grants PUT/GET/DELETE
    业务路由与确认流程 GET /api/audit 与 /api/state
    GrantStore grant 后 grantsFor/check 可见
  and 2 more tests!


[Survived] BlockStatement
lib/store/grants.ts:40:69
-     grant(projectKey: string, connId: string, mode: AccessMode): void {
-       const data = this.load();
-       const list = data.grants[projectKey] ?? [];
-       const existing = list.find((g) => g.connId === connId);
-       if (existing) {
-         existing.mode = mode;
-         existing.grantedAt = new Date().toISOString();
-       } else {
-         list.push({ connId, mode, grantedAt: new Date().toISOString() });
-       }
-       data.grants[projectKey] = list;
-       this.save(data);
-     }
+     grant(projectKey: string, connId: string, mode: AccessMode): void {}
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 grants PUT/GET/DELETE
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 28 more tests!


[Survived] ArrayDeclaration
lib/store/grants.ts:42:45
-       const list = data.grants[projectKey] ?? [];
+       const list = data.grants[projectKey] ?? ["Stryker was here"];
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 grants PUT/GET/DELETE
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 28 more tests!


[Survived] ArrowFunction
lib/store/grants.ts:43:32
-       const existing = list.find((g) => g.connId === connId);
+       const existing = list.find(() => undefined);
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 grants PUT/GET/DELETE
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 28 more tests!


[Survived] LogicalOperator
lib/store/grants.ts:42:18
-       const list = data.grants[projectKey] ?? [];
+       const list = data.grants[projectKey] && [];
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 grants PUT/GET/DELETE
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 28 more tests!


[Survived] ConditionalExpression
lib/store/grants.ts:43:39
-       const existing = list.find((g) => g.connId === connId);
+       const existing = list.find((g) => true);
Tests ran:
    连接管理与授权路由 grants PUT/GET/DELETE
    ConnectionStore remove 级联删除 secrets 与全部项目的 grants
    GrantStore 同连接重复授权更新模式（ro → rw）
  and 1 more test!


[Survived] ConditionalExpression
lib/store/grants.ts:43:39
-       const existing = list.find((g) => g.connId === connId);
+       const existing = list.find((g) => false);
Tests ran:
    连接管理与授权路由 grants PUT/GET/DELETE
    ConnectionStore remove 级联删除 secrets 与全部项目的 grants
    GrantStore 同连接重复授权更新模式（ro → rw）
  and 1 more test!


[Survived] EqualityOperator
lib/store/grants.ts:43:39
-       const existing = list.find((g) => g.connId === connId);
+       const existing = list.find((g) => g.connId !== connId);
Tests ran:
    连接管理与授权路由 grants PUT/GET/DELETE
    ConnectionStore remove 级联删除 secrets 与全部项目的 grants
    GrantStore 同连接重复授权更新模式（ro → rw）
  and 1 more test!


[Survived] BlockStatement
lib/store/grants.ts:44:19
-       if (existing) {
-         existing.mode = mode;
-         existing.grantedAt = new Date().toISOString();
-       } else {
+       if (existing) {} else {
Tests ran:
    连接管理与授权路由 grants PUT/GET/DELETE
    GrantStore 同连接重复授权更新模式（ro → rw）


[Survived] ConditionalExpression
lib/store/grants.ts:44:9
-       if (existing) {
+       if (true) {
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 grants PUT/GET/DELETE
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 28 more tests!


[Survived] ConditionalExpression
lib/store/grants.ts:44:9
-       if (existing) {
+       if (false) {
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 grants PUT/GET/DELETE
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 28 more tests!


[Survived] BlockStatement
lib/store/grants.ts:47:12
-       } else {
-         list.push({ connId, mode, grantedAt: new Date().toISOString() });
-       }
+       } else {}
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 grants PUT/GET/DELETE
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 28 more tests!


[Survived] BlockStatement
lib/store/grants.ts:55:52
-     revoke(projectKey: string, connId: string): void {
-       const data = this.load();
-       const list = data.grants[projectKey];
-       if (!list) return;
-       const next = list.filter((g) => g.connId !== connId);
-       if (next.length === list.length) return;
-       if (next.length === 0) delete data.grants[projectKey];
-       else data.grants[projectKey] = next;
-       this.save(data);
-     }
+     revoke(projectKey: string, connId: string): void {}
Tests ran:
    连接管理与授权路由 grants PUT/GET/DELETE
    GrantStore revoke 撤销后不可见；撤销不存在者静默
    GrantStore 同一连接多项目授权互不干扰


[Survived] BooleanLiteral
lib/store/grants.ts:58:9
-       if (!list) return;
+       if (list) return;
Tests ran:
    连接管理与授权路由 grants PUT/GET/DELETE
    GrantStore revoke 撤销后不可见；撤销不存在者静默
    GrantStore 同一连接多项目授权互不干扰


[Survived] ObjectLiteral
lib/store/grants.ts:48:17
-         list.push({ connId, mode, grantedAt: new Date().toISOString() });
+         list.push({});
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 grants PUT/GET/DELETE
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 28 more tests!


[Survived] CallExpression
lib/store/grants.ts:51:5
-       this.save(data);
+       ;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 grants PUT/GET/DELETE
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 28 more tests!


[Survived] ConditionalExpression
lib/store/grants.ts:58:9
-       if (!list) return;
+       if (true) return;
Tests ran:
    连接管理与授权路由 grants PUT/GET/DELETE
    GrantStore revoke 撤销后不可见；撤销不存在者静默
    GrantStore 同一连接多项目授权互不干扰


[Survived] ConditionalExpression
lib/store/grants.ts:58:9
-       if (!list) return;
+       if (false) return;
Tests ran:
    连接管理与授权路由 grants PUT/GET/DELETE
    GrantStore revoke 撤销后不可见；撤销不存在者静默
    GrantStore 同一连接多项目授权互不干扰


[Survived] MethodExpression
lib/store/grants.ts:59:18
-       const next = list.filter((g) => g.connId !== connId);
+       const next = list;
Tests ran:
    连接管理与授权路由 grants PUT/GET/DELETE
    GrantStore revoke 撤销后不可见；撤销不存在者静默
    GrantStore 同一连接多项目授权互不干扰


[Survived] ArrowFunction
lib/store/grants.ts:59:30
-       const next = list.filter((g) => g.connId !== connId);
+       const next = list.filter(() => undefined);
Tests ran:
    连接管理与授权路由 grants PUT/GET/DELETE
    GrantStore revoke 撤销后不可见；撤销不存在者静默
    GrantStore 同一连接多项目授权互不干扰


[Survived] ConditionalExpression
lib/store/grants.ts:59:37
-       const next = list.filter((g) => g.connId !== connId);
+       const next = list.filter((g) => true);
Tests ran:
    连接管理与授权路由 grants PUT/GET/DELETE
    GrantStore revoke 撤销后不可见；撤销不存在者静默
    GrantStore 同一连接多项目授权互不干扰


[Survived] ConditionalExpression
lib/store/grants.ts:59:37
-       const next = list.filter((g) => g.connId !== connId);
+       const next = list.filter((g) => false);
Tests ran:
    连接管理与授权路由 grants PUT/GET/DELETE
    GrantStore revoke 撤销后不可见；撤销不存在者静默
    GrantStore 同一连接多项目授权互不干扰


[Survived] EqualityOperator
lib/store/grants.ts:59:37
-       const next = list.filter((g) => g.connId !== connId);
+       const next = list.filter((g) => g.connId === connId);
Tests ran:
    连接管理与授权路由 grants PUT/GET/DELETE
    GrantStore revoke 撤销后不可见；撤销不存在者静默
    GrantStore 同一连接多项目授权互不干扰


[Survived] ConditionalExpression
lib/store/grants.ts:60:9
-       if (next.length === list.length) return;
+       if (true) return;
Tests ran:
    连接管理与授权路由 grants PUT/GET/DELETE
    GrantStore revoke 撤销后不可见；撤销不存在者静默
    GrantStore 同一连接多项目授权互不干扰


[Survived] ConditionalExpression
lib/store/grants.ts:60:9
-       if (next.length === list.length) return;
+       if (false) return;
Tests ran:
    连接管理与授权路由 grants PUT/GET/DELETE
    GrantStore revoke 撤销后不可见；撤销不存在者静默
    GrantStore 同一连接多项目授权互不干扰


[Survived] EqualityOperator
lib/store/grants.ts:60:9
-       if (next.length === list.length) return;
+       if (next.length !== list.length) return;
Tests ran:
    连接管理与授权路由 grants PUT/GET/DELETE
    GrantStore revoke 撤销后不可见；撤销不存在者静默
    GrantStore 同一连接多项目授权互不干扰


[Survived] ConditionalExpression
lib/store/grants.ts:61:9
-       if (next.length === 0) delete data.grants[projectKey];
+       if (true) delete data.grants[projectKey];
Tests ran:
    连接管理与授权路由 grants PUT/GET/DELETE
    GrantStore revoke 撤销后不可见；撤销不存在者静默
    GrantStore 同一连接多项目授权互不干扰


[Survived] ConditionalExpression
lib/store/grants.ts:61:9
-       if (next.length === 0) delete data.grants[projectKey];
+       if (false) delete data.grants[projectKey];
Tests ran:
    连接管理与授权路由 grants PUT/GET/DELETE
    GrantStore revoke 撤销后不可见；撤销不存在者静默
    GrantStore 同一连接多项目授权互不干扰


[Survived] EqualityOperator
lib/store/grants.ts:61:9
-       if (next.length === 0) delete data.grants[projectKey];
+       if (next.length !== 0) delete data.grants[projectKey];
Tests ran:
    连接管理与授权路由 grants PUT/GET/DELETE
    GrantStore revoke 撤销后不可见；撤销不存在者静默
    GrantStore 同一连接多项目授权互不干扰


[Survived] CallExpression
lib/store/grants.ts:63:5
-       this.save(data);
+       ;
Tests ran:
    连接管理与授权路由 grants PUT/GET/DELETE
    GrantStore revoke 撤销后不可见；撤销不存在者静默
    GrantStore 同一连接多项目授权互不干扰


[Survived] BlockStatement
lib/store/grants.ts:67:69
-     check(projectKey: string, connId: string): AccessMode | undefined {
-       const modes = (this.load().grants[projectKey] ?? [])
-         .filter((g) => g.connId === connId)
-         .map((g) => g.mode);
-       if (modes.includes('rw')) return 'rw';
-       return modes.includes('ro') ? 'ro' : undefined;
-     }
+     check(projectKey: string, connId: string): AccessMode | undefined {}
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
  and 27 more tests!


[Survived] MethodExpression
lib/store/grants.ts:68:19
-       const modes = (this.load().grants[projectKey] ?? [])
-         .filter((g) => g.connId === connId)
+       const modes = this.load().grants[projectKey] ?? []
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
  and 27 more tests!


[Survived] LogicalOperator
lib/store/grants.ts:68:20
-       const modes = (this.load().grants[projectKey] ?? [])
+       const modes = (this.load().grants[projectKey] && [])
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
  and 27 more tests!


[Survived] ArrayDeclaration
lib/store/grants.ts:68:54
-       const modes = (this.load().grants[projectKey] ?? [])
+       const modes = (this.load().grants[projectKey] ?? ["Stryker was here"])
Tests ran:
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出）
  and 2 more tests!


[Survived] ArrowFunction
lib/store/grants.ts:69:15
-         .filter((g) => g.connId === connId)
+         .filter(() => undefined)
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
  and 27 more tests!


[Survived] ConditionalExpression
lib/store/grants.ts:69:22
-         .filter((g) => g.connId === connId)
+         .filter((g) => true)
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
  and 24 more tests!


[Survived] ConditionalExpression
lib/store/grants.ts:69:22
-         .filter((g) => g.connId === connId)
+         .filter((g) => false)
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
  and 24 more tests!


[Survived] EqualityOperator
lib/store/grants.ts:69:22
-         .filter((g) => g.connId === connId)
+         .filter((g) => g.connId !== connId)
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
  and 24 more tests!


[Survived] ArrowFunction
lib/store/grants.ts:70:12
-         .map((g) => g.mode);
+         .map(() => undefined);
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
  and 27 more tests!


[Survived] ConditionalExpression
lib/store/grants.ts:71:9
-       if (modes.includes('rw')) return 'rw';
+       if (true) return 'rw';
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
  and 27 more tests!


[Survived] ConditionalExpression
lib/store/grants.ts:71:9
-       if (modes.includes('rw')) return 'rw';
+       if (false) return 'rw';
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
  and 27 more tests!


[Survived] StringLiteral
lib/store/grants.ts:71:24
-       if (modes.includes('rw')) return 'rw';
+       if (modes.includes("")) return 'rw';
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
  and 27 more tests!


[Survived] StringLiteral
lib/store/grants.ts:72:35
-       return modes.includes('ro') ? 'ro' : undefined;
+       return modes.includes('ro') ? "" : undefined;
Tests ran:
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
    DbToolService: 危险确认与 challenge query 通道的写语句同样需要确认；SELECT 直通
    DbToolService: 危险确认与 challenge ro 连接对危险语句也先报 READ_ONLY（授权优先于 guard）
  and 2 more tests!


[Survived] StringLiteral
lib/store/grants.ts:72:27
-       return modes.includes('ro') ? 'ro' : undefined;
+       return modes.includes("") ? 'ro' : undefined;
Tests ran:
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 8 more tests!


[Survived] StringLiteral
lib/store/grants.ts:71:38
-       if (modes.includes('rw')) return 'rw';
+       if (modes.includes('rw')) return "";
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
  and 19 more tests!


[Survived] BlockStatement
lib/store/grants.ts:76:36
-     removeConn(connId: string): void {
-       const data = this.load();
-       let dirty = false;
-       for (const key of Object.keys(data.grants)) {
-         const list = data.grants[key];
-         if (!list) continue;
-         const next = list.filter((g) => g.connId !== connId);
-         if (next.length !== list.length) {
-           dirty = true;
-           if (next.length === 0) delete data.grants[key];
-           else data.grants[key] = next;
-         }
-       }
-       if (dirty) this.save(data);
-     }
+     removeConn(connId: string): void {}
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
    ConnectionStore remove 级联删除 secrets 与全部项目的 grants


[Survived] BooleanLiteral
lib/store/grants.ts:78:17
-       let dirty = false;
+       let dirty = true;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
    ConnectionStore remove 级联删除 secrets 与全部项目的 grants


[Survived] BooleanLiteral
lib/store/grants.ts:81:11
-         if (!list) continue;
+         if (list) continue;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
    ConnectionStore remove 级联删除 secrets 与全部项目的 grants


[Survived] BlockStatement
lib/store/grants.ts:79:49
-       for (const key of Object.keys(data.grants)) {
-         const list = data.grants[key];
-         if (!list) continue;
-         const next = list.filter((g) => g.connId !== connId);
-         if (next.length !== list.length) {
-           dirty = true;
-           if (next.length === 0) delete data.grants[key];
-           else data.grants[key] = next;
-         }
-       }
+       for (const key of Object.keys(data.grants)) {}
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
    ConnectionStore remove 级联删除 secrets 与全部项目的 grants


[Survived] ConditionalExpression
lib/store/grants.ts:81:11
-         if (!list) continue;
+         if (true) continue;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
    ConnectionStore remove 级联删除 secrets 与全部项目的 grants


[Survived] ConditionalExpression
lib/store/grants.ts:81:11
-         if (!list) continue;
+         if (false) continue;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
    ConnectionStore remove 级联删除 secrets 与全部项目的 grants


[Survived] MethodExpression
lib/store/grants.ts:82:20
-         const next = list.filter((g) => g.connId !== connId);
+         const next = list;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
    ConnectionStore remove 级联删除 secrets 与全部项目的 grants


[Survived] ArrowFunction
lib/store/grants.ts:82:32
-         const next = list.filter((g) => g.connId !== connId);
+         const next = list.filter(() => undefined);
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
    ConnectionStore remove 级联删除 secrets 与全部项目的 grants


[Survived] ConditionalExpression
lib/store/grants.ts:82:39
-         const next = list.filter((g) => g.connId !== connId);
+         const next = list.filter((g) => true);
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
    ConnectionStore remove 级联删除 secrets 与全部项目的 grants


[Survived] ConditionalExpression
lib/store/grants.ts:82:39
-         const next = list.filter((g) => g.connId !== connId);
+         const next = list.filter((g) => false);
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
    ConnectionStore remove 级联删除 secrets 与全部项目的 grants


[Survived] EqualityOperator
lib/store/grants.ts:82:39
-         const next = list.filter((g) => g.connId !== connId);
+         const next = list.filter((g) => g.connId === connId);
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
    ConnectionStore remove 级联删除 secrets 与全部项目的 grants


[Survived] ConditionalExpression
lib/store/grants.ts:83:11
-         if (next.length !== list.length) {
+         if (true) {
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
    ConnectionStore remove 级联删除 secrets 与全部项目的 grants


[Survived] ConditionalExpression
lib/store/grants.ts:83:11
-         if (next.length !== list.length) {
+         if (false) {
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
    ConnectionStore remove 级联删除 secrets 与全部项目的 grants


[Survived] BlockStatement
lib/store/grants.ts:83:40
-         if (next.length !== list.length) {
-           dirty = true;
-           if (next.length === 0) delete data.grants[key];
-           else data.grants[key] = next;
-         }
+         if (next.length !== list.length) {}
Tests ran:
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
    ConnectionStore remove 级联删除 secrets 与全部项目的 grants


[Survived] BooleanLiteral
lib/store/grants.ts:84:17
-           dirty = true;
+           dirty = false;
Tests ran:
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
    ConnectionStore remove 级联删除 secrets 与全部项目的 grants


[Survived] EqualityOperator
lib/store/grants.ts:83:11
-         if (next.length !== list.length) {
+         if (next.length === list.length) {
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
    ConnectionStore remove 级联删除 secrets 与全部项目的 grants


[Survived] ConditionalExpression
lib/store/grants.ts:85:13
-           if (next.length === 0) delete data.grants[key];
+           if (true) delete data.grants[key];
Tests ran:
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
    ConnectionStore remove 级联删除 secrets 与全部项目的 grants


[Survived] ConditionalExpression
lib/store/grants.ts:85:13
-           if (next.length === 0) delete data.grants[key];
+           if (false) delete data.grants[key];
Tests ran:
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
    ConnectionStore remove 级联删除 secrets 与全部项目的 grants


[Survived] EqualityOperator
lib/store/grants.ts:85:13
-           if (next.length === 0) delete data.grants[key];
+           if (next.length !== 0) delete data.grants[key];
Tests ran:
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
    ConnectionStore remove 级联删除 secrets 与全部项目的 grants


[Survived] CallExpression
lib/store/grants.ts:89:16
-       if (dirty) this.save(data);
+       if (dirty) ;
Tests ran:
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
    ConnectionStore remove 级联删除 secrets 与全部项目的 grants


[Survived] ConditionalExpression
lib/store/grants.ts:89:9
-       if (dirty) this.save(data);
+       if (true) this.save(data);
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
    ConnectionStore remove 级联删除 secrets 与全部项目的 grants


[Survived] ConditionalExpression
lib/store/grants.ts:89:9
-       if (dirty) this.save(data);
+       if (false) this.save(data);
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
    ConnectionStore remove 级联删除 secrets 与全部项目的 grants


[NoCoverage] StringLiteral
lib/store/index.ts:41:41
-       const home = homeDir ?? process.env['DSH_HOME'] ?? path.join(os.homedir(), '.dsh');
+       const home = homeDir ?? process.env[""] ?? path.join(os.homedir(), '.dsh');

[NoCoverage] StringLiteral
lib/store/index.ts:41:80
-       const home = homeDir ?? process.env['DSH_HOME'] ?? path.join(os.homedir(), '.dsh');
+       const home = homeDir ?? process.env['DSH_HOME'] ?? path.join(os.homedir(), "");

[Survived] BlockStatement
lib/store/index.ts:40:33
-     constructor(homeDir?: string) {
-       const home = homeDir ?? process.env['DSH_HOME'] ?? path.join(os.homedir(), '.dsh');
-       this.dir = path.join(home, 'db-tool');
-       fs.mkdirSync(this.dir, { recursive: true });
-       chmodBestEffort(this.dir, 0o700);
-       this.secrets = new SecretsBox(this.dir);
-       this.grants = new GrantStore(this.dir);
-       this.audit = new AuditLog(this.dir);
-       this.connections = new ConnectionStore(this.dir, this.secrets, this.grants);
-     }
+     constructor(homeDir?: string) {}
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 44 more tests!


[Survived] LogicalOperator
lib/store/index.ts:41:18
-       const home = homeDir ?? process.env['DSH_HOME'] ?? path.join(os.homedir(), '.dsh');
+       const home = (homeDir ?? process.env['DSH_HOME']) && path.join(os.homedir(), '.dsh');
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 44 more tests!


[Survived] LogicalOperator
lib/store/index.ts:41:18
-       const home = homeDir ?? process.env['DSH_HOME'] ?? path.join(os.homedir(), '.dsh');
+       const home = homeDir && process.env['DSH_HOME'] ?? path.join(os.homedir(), '.dsh');
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 44 more tests!


[Survived] StringLiteral
lib/store/index.ts:42:32
-       this.dir = path.join(home, 'db-tool');
+       this.dir = path.join(home, "");
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 44 more tests!


[Survived] ObjectLiteral
lib/store/index.ts:43:28
-       fs.mkdirSync(this.dir, { recursive: true });
+       fs.mkdirSync(this.dir, {});
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 44 more tests!


[Survived] BooleanLiteral
lib/store/index.ts:43:41
-       fs.mkdirSync(this.dir, { recursive: true });
+       fs.mkdirSync(this.dir, { recursive: false });
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 44 more tests!


[Survived] CallExpression
lib/store/index.ts:44:5
-       chmodBestEffort(this.dir, 0o700);
+       ;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 44 more tests!


[NoCoverage] BlockStatement
lib/store/io.ts:31:17
-     } catch (err) {
-       try {
-         fs.rmSync(tmp, { force: true });
-       } catch {
-         // 清理失败不掩盖原始错误
-       }
-       throw err;
-     }
+     } catch (err) {}

[NoCoverage] BlockStatement
lib/store/io.ts:32:9
-       try {
-         fs.rmSync(tmp, { force: true });
-       } catch {
+       try {} catch {

[NoCoverage] ObjectLiteral
lib/store/io.ts:33:22
-         fs.rmSync(tmp, { force: true });
+         fs.rmSync(tmp, {});

[NoCoverage] BooleanLiteral
lib/store/io.ts:33:31
-         fs.rmSync(tmp, { force: true });
+         fs.rmSync(tmp, { force: false });

[Survived] BlockStatement
lib/store/io.ts:11:69
-   export function chmodBestEffort(target: string, mode: number): void {
-     try {
-       fs.chmodSync(target, mode);
-     } catch {
-       // 权限设置失败（Windows 等）不影响功能
-     }
-   }
+   export function chmodBestEffort(target: string, mode: number): void {}
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 45 more tests!


[Survived] BlockStatement
lib/store/io.ts:12:7
-     try {
-       fs.chmodSync(target, mode);
-     } catch {
+     try {} catch {
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 45 more tests!


[Survived] CallExpression
lib/store/io.ts:13:5
-       fs.chmodSync(target, mode);
+       ;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 45 more tests!


[Survived] BlockStatement
lib/store/io.ts:24:88
-   export function writeJsonAtomic(file: string, value: unknown, fileMode?: number): void {
-     const dir = path.dirname(file);
-     fs.mkdirSync(dir, { recursive: true });
-     const tmp = path.join(dir, `.${path.basename(file)}.${process.pid}.${Date.now()}.tmp`);
-     fs.writeFileSync(tmp, JSON.stringify(value, null, 2) + '\n', 'utf8');
-     try {
-       fs.renameSync(tmp, file);
-     } catch (err) {
-       try {
-         fs.rmSync(tmp, { force: true });
-       } catch {
-         // 清理失败不掩盖原始错误
-       }
-       throw err;
-     }
-     if (fileMode !== undefined) chmodBestEffort(file, fileMode);
-   }
+   export function writeJsonAtomic(file: string, value: unknown, fileMode?: number): void {}
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    连接管理与授权路由 grants PUT/GET/DELETE
  and 40 more tests!


[Survived] ObjectLiteral
lib/store/io.ts:26:21
-     fs.mkdirSync(dir, { recursive: true });
+     fs.mkdirSync(dir, {});
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    连接管理与授权路由 grants PUT/GET/DELETE
  and 40 more tests!


[Survived] BooleanLiteral
lib/store/io.ts:26:34
-     fs.mkdirSync(dir, { recursive: true });
+     fs.mkdirSync(dir, { recursive: false });
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    连接管理与授权路由 grants PUT/GET/DELETE
  and 40 more tests!


[Survived] StringLiteral
lib/store/io.ts:27:30
-     const tmp = path.join(dir, `.${path.basename(file)}.${process.pid}.${Date.now()}.tmp`);
+     const tmp = path.join(dir, ``);
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    连接管理与授权路由 grants PUT/GET/DELETE
  and 40 more tests!


[Survived] StringLiteral
lib/store/io.ts:28:58
-     fs.writeFileSync(tmp, JSON.stringify(value, null, 2) + '\n', 'utf8');
+     fs.writeFileSync(tmp, JSON.stringify(value, null, 2) + "", 'utf8');
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    连接管理与授权路由 grants PUT/GET/DELETE
  and 40 more tests!


[Survived] StringLiteral
lib/store/io.ts:28:64
-     fs.writeFileSync(tmp, JSON.stringify(value, null, 2) + '\n', 'utf8');
+     fs.writeFileSync(tmp, JSON.stringify(value, null, 2) + '\n', "");
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    连接管理与授权路由 grants PUT/GET/DELETE
  and 40 more tests!


[Survived] BlockStatement
lib/store/io.ts:29:7
-     try {
-       fs.renameSync(tmp, file);
-     } catch (err) {
+     try {} catch (err) {
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    连接管理与授权路由 grants PUT/GET/DELETE
  and 40 more tests!


[Survived] CallExpression
lib/store/io.ts:30:5
-       fs.renameSync(tmp, file);
+       ;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    连接管理与授权路由 grants PUT/GET/DELETE
  and 40 more tests!


[Survived] ConditionalExpression
lib/store/io.ts:39:7
-     if (fileMode !== undefined) chmodBestEffort(file, fileMode);
+     if (true) chmodBestEffort(file, fileMode);
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    连接管理与授权路由 grants PUT/GET/DELETE
  and 40 more tests!


[Survived] ConditionalExpression
lib/store/io.ts:39:7
-     if (fileMode !== undefined) chmodBestEffort(file, fileMode);
+     if (false) chmodBestEffort(file, fileMode);
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    连接管理与授权路由 grants PUT/GET/DELETE
  and 40 more tests!


[Survived] EqualityOperator
lib/store/io.ts:39:7
-     if (fileMode !== undefined) chmodBestEffort(file, fileMode);
+     if (fileMode === undefined) chmodBestEffort(file, fileMode);
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    连接管理与授权路由 grants PUT/GET/DELETE
  and 40 more tests!


[Survived] CallExpression
lib/store/io.ts:39:31
-     if (fileMode !== undefined) chmodBestEffort(file, fileMode);
+     if (fileMode !== undefined) ;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 31 more tests!


[Survived] BlockStatement
lib/store/io.ts:48:59
-   export function readJson<T>(file: string, fallback: T): T {
-     if (!fs.existsSync(file)) return fallback;
-     try {
-       return JSON.parse(fs.readFileSync(file, 'utf8')) as T;
-     } catch {
-       try {
-         fs.rmSync(`${file}.bak`, { force: true });
-         fs.renameSync(file, `${file}.bak`);
-       } catch {
-         // 备份失败也要继续重建
-       }
-       return fallback;
-     }
-   }
+   export function readJson<T>(file: string, fallback: T): T {}
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    fenced 校验 loopback Origin 同端口放行
    fenced 校验 无 Origin/Referer（本地脚本）放行
  and 52 more tests!


[Survived] BooleanLiteral
lib/store/io.ts:49:7
-     if (!fs.existsSync(file)) return fallback;
+     if (fs.existsSync(file)) return fallback;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    fenced 校验 loopback Origin 同端口放行
    fenced 校验 无 Origin/Referer（本地脚本）放行
  and 52 more tests!


[Survived] BlockStatement
lib/store/io.ts:52:11
-     } catch {
-       try {
-         fs.rmSync(`${file}.bak`, { force: true });
-         fs.renameSync(file, `${file}.bak`);
-       } catch {
-         // 备份失败也要继续重建
-       }
-       return fallback;
-     }
+     } catch {}
Tests ran:
    io：原子写与损坏容错 损坏 JSON 备份为 .bak 并返回 fallback（store 级验证）
    io：原子写与损坏容错 secrets.json 损坏同样容错且重建后权限仍为 0600（POSIX）


[Survived] BlockStatement
lib/store/io.ts:53:9
-       try {
-         fs.rmSync(`${file}.bak`, { force: true });
-         fs.renameSync(file, `${file}.bak`);
-       } catch {
+       try {} catch {
Tests ran:
    io：原子写与损坏容错 损坏 JSON 备份为 .bak 并返回 fallback（store 级验证）
    io：原子写与损坏容错 secrets.json 损坏同样容错且重建后权限仍为 0600（POSIX）


[Survived] StringLiteral
lib/store/io.ts:54:17
-         fs.rmSync(`${file}.bak`, { force: true });
+         fs.rmSync(``, { force: true });
Tests ran:
    io：原子写与损坏容错 损坏 JSON 备份为 .bak 并返回 fallback（store 级验证）
    io：原子写与损坏容错 secrets.json 损坏同样容错且重建后权限仍为 0600（POSIX）


[Survived] ObjectLiteral
lib/store/io.ts:54:32
-         fs.rmSync(`${file}.bak`, { force: true });
+         fs.rmSync(`${file}.bak`, {});
Tests ran:
    io：原子写与损坏容错 损坏 JSON 备份为 .bak 并返回 fallback（store 级验证）
    io：原子写与损坏容错 secrets.json 损坏同样容错且重建后权限仍为 0600（POSIX）


[Survived] BooleanLiteral
lib/store/io.ts:54:41
-         fs.rmSync(`${file}.bak`, { force: true });
+         fs.rmSync(`${file}.bak`, { force: false });
Tests ran:
    io：原子写与损坏容错 损坏 JSON 备份为 .bak 并返回 fallback（store 级验证）
    io：原子写与损坏容错 secrets.json 损坏同样容错且重建后权限仍为 0600（POSIX）


[Survived] ConditionalExpression
lib/store/io.ts:49:7
-     if (!fs.existsSync(file)) return fallback;
+     if (true) return fallback;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    fenced 校验 loopback Origin 同端口放行
    fenced 校验 无 Origin/Referer（本地脚本）放行
  and 52 more tests!


[Survived] StringLiteral
lib/store/io.ts:55:27
-         fs.renameSync(file, `${file}.bak`);
+         fs.renameSync(file, ``);
Tests ran:
    io：原子写与损坏容错 损坏 JSON 备份为 .bak 并返回 fallback（store 级验证）
    io：原子写与损坏容错 secrets.json 损坏同样容错且重建后权限仍为 0600（POSIX）


[Survived] ConditionalExpression
lib/store/io.ts:49:7
-     if (!fs.existsSync(file)) return fallback;
+     if (false) return fallback;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    fenced 校验 loopback Origin 同端口放行
    fenced 校验 无 Origin/Referer（本地脚本）放行
  and 52 more tests!


[Survived] BlockStatement
lib/store/io.ts:50:7
-     try {
-       return JSON.parse(fs.readFileSync(file, 'utf8')) as T;
-     } catch {
+     try {} catch {
Tests ran:
    fenced 校验 loopback Origin 同端口放行
    fenced 校验 无 Origin/Referer（本地脚本）放行
    handleDbToolRequest 直调（prefix 挂载形态 + trust） loopback 同端口 + prefix 路径 /dsh-db-tool/api/state → 200
  and 46 more tests!


[Survived] Regex
lib/store/normalize.ts:19:21
-     key = key.replace(/^([A-Z]):/, (_m, drive: string) => `${drive.toLowerCase()}:`);
+     key = key.replace(/([A-Z]):/, (_m, drive: string) => `${drive.toLowerCase()}:`);
Ran all tests for this mutant.

[Survived] ConditionalExpression
lib/store/normalize.ts:20:7
-     if (key.length > 1) key = key.replace(/\/+$/, '');
+     if (true) key = key.replace(/\/+$/, '');
Ran all tests for this mutant.

[Survived] ConditionalExpression
lib/store/normalize.ts:20:7
-     if (key.length > 1) key = key.replace(/\/+$/, '');
+     if (false) key = key.replace(/\/+$/, '');
Ran all tests for this mutant.

[Survived] EqualityOperator
lib/store/normalize.ts:20:7
-     if (key.length > 1) key = key.replace(/\/+$/, '');
+     if (key.length >= 1) key = key.replace(/\/+$/, '');
Ran all tests for this mutant.

[Survived] EqualityOperator
lib/store/normalize.ts:20:7
-     if (key.length > 1) key = key.replace(/\/+$/, '');
+     if (key.length <= 1) key = key.replace(/\/+$/, '');
Ran all tests for this mutant.

[Survived] Regex
lib/store/normalize.ts:20:41
-     if (key.length > 1) key = key.replace(/\/+$/, '');
+     if (key.length > 1) key = key.replace(/\/$/, '');
Ran all tests for this mutant.

[Survived] StringLiteral
lib/store/normalize.ts:20:49
-     if (key.length > 1) key = key.replace(/\/+$/, '');
+     if (key.length > 1) key = key.replace(/\/+$/, "Stryker was here!");
Ran all tests for this mutant.

[Survived] BlockStatement
lib/store/secrets.ts:28:28
-     constructor(dir: string) {
-       this.file = path.join(dir, 'secrets.json');
-     }
+     constructor(dir: string) {}
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 44 more tests!


[Survived] BlockStatement
lib/store/secrets.ts:32:31
-     private load(): SecretsFile {
-       return readJson<SecretsFile>(this.file, { secrets: {} });
-     }
+     private load(): SecretsFile {}
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
  and 32 more tests!


[Survived] StringLiteral
lib/store/secrets.ts:29:32
-       this.file = path.join(dir, 'secrets.json');
+       this.file = path.join(dir, "");
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 44 more tests!


[Survived] ObjectLiteral
lib/store/secrets.ts:33:45
-       return readJson<SecretsFile>(this.file, { secrets: {} });
+       return readJson<SecretsFile>(this.file, {});
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
  and 32 more tests!


[Survived] BlockStatement
lib/store/secrets.ts:36:41
-     private save(data: SecretsFile): void {
-       writeJsonAtomic(this.file, data, FILE_MODE);
-     }
+     private save(data: SecretsFile): void {}
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 31 more tests!


[Survived] CallExpression
lib/store/secrets.ts:37:5
-       writeJsonAtomic(this.file, data, FILE_MODE);
+       ;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 31 more tests!


[Survived] BlockStatement
lib/store/secrets.ts:41:48
-     get(connId: string): SecretEntry | undefined {
-       const entry = this.load().secrets[connId];
-       return entry ? { ...entry } : undefined;
-     }
+     get(connId: string): SecretEntry | undefined {}
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
    DbToolService: 授权与只读 rw 连接 mode=rw 透传，DML 直接执行
  and 18 more tests!


[Survived] ObjectLiteral
lib/store/secrets.ts:43:20
-       return entry ? { ...entry } : undefined;
+       return entry ? {} : undefined;
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
    DbToolService: 授权与只读 rw 连接 mode=rw 透传，DML 直接执行
  and 17 more tests!


[Survived] ObjectLiteral
lib/store/secrets.ts:49:28
-       data.secrets[connId] = { ...(data.secrets[connId] ?? {}), ...patch };
+       data.secrets[connId] = {};
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 31 more tests!


[Survived] BlockStatement
lib/store/secrets.ts:47:49
-     set(connId: string, patch: SecretEntry): void {
-       const data = this.load();
-       data.secrets[connId] = { ...(data.secrets[connId] ?? {}), ...patch };
-       this.save(data);
-     }
+     set(connId: string, patch: SecretEntry): void {}
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 31 more tests!


[Survived] LogicalOperator
lib/store/secrets.ts:49:34
-       data.secrets[connId] = { ...(data.secrets[connId] ?? {}), ...patch };
+       data.secrets[connId] = { ...(data.secrets[connId] && {}), ...patch };
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 31 more tests!


[Survived] CallExpression
lib/store/secrets.ts:50:5
-       this.save(data);
+       ;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
  and 31 more tests!


[Survived] BlockStatement
lib/store/secrets.ts:54:32
-     delete(connId: string): void {
-       const data = this.load();
-       if (!(connId in data.secrets)) return;
-       delete data.secrets[connId];
-       this.save(data);
-     }
+     delete(connId: string): void {}
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
    ConnectionStore remove 级联删除 secrets 与全部项目的 grants
  and 1 more test!


[Survived] BooleanLiteral
lib/store/secrets.ts:56:9
-       if (!(connId in data.secrets)) return;
+       if (connId in data.secrets) return;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
    ConnectionStore remove 级联删除 secrets 与全部项目的 grants
  and 1 more test!


[Survived] ConditionalExpression
lib/store/secrets.ts:56:9
-       if (!(connId in data.secrets)) return;
+       if (true) return;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
    ConnectionStore remove 级联删除 secrets 与全部项目的 grants
  and 1 more test!


[Survived] ConditionalExpression
lib/store/secrets.ts:56:9
-       if (!(connId in data.secrets)) return;
+       if (false) return;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
    ConnectionStore remove 级联删除 secrets 与全部项目的 grants
  and 1 more test!


[Survived] CallExpression
lib/store/secrets.ts:58:5
-       this.save(data);
+       ;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND
    ConnectionStore remove 级联删除 secrets 与全部项目的 grants
  and 1 more test!


[NoCoverage] StringLiteral
lib/manager.ts:71:48
-     const s = typeof v === 'string' ? v.trim() : '';
+     const s = typeof v === 'string' ? v.trim() : "Stryker was here!";

[NoCoverage] StringLiteral
lib/manager.ts:72:39
-     if (s === '') throw new DbToolError('INVALID_ARGUMENT', `缺少必填参数: ${label}`);
+     if (s === '') throw new DbToolError("", `缺少必填参数: ${label}`);

[NoCoverage] StringLiteral
lib/manager.ts:72:59
-     if (s === '') throw new DbToolError('INVALID_ARGUMENT', `缺少必填参数: ${label}`);
+     if (s === '') throw new DbToolError('INVALID_ARGUMENT', ``);

[NoCoverage] StringLiteral
lib/manager.ts:103:38
-       if (!meta) throw new DbToolError('NOT_FOUND', `连接不存在: ${id}`);
+       if (!meta) throw new DbToolError("", `连接不存在: ${id}`);

[NoCoverage] StringLiteral
lib/manager.ts:103:51
-       if (!meta) throw new DbToolError('NOT_FOUND', `连接不存在: ${id}`);
+       if (!meta) throw new DbToolError('NOT_FOUND', ``);

[NoCoverage] BlockStatement
lib/manager.ts:116:64
-     async testConnection(id: string): Promise<TestConnectResult> {
-       const rc = this.requireConn(id);
-       try {
-         const factory = await this.resolver(rc.meta.kind);
-         const adapter = await factory(rc);
-         try {
-           return await adapter.testConnect();
-         } finally {
-           await adapter.close().catch(() => {});
-         }
-       } catch (e) {
-         throw this.toDriverError(e);
-       }
-     }
+     async testConnection(id: string): Promise<TestConnectResult> {}

[NoCoverage] BlockStatement
lib/manager.ts:118:9
-       try {
-         const factory = await this.resolver(rc.meta.kind);
-         const adapter = await factory(rc);
-         try {
-           return await adapter.testConnect();
-         } finally {
-           await adapter.close().catch(() => {});
-         }
-       } catch (e) {
+       try {} catch (e) {

[NoCoverage] BlockStatement
lib/manager.ts:121:11
-         try {
-           return await adapter.testConnect();
-         } finally {
+         try {} finally {

[NoCoverage] BlockStatement
lib/manager.ts:123:17
-         } finally {
-           await adapter.close().catch(() => {});
-         }
+         } finally {}

[NoCoverage] BlockStatement
lib/manager.ts:126:17
-       } catch (e) {
-         throw this.toDriverError(e);
-       }
+       } catch (e) {}

[NoCoverage] BlockStatement
lib/manager.ts:157:17
-       } catch (e) {
-         this.audit(key, connId, 'databases', 'listDatabases', 'none', false, false, this.errText(e));
-         throw this.toDriverError(e);
-       }
+       } catch (e) {}

[NoCoverage] StringLiteral
lib/manager.ts:158:31
-         this.audit(key, connId, 'databases', 'listDatabases', 'none', false, false, this.errText(e));
+         this.audit(key, connId, "", 'listDatabases', 'none', false, false, this.errText(e));

[NoCoverage] StringLiteral
lib/manager.ts:158:44
-         this.audit(key, connId, 'databases', 'listDatabases', 'none', false, false, this.errText(e));
+         this.audit(key, connId, 'databases', "", 'none', false, false, this.errText(e));

[NoCoverage] StringLiteral
lib/manager.ts:158:61
-         this.audit(key, connId, 'databases', 'listDatabases', 'none', false, false, this.errText(e));
+         this.audit(key, connId, 'databases', 'listDatabases', "", false, false, this.errText(e));

[NoCoverage] BooleanLiteral
lib/manager.ts:158:69
-         this.audit(key, connId, 'databases', 'listDatabases', 'none', false, false, this.errText(e));
+         this.audit(key, connId, 'databases', 'listDatabases', 'none', true, false, this.errText(e));

[NoCoverage] BooleanLiteral
lib/manager.ts:158:76
-         this.audit(key, connId, 'databases', 'listDatabases', 'none', false, false, this.errText(e));
+         this.audit(key, connId, 'databases', 'listDatabases', 'none', false, true, this.errText(e));

[NoCoverage] BlockStatement
lib/manager.ts:169:17
-       } catch (e) {
-         this.audit(key, connId, 'tables', database ?? 'default', 'none', false, false, this.errText(e));
-         throw this.toDriverError(e);
-       }
+       } catch (e) {}

[NoCoverage] StringLiteral
lib/manager.ts:170:31
-         this.audit(key, connId, 'tables', database ?? 'default', 'none', false, false, this.errText(e));
+         this.audit(key, connId, "", database ?? 'default', 'none', false, false, this.errText(e));

[NoCoverage] LogicalOperator
lib/manager.ts:170:41
-         this.audit(key, connId, 'tables', database ?? 'default', 'none', false, false, this.errText(e));
+         this.audit(key, connId, 'tables', database && 'default', 'none', false, false, this.errText(e));

[NoCoverage] StringLiteral
lib/manager.ts:170:53
-         this.audit(key, connId, 'tables', database ?? 'default', 'none', false, false, this.errText(e));
+         this.audit(key, connId, 'tables', database ?? "", 'none', false, false, this.errText(e));

[NoCoverage] StringLiteral
lib/manager.ts:170:64
-         this.audit(key, connId, 'tables', database ?? 'default', 'none', false, false, this.errText(e));
+         this.audit(key, connId, 'tables', database ?? 'default', "", false, false, this.errText(e));

[NoCoverage] BooleanLiteral
lib/manager.ts:170:72
-         this.audit(key, connId, 'tables', database ?? 'default', 'none', false, false, this.errText(e));
+         this.audit(key, connId, 'tables', database ?? 'default', 'none', true, false, this.errText(e));

[NoCoverage] BooleanLiteral
lib/manager.ts:170:79
-         this.audit(key, connId, 'tables', database ?? 'default', 'none', false, false, this.errText(e));
+         this.audit(key, connId, 'tables', database ?? 'default', 'none', false, true, this.errText(e));

[NoCoverage] BlockStatement
lib/manager.ts:182:17
-       } catch (e) {
-         this.audit(key, connId, 'schema', t, 'none', false, false, this.errText(e));
-         throw this.toDriverError(e);
-       }
+       } catch (e) {}

[NoCoverage] StringLiteral
lib/manager.ts:183:31
-         this.audit(key, connId, 'schema', t, 'none', false, false, this.errText(e));
+         this.audit(key, connId, "", t, 'none', false, false, this.errText(e));

[NoCoverage] StringLiteral
lib/manager.ts:183:44
-         this.audit(key, connId, 'schema', t, 'none', false, false, this.errText(e));
+         this.audit(key, connId, 'schema', t, "", false, false, this.errText(e));

[NoCoverage] BooleanLiteral
lib/manager.ts:183:52
-         this.audit(key, connId, 'schema', t, 'none', false, false, this.errText(e));
+         this.audit(key, connId, 'schema', t, 'none', true, false, this.errText(e));

[NoCoverage] BooleanLiteral
lib/manager.ts:183:59
-         this.audit(key, connId, 'schema', t, 'none', false, false, this.errText(e));
+         this.audit(key, connId, 'schema', t, 'none', false, true, this.errText(e));

[NoCoverage] BlockStatement
lib/manager.ts:202:17
-       } catch (e) {
-         this.audit(key, connId, 'preview', `PREVIEW ${t} LIMIT ${capped}`, 'none', false, false, this.errText(e));
-         throw this.toDriverError(e);
-       }
+       } catch (e) {}

[NoCoverage] StringLiteral
lib/manager.ts:203:31
-         this.audit(key, connId, 'preview', `PREVIEW ${t} LIMIT ${capped}`, 'none', false, false, this.errText(e));
+         this.audit(key, connId, "", `PREVIEW ${t} LIMIT ${capped}`, 'none', false, false, this.errText(e));

[NoCoverage] StringLiteral
lib/manager.ts:203:42
-         this.audit(key, connId, 'preview', `PREVIEW ${t} LIMIT ${capped}`, 'none', false, false, this.errText(e));
+         this.audit(key, connId, 'preview', ``, 'none', false, false, this.errText(e));

[NoCoverage] StringLiteral
lib/manager.ts:203:74
-         this.audit(key, connId, 'preview', `PREVIEW ${t} LIMIT ${capped}`, 'none', false, false, this.errText(e));
+         this.audit(key, connId, 'preview', `PREVIEW ${t} LIMIT ${capped}`, "", false, false, this.errText(e));

[NoCoverage] BooleanLiteral
lib/manager.ts:203:82
-         this.audit(key, connId, 'preview', `PREVIEW ${t} LIMIT ${capped}`, 'none', false, false, this.errText(e));
+         this.audit(key, connId, 'preview', `PREVIEW ${t} LIMIT ${capped}`, 'none', true, false, this.errText(e));

[NoCoverage] BooleanLiteral
lib/manager.ts:203:89
-         this.audit(key, connId, 'preview', `PREVIEW ${t} LIMIT ${capped}`, 'none', false, false, this.errText(e));
+         this.audit(key, connId, 'preview', `PREVIEW ${t} LIMIT ${capped}`, 'none', false, true, this.errText(e));

[NoCoverage] BlockStatement
lib/manager.ts:273:24
-       if (mode !== 'rw') {
-         db.execute = async () => {
-           throw new DbToolError('READ_ONLY', '该连接对本项目为只读授权（ro），脚本不可执行写操作');
-         };
-       }
+       if (mode !== 'rw') {}

[NoCoverage] BlockStatement
lib/manager.ts:274:32
-         db.execute = async () => {
-           throw new DbToolError('READ_ONLY', '该连接对本项目为只读授权（ro），脚本不可执行写操作');
-         };
+         db.execute = async () => {};

[NoCoverage] StringLiteral
lib/manager.ts:275:31
-           throw new DbToolError('READ_ONLY', '该连接对本项目为只读授权（ro），脚本不可执行写操作');
+           throw new DbToolError("", '该连接对本项目为只读授权（ro），脚本不可执行写操作');

[NoCoverage] StringLiteral
lib/manager.ts:275:44
-           throw new DbToolError('READ_ONLY', '该连接对本项目为只读授权（ro），脚本不可执行写操作');
+           throw new DbToolError('READ_ONLY', "");

[NoCoverage] MethodExpression
lib/manager.ts:279:58
-       this.audit(key, connId, 'script', src.length > 200 ? src.slice(0, 200) + '…' : src, 'none', false, true);
+       this.audit(key, connId, 'script', src.length > 200 ? src + '…' : src, 'none', false, true);

[NoCoverage] StringLiteral
lib/manager.ts:279:78
-       this.audit(key, connId, 'script', src.length > 200 ? src.slice(0, 200) + '…' : src, 'none', false, true);
+       this.audit(key, connId, 'script', src.length > 200 ? src.slice(0, 200) + "" : src, 'none', false, true);

[NoCoverage] StringLiteral
lib/manager.ts:282:56
-         console: { log: (...a: unknown[]) => console.log('[db-script]', ...a), error: (...a: unknown[]) => console.error('[db-script]', ...a) },
+         console: { log: (...a: unknown[]) => console.log("", ...a), error: (...a: unknown[]) => console.error('[db-script]', ...a) },

[NoCoverage] StringLiteral
lib/manager.ts:282:120
-         console: { log: (...a: unknown[]) => console.log('[db-script]', ...a), error: (...a: unknown[]) => console.error('[db-script]', ...a) },
+         console: { log: (...a: unknown[]) => console.log('[db-script]', ...a), error: (...a: unknown[]) => console.error("", ...a) },

[NoCoverage] StringLiteral
lib/manager.ts:292:58
-             const t = setTimeout(() => rej(new DbToolError('SCRIPT_TIMEOUT', '脚本执行超时（60s）')), 60_000);
+             const t = setTimeout(() => rej(new DbToolError("", '脚本执行超时（60s）')), 60_000);

[NoCoverage] StringLiteral
lib/manager.ts:292:76
-             const t = setTimeout(() => rej(new DbToolError('SCRIPT_TIMEOUT', '脚本执行超时（60s）')), 60_000);
+             const t = setTimeout(() => rej(new DbToolError('SCRIPT_TIMEOUT', "")), 60_000);

[NoCoverage] BlockStatement
lib/manager.ts:345:13
-       } catch {
-         throw new DbToolError('NOT_FOUND', `连接不存在: ${connId}`);
-       }
+       } catch {}

[NoCoverage] StringLiteral
lib/manager.ts:346:29
-         throw new DbToolError('NOT_FOUND', `连接不存在: ${connId}`);
+         throw new DbToolError("", `连接不存在: ${connId}`);

[NoCoverage] StringLiteral
lib/manager.ts:346:42
-         throw new DbToolError('NOT_FOUND', `连接不存在: ${connId}`);
+         throw new DbToolError('NOT_FOUND', ``);

[NoCoverage] StringLiteral
lib/manager.ts:356:46
-       if (this.disposed) throw new DbToolError('INVALID_ARGUMENT', '服务已关闭');
+       if (this.disposed) throw new DbToolError("", '服务已关闭');

[NoCoverage] StringLiteral
lib/manager.ts:356:66
-       if (this.disposed) throw new DbToolError('INVALID_ARGUMENT', '服务已关闭');
+       if (this.disposed) throw new DbToolError('INVALID_ARGUMENT', "");

[NoCoverage] BlockStatement
lib/manager.ts:377:25
-         })().catch((e) => {
-           this.adapterCache.delete(connId);
-           throw e instanceof DbToolError ? e : this.toDriverError(e);
-         });
+         })().catch((e) => {});

[NoCoverage] CallExpression
lib/manager.ts:378:9
-           this.adapterCache.delete(connId);
+           ;

[NoCoverage] StringLiteral
lib/manager.ts:405:115
-           return { needConfirmation: true, challengeId: cid, statement, danger: 'danger', reason: verdict.reason ?? '危险操作，需要用户确认' };
+           return { needConfirmation: true, challengeId: cid, statement, danger: 'danger', reason: verdict.reason ?? "" };

[NoCoverage] BlockStatement
lib/manager.ts:417:17
-       } catch (e) {
-         this.audit(key, connId, op, statement, verdict.level as DangerLevel, verdict.level === 'danger', false, this.errText(e));
-         throw this.toDriverError(e);
-       }
+       } catch (e) {}

[NoCoverage] CallExpression
lib/manager.ts:418:7
-         this.audit(key, connId, op, statement, verdict.level as DangerLevel, verdict.level === 'danger', false, this.errText(e));
+         ;

[NoCoverage] ConditionalExpression
lib/manager.ts:418:76
-         this.audit(key, connId, op, statement, verdict.level as DangerLevel, verdict.level === 'danger', false, this.errText(e));
+         this.audit(key, connId, op, statement, verdict.level as DangerLevel, true, false, this.errText(e));

[NoCoverage] ConditionalExpression
lib/manager.ts:418:76
-         this.audit(key, connId, op, statement, verdict.level as DangerLevel, verdict.level === 'danger', false, this.errText(e));
+         this.audit(key, connId, op, statement, verdict.level as DangerLevel, false, false, this.errText(e));

[NoCoverage] EqualityOperator
lib/manager.ts:418:76
-         this.audit(key, connId, op, statement, verdict.level as DangerLevel, verdict.level === 'danger', false, this.errText(e));
+         this.audit(key, connId, op, statement, verdict.level as DangerLevel, verdict.level !== 'danger', false, this.errText(e));

[NoCoverage] StringLiteral
lib/manager.ts:418:94
-         this.audit(key, connId, op, statement, verdict.level as DangerLevel, verdict.level === 'danger', false, this.errText(e));
+         this.audit(key, connId, op, statement, verdict.level as DangerLevel, verdict.level === "", false, this.errText(e));

[NoCoverage] BooleanLiteral
lib/manager.ts:418:104
-         this.audit(key, connId, op, statement, verdict.level as DangerLevel, verdict.level === 'danger', false, this.errText(e));
+         this.audit(key, connId, op, statement, verdict.level as DangerLevel, verdict.level === 'danger', true, this.errText(e));

[NoCoverage] ConditionalExpression
lib/manager.ts:518:7
-         case 'schema': {
-           const conn = requireConn(connId);
-           if (args.table) return j(await service.schema(projectPath, conn, args.table, args.database));
-           if (args.database) return j(await service.tables(projectPath, conn, args.database));
-           return j(await service.databases(projectPath, conn));
-         }
+         case 'schema':

[NoCoverage] BlockStatement
lib/manager.ts:518:22
-         case 'schema': {
-           const conn = requireConn(connId);
-           if (args.table) return j(await service.schema(projectPath, conn, args.table, args.database));
-           if (args.database) return j(await service.tables(projectPath, conn, args.database));
-           return j(await service.databases(projectPath, conn));
-         }
+         case 'schema': {}

[NoCoverage] ConditionalExpression
lib/manager.ts:520:13
-           if (args.table) return j(await service.schema(projectPath, conn, args.table, args.database));
+           if (true) return j(await service.schema(projectPath, conn, args.table, args.database));

[NoCoverage] ConditionalExpression
lib/manager.ts:520:13
-           if (args.table) return j(await service.schema(projectPath, conn, args.table, args.database));
+           if (false) return j(await service.schema(projectPath, conn, args.table, args.database));

[NoCoverage] ConditionalExpression
lib/manager.ts:521:13
-           if (args.database) return j(await service.tables(projectPath, conn, args.database));
+           if (true) return j(await service.tables(projectPath, conn, args.database));

[NoCoverage] ConditionalExpression
lib/manager.ts:521:13
-           if (args.database) return j(await service.tables(projectPath, conn, args.database));
+           if (false) return j(await service.tables(projectPath, conn, args.database));

[NoCoverage] ConditionalExpression
lib/manager.ts:525:7
-         case 'preview': {
-           const r = await service.preview(projectPath, requireConn(connId), assertArg(args.table, 'table'), args.limit, args.database);
-           return j(r);
-         }
+         case 'preview':

[NoCoverage] BlockStatement
lib/manager.ts:525:23
-         case 'preview': {
-           const r = await service.preview(projectPath, requireConn(connId), assertArg(args.table, 'table'), args.limit, args.database);
-           return j(r);
-         }
+         case 'preview': {}

[NoCoverage] StringLiteral
lib/manager.ts:526:97
-           const r = await service.preview(projectPath, requireConn(connId), assertArg(args.table, 'table'), args.limit, args.database);
+           const r = await service.preview(projectPath, requireConn(connId), assertArg(args.table, ""), args.limit, args.database);

[NoCoverage] StringLiteral
lib/manager.ts:539:54
-       const code = e instanceof DbToolError ? e.code : 'DRIVER_ERROR';
+       const code = e instanceof DbToolError ? e.code : "";

[NoCoverage] StringLiteral
lib/manager.ts:550:52
-     if (!v || v.trim() === '') throw new DbToolError('INVALID_ARGUMENT', `缺少必填参数: ${label}`);
+     if (!v || v.trim() === '') throw new DbToolError("", `缺少必填参数: ${label}`);

[NoCoverage] StringLiteral
lib/manager.ts:550:72
-     if (!v || v.trim() === '') throw new DbToolError('INVALID_ARGUMENT', `缺少必填参数: ${label}`);
+     if (!v || v.trim() === '') throw new DbToolError('INVALID_ARGUMENT', ``);

[Survived] StringLiteral
lib/manager.ts:51:17
-       this.name = 'DbToolError';
+       this.name = "";
Tests ran:
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
  and 17 more tests!


[Survived] BlockStatement
lib/manager.ts:70:71
-   function assertNonEmpty(v: string | undefined, label: string): string {
-     const s = typeof v === 'string' ? v.trim() : '';
-     if (s === '') throw new DbToolError('INVALID_ARGUMENT', `缺少必填参数: ${label}`);
-     return s;
-   }
+   function assertNonEmpty(v: string | undefined, label: string): string {}
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
  and 22 more tests!


[Survived] ConditionalExpression
lib/manager.ts:71:13
-     const s = typeof v === 'string' ? v.trim() : '';
+     const s = true ? v.trim() : '';
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
  and 22 more tests!


[Survived] ConditionalExpression
lib/manager.ts:71:13
-     const s = typeof v === 'string' ? v.trim() : '';
+     const s = false ? v.trim() : '';
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
  and 22 more tests!


[Survived] EqualityOperator
lib/manager.ts:71:13
-     const s = typeof v === 'string' ? v.trim() : '';
+     const s = typeof v !== 'string' ? v.trim() : '';
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
  and 22 more tests!


[Survived] StringLiteral
lib/manager.ts:71:26
-     const s = typeof v === 'string' ? v.trim() : '';
+     const s = typeof v === "" ? v.trim() : '';
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
  and 22 more tests!


[Survived] MethodExpression
lib/manager.ts:71:37
-     const s = typeof v === 'string' ? v.trim() : '';
+     const s = typeof v === 'string' ? v : '';
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
  and 22 more tests!


[Survived] ConditionalExpression
lib/manager.ts:72:7
-     if (s === '') throw new DbToolError('INVALID_ARGUMENT', `缺少必填参数: ${label}`);
+     if (true) throw new DbToolError('INVALID_ARGUMENT', `缺少必填参数: ${label}`);
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
  and 22 more tests!


[Survived] ConditionalExpression
lib/manager.ts:72:7
-     if (s === '') throw new DbToolError('INVALID_ARGUMENT', `缺少必填参数: ${label}`);
+     if (false) throw new DbToolError('INVALID_ARGUMENT', `缺少必填参数: ${label}`);
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
  and 22 more tests!


[Survived] EqualityOperator
lib/manager.ts:72:7
-     if (s === '') throw new DbToolError('INVALID_ARGUMENT', `缺少必填参数: ${label}`);
+     if (s !== '') throw new DbToolError('INVALID_ARGUMENT', `缺少必填参数: ${label}`);
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
  and 22 more tests!


[Survived] StringLiteral
lib/manager.ts:72:13
-     if (s === '') throw new DbToolError('INVALID_ARGUMENT', `缺少必填参数: ${label}`);
+     if (s === "Stryker was here!") throw new DbToolError('INVALID_ARGUMENT', `缺少必填参数: ${label}`);
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
  and 22 more tests!


[Survived] BooleanLiteral
lib/manager.ts:82:22
-     private disposed = false;
+     private disposed = true;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 21 more tests!


[Survived] BlockStatement
lib/manager.ts:84:73
-     constructor(readonly store: DbToolStore, opts?: DbToolServiceOptions) {
-       this.resolver = opts?.adapterResolver ?? getAdapter;
-       this.challenges = opts?.challenges ?? new ChallengeStore();
-     }
+     constructor(readonly store: DbToolStore, opts?: DbToolServiceOptions) {}
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 21 more tests!


[Survived] LogicalOperator
lib/manager.ts:85:21
-       this.resolver = opts?.adapterResolver ?? getAdapter;
+       this.resolver = opts?.adapterResolver && getAdapter;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 21 more tests!


[Survived] OptionalChaining
lib/manager.ts:85:21
-       this.resolver = opts?.adapterResolver ?? getAdapter;
+       this.resolver = opts.adapterResolver ?? getAdapter;
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 21 more tests!


[Survived] LogicalOperator
lib/manager.ts:86:23
-       this.challenges = opts?.challenges ?? new ChallengeStore();
+       this.challenges = opts?.challenges && new ChallengeStore();
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 21 more tests!


[Survived] BlockStatement
lib/manager.ts:95:96
-     createConnection(input: Parameters<DbToolStore['connections']['create']>[0]): ConnectionMeta {
-       const meta = this.store.connections.create(input);
-       this.audit('', input.id, 'create_connection', input.id, 'none', false, true);
-       return meta;
-     }
+     createConnection(input: Parameters<DbToolStore['connections']['create']>[0]): ConnectionMeta {}
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除


[Survived] OptionalChaining
lib/manager.ts:86:23
-       this.challenges = opts?.challenges ?? new ChallengeStore();
+       this.challenges = opts.challenges ?? new ChallengeStore();
Tests ran:
    fenced 校验 跨站 Origin → 403 FORBIDDEN
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 21 more tests!


[Survived] StringLiteral
lib/manager.ts:97:16
-       this.audit('', input.id, 'create_connection', input.id, 'none', false, true);
+       this.audit("Stryker was here!", input.id, 'create_connection', input.id, 'none', false, true);
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除


[Survived] BlockStatement
lib/manager.ts:91:39
-     listConnections(): ConnectionMeta[] {
-       return this.store.connections.list();
-     }
+     listConnections(): ConnectionMeta[] {}
Tests ran:
    fenced 校验 loopback Origin 同端口放行
    fenced 校验 无 Origin/Referer（本地脚本）放行
    handleDbToolRequest 直调（prefix 挂载形态 + trust） loopback 同端口 + prefix 路径 /dsh-db-tool/api/state → 200
  and 6 more tests!


[Survived] StringLiteral
lib/manager.ts:97:30
-       this.audit('', input.id, 'create_connection', input.id, 'none', false, true);
+       this.audit('', input.id, "", input.id, 'none', false, true);
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除


[Survived] StringLiteral
lib/manager.ts:97:61
-       this.audit('', input.id, 'create_connection', input.id, 'none', false, true);
+       this.audit('', input.id, 'create_connection', input.id, "", false, true);
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除


[Survived] BooleanLiteral
lib/manager.ts:97:69
-       this.audit('', input.id, 'create_connection', input.id, 'none', false, true);
+       this.audit('', input.id, 'create_connection', input.id, 'none', true, true);
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除


[Survived] BooleanLiteral
lib/manager.ts:97:76
-       this.audit('', input.id, 'create_connection', input.id, 'none', false, true);
+       this.audit('', input.id, 'create_connection', input.id, 'none', false, false);
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除


[Survived] BlockStatement
lib/manager.ts:101:108
-     updateConnection(id: string, patch: Parameters<DbToolStore['connections']['update']>[1]): ConnectionMeta {
-       const meta = this.store.connections.update(id, patch);
-       if (!meta) throw new DbToolError('NOT_FOUND', `连接不存在: ${id}`);
-       this.audit('', id, 'update_connection', id, 'none', false, true);
-       return meta;
-     }
+     updateConnection(id: string, patch: Parameters<DbToolStore['connections']['update']>[1]): ConnectionMeta {}
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除


[Survived] BooleanLiteral
lib/manager.ts:103:9
-       if (!meta) throw new DbToolError('NOT_FOUND', `连接不存在: ${id}`);
+       if (meta) throw new DbToolError('NOT_FOUND', `连接不存在: ${id}`);
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除


[Survived] ConditionalExpression
lib/manager.ts:103:9
-       if (!meta) throw new DbToolError('NOT_FOUND', `连接不存在: ${id}`);
+       if (true) throw new DbToolError('NOT_FOUND', `连接不存在: ${id}`);
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除


[Survived] ConditionalExpression
lib/manager.ts:103:9
-       if (!meta) throw new DbToolError('NOT_FOUND', `连接不存在: ${id}`);
+       if (false) throw new DbToolError('NOT_FOUND', `连接不存在: ${id}`);
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除


[Survived] StringLiteral
lib/manager.ts:104:16
-       this.audit('', id, 'update_connection', id, 'none', false, true);
+       this.audit("Stryker was here!", id, 'update_connection', id, 'none', false, true);
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除


[Survived] StringLiteral
lib/manager.ts:104:24
-       this.audit('', id, 'update_connection', id, 'none', false, true);
+       this.audit('', id, "", id, 'none', false, true);
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除


[Survived] StringLiteral
lib/manager.ts:104:49
-       this.audit('', id, 'update_connection', id, 'none', false, true);
+       this.audit('', id, 'update_connection', id, "", false, true);
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除


[Survived] BooleanLiteral
lib/manager.ts:104:57
-       this.audit('', id, 'update_connection', id, 'none', false, true);
+       this.audit('', id, 'update_connection', id, 'none', true, true);
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除


[Survived] BooleanLiteral
lib/manager.ts:104:64
-       this.audit('', id, 'update_connection', id, 'none', false, true);
+       this.audit('', id, 'update_connection', id, 'none', false, false);
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除


[Survived] BlockStatement
lib/manager.ts:108:41
-     removeConnection(id: string): boolean {
-       const ok = this.store.connections.remove(id);
-       if (!ok) throw new DbToolError('NOT_FOUND', `连接不存在: ${id}`);
-       this.adapterCache.delete(id);
-       this.audit('', id, 'remove_connection', id, 'none', false, true);
-       return ok;
-     }
+     removeConnection(id: string): boolean {}
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND


[Survived] BooleanLiteral
lib/manager.ts:110:9
-       if (!ok) throw new DbToolError('NOT_FOUND', `连接不存在: ${id}`);
+       if (ok) throw new DbToolError('NOT_FOUND', `连接不存在: ${id}`);
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND


[Survived] ConditionalExpression
lib/manager.ts:110:9
-       if (!ok) throw new DbToolError('NOT_FOUND', `连接不存在: ${id}`);
+       if (true) throw new DbToolError('NOT_FOUND', `连接不存在: ${id}`);
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND


[Survived] ConditionalExpression
lib/manager.ts:110:9
-       if (!ok) throw new DbToolError('NOT_FOUND', `连接不存在: ${id}`);
+       if (false) throw new DbToolError('NOT_FOUND', `连接不存在: ${id}`);
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND


[Survived] StringLiteral
lib/manager.ts:110:36
-       if (!ok) throw new DbToolError('NOT_FOUND', `连接不存在: ${id}`);
+       if (!ok) throw new DbToolError("", `连接不存在: ${id}`);
Tests ran:
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND


[Survived] StringLiteral
lib/manager.ts:110:49
-       if (!ok) throw new DbToolError('NOT_FOUND', `连接不存在: ${id}`);
+       if (!ok) throw new DbToolError('NOT_FOUND', ``);
Tests ran:
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND


[Survived] CallExpression
lib/manager.ts:111:5
-       this.adapterCache.delete(id);
+       ;
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND


[Survived] StringLiteral
lib/manager.ts:112:16
-       this.audit('', id, 'remove_connection', id, 'none', false, true);
+       this.audit("Stryker was here!", id, 'remove_connection', id, 'none', false, true);
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND


[Survived] StringLiteral
lib/manager.ts:112:24
-       this.audit('', id, 'remove_connection', id, 'none', false, true);
+       this.audit('', id, "", id, 'none', false, true);
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND


[Survived] StringLiteral
lib/manager.ts:112:49
-       this.audit('', id, 'remove_connection', id, 'none', false, true);
+       this.audit('', id, 'remove_connection', id, "", false, true);
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND


[Survived] BlockStatement
lib/manager.ts:133:75
-     grantsFor(projectPath: string): { connId: string; mode: 'ro' | 'rw' }[] {
-       return this.store.grants.grantsFor(normalizeProjectKey(projectPath));
-     }
+     grantsFor(projectPath: string): { connId: string; mode: 'ro' | 'rw' }[] {}
Tests ran:
    连接管理与授权路由 grants PUT/GET/DELETE
    业务路由与确认流程 GET /api/audit 与 /api/state


[Survived] BlockStatement
lib/manager.ts:137:71
-     grant(projectPath: string, connId: string, mode: 'ro' | 'rw'): void {
-       const key = normalizeProjectKey(projectPath);
-       this.store.grants.grant(key, connId, mode);
-       this.audit(key, connId, 'grant', `${connId} -> ${mode}`, 'none', false, true);
-     }
+     grant(projectPath: string, connId: string, mode: 'ro' | 'rw'): void {}
Tests ran:
    连接管理与授权路由 grants PUT/GET/DELETE


[Survived] BooleanLiteral
lib/manager.ts:112:57
-       this.audit('', id, 'remove_connection', id, 'none', false, true);
+       this.audit('', id, 'remove_connection', id, 'none', true, true);
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND


[Survived] BooleanLiteral
lib/manager.ts:112:64
-       this.audit('', id, 'remove_connection', id, 'none', false, true);
+       this.audit('', id, 'remove_connection', id, 'none', false, false);
Tests ran:
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    DbToolService: 连接管理与 secrets remove 级联；重复 remove → NOT_FOUND


[Survived] CallExpression
lib/manager.ts:139:5
-       this.store.grants.grant(key, connId, mode);
+       ;
Tests ran:
    连接管理与授权路由 grants PUT/GET/DELETE


[Survived] StringLiteral
lib/manager.ts:140:29
-       this.audit(key, connId, 'grant', `${connId} -> ${mode}`, 'none', false, true);
+       this.audit(key, connId, "", `${connId} -> ${mode}`, 'none', false, true);
Tests ran:
    连接管理与授权路由 grants PUT/GET/DELETE


[Survived] StringLiteral
lib/manager.ts:140:38
-       this.audit(key, connId, 'grant', `${connId} -> ${mode}`, 'none', false, true);
+       this.audit(key, connId, 'grant', ``, 'none', false, true);
Tests ran:
    连接管理与授权路由 grants PUT/GET/DELETE


[Survived] BooleanLiteral
lib/manager.ts:140:70
-       this.audit(key, connId, 'grant', `${connId} -> ${mode}`, 'none', false, true);
+       this.audit(key, connId, 'grant', `${connId} -> ${mode}`, 'none', true, true);
Tests ran:
    连接管理与授权路由 grants PUT/GET/DELETE


[Survived] StringLiteral
lib/manager.ts:140:62
-       this.audit(key, connId, 'grant', `${connId} -> ${mode}`, 'none', false, true);
+       this.audit(key, connId, 'grant', `${connId} -> ${mode}`, "", false, true);
Tests ran:
    连接管理与授权路由 grants PUT/GET/DELETE


[Survived] BooleanLiteral
lib/manager.ts:140:77
-       this.audit(key, connId, 'grant', `${connId} -> ${mode}`, 'none', false, true);
+       this.audit(key, connId, 'grant', `${connId} -> ${mode}`, 'none', false, false);
Tests ran:
    连接管理与授权路由 grants PUT/GET/DELETE


[Survived] BlockStatement
lib/manager.ts:143:58
-     revokeGrant(projectPath: string, connId: string): void {
-       const key = normalizeProjectKey(projectPath);
-       this.store.grants.revoke(key, connId);
-       this.audit(key, connId, 'revoke', connId, 'none', false, true);
-     }
+     revokeGrant(projectPath: string, connId: string): void {}
Tests ran:
    连接管理与授权路由 grants PUT/GET/DELETE


[Survived] CallExpression
lib/manager.ts:145:5
-       this.store.grants.revoke(key, connId);
+       ;
Tests ran:
    连接管理与授权路由 grants PUT/GET/DELETE


[Survived] StringLiteral
lib/manager.ts:146:29
-       this.audit(key, connId, 'revoke', connId, 'none', false, true);
+       this.audit(key, connId, "", connId, 'none', false, true);
Tests ran:
    连接管理与授权路由 grants PUT/GET/DELETE


[Survived] StringLiteral
lib/manager.ts:146:47
-       this.audit(key, connId, 'revoke', connId, 'none', false, true);
+       this.audit(key, connId, 'revoke', connId, "", false, true);
Tests ran:
    连接管理与授权路由 grants PUT/GET/DELETE


[Survived] BooleanLiteral
lib/manager.ts:146:55
-       this.audit(key, connId, 'revoke', connId, 'none', false, true);
+       this.audit(key, connId, 'revoke', connId, 'none', true, true);
Tests ran:
    连接管理与授权路由 grants PUT/GET/DELETE


[Survived] BooleanLiteral
lib/manager.ts:146:62
-       this.audit(key, connId, 'revoke', connId, 'none', false, true);
+       this.audit(key, connId, 'revoke', connId, 'none', false, false);
Tests ran:
    连接管理与授权路由 grants PUT/GET/DELETE


[Survived] BlockStatement
lib/manager.ts:151:87
-     async databases(projectPath: string | undefined, connId: string): Promise<string[]> {
-       const { key, adapter } = await this.authorize(projectPath, connId, false);
-       try {
-         const result = await adapter.listDatabases();
-         this.audit(key, connId, 'databases', 'listDatabases', 'none', false, true);
-         return result;
-       } catch (e) {
-         this.audit(key, connId, 'databases', 'listDatabases', 'none', false, false, this.errText(e));
-         throw this.toDriverError(e);
-       }
-     }
+     async databases(projectPath: string | undefined, connId: string): Promise<string[]> {}
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] BooleanLiteral
lib/manager.ts:152:72
-       const { key, adapter } = await this.authorize(projectPath, connId, false);
+       const { key, adapter } = await this.authorize(projectPath, connId, true);
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] BlockStatement
lib/manager.ts:153:9
-       try {
-         const result = await adapter.listDatabases();
-         this.audit(key, connId, 'databases', 'listDatabases', 'none', false, true);
-         return result;
-       } catch (e) {
+       try {} catch (e) {
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] StringLiteral
lib/manager.ts:155:31
-         this.audit(key, connId, 'databases', 'listDatabases', 'none', false, true);
+         this.audit(key, connId, "", 'listDatabases', 'none', false, true);
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] StringLiteral
lib/manager.ts:155:44
-         this.audit(key, connId, 'databases', 'listDatabases', 'none', false, true);
+         this.audit(key, connId, 'databases', "", 'none', false, true);
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] StringLiteral
lib/manager.ts:155:61
-         this.audit(key, connId, 'databases', 'listDatabases', 'none', false, true);
+         this.audit(key, connId, 'databases', 'listDatabases', "", false, true);
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] BooleanLiteral
lib/manager.ts:155:69
-         this.audit(key, connId, 'databases', 'listDatabases', 'none', false, true);
+         this.audit(key, connId, 'databases', 'listDatabases', 'none', true, true);
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] BooleanLiteral
lib/manager.ts:155:76
-         this.audit(key, connId, 'databases', 'listDatabases', 'none', false, true);
+         this.audit(key, connId, 'databases', 'listDatabases', 'none', false, false);
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] BlockStatement
lib/manager.ts:163:106
-     async tables(projectPath: string | undefined, connId: string, database?: string): Promise<TableInfo[]> {
-       const { key, adapter } = await this.authorize(projectPath, connId, false);
-       try {
-         const result = await adapter.listTables(database);
-         this.audit(key, connId, 'tables', database ?? 'default', 'none', false, true);
-         return result;
-       } catch (e) {
-         this.audit(key, connId, 'tables', database ?? 'default', 'none', false, false, this.errText(e));
-         throw this.toDriverError(e);
-       }
-     }
+     async tables(projectPath: string | undefined, connId: string, database?: string): Promise<TableInfo[]> {}
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] BooleanLiteral
lib/manager.ts:164:72
-       const { key, adapter } = await this.authorize(projectPath, connId, false);
+       const { key, adapter } = await this.authorize(projectPath, connId, true);
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] BlockStatement
lib/manager.ts:165:9
-       try {
-         const result = await adapter.listTables(database);
-         this.audit(key, connId, 'tables', database ?? 'default', 'none', false, true);
-         return result;
-       } catch (e) {
+       try {} catch (e) {
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] StringLiteral
lib/manager.ts:167:31
-         this.audit(key, connId, 'tables', database ?? 'default', 'none', false, true);
+         this.audit(key, connId, "", database ?? 'default', 'none', false, true);
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] LogicalOperator
lib/manager.ts:167:41
-         this.audit(key, connId, 'tables', database ?? 'default', 'none', false, true);
+         this.audit(key, connId, 'tables', database && 'default', 'none', false, true);
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] StringLiteral
lib/manager.ts:167:53
-         this.audit(key, connId, 'tables', database ?? 'default', 'none', false, true);
+         this.audit(key, connId, 'tables', database ?? "", 'none', false, true);
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] StringLiteral
lib/manager.ts:167:64
-         this.audit(key, connId, 'tables', database ?? 'default', 'none', false, true);
+         this.audit(key, connId, 'tables', database ?? 'default', "", false, true);
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] BooleanLiteral
lib/manager.ts:167:72
-         this.audit(key, connId, 'tables', database ?? 'default', 'none', false, true);
+         this.audit(key, connId, 'tables', database ?? 'default', 'none', true, true);
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] BooleanLiteral
lib/manager.ts:167:79
-         this.audit(key, connId, 'tables', database ?? 'default', 'none', false, true);
+         this.audit(key, connId, 'tables', database ?? 'default', 'none', false, false);
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] BlockStatement
lib/manager.ts:175:122
-     async schema(projectPath: string | undefined, connId: string, table: string, database?: string): Promise<ColumnInfo[]> {
-       const t = assertNonEmpty(table, 'table');
-       const { key, adapter } = await this.authorize(projectPath, connId, false);
-       try {
-         const result = await adapter.describeTable(t, database);
-         this.audit(key, connId, 'schema', t, 'none', false, true);
-         return result;
-       } catch (e) {
-         this.audit(key, connId, 'schema', t, 'none', false, false, this.errText(e));
-         throw this.toDriverError(e);
-       }
-     }
+     async schema(projectPath: string | undefined, connId: string, table: string, database?: string): Promise<ColumnInfo[]> {}
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] StringLiteral
lib/manager.ts:176:37
-       const t = assertNonEmpty(table, 'table');
+       const t = assertNonEmpty(table, "");
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] BooleanLiteral
lib/manager.ts:177:72
-       const { key, adapter } = await this.authorize(projectPath, connId, false);
+       const { key, adapter } = await this.authorize(projectPath, connId, true);
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] BlockStatement
lib/manager.ts:178:9
-       try {
-         const result = await adapter.describeTable(t, database);
-         this.audit(key, connId, 'schema', t, 'none', false, true);
-         return result;
-       } catch (e) {
+       try {} catch (e) {
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] StringLiteral
lib/manager.ts:180:31
-         this.audit(key, connId, 'schema', t, 'none', false, true);
+         this.audit(key, connId, "", t, 'none', false, true);
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] StringLiteral
lib/manager.ts:180:44
-         this.audit(key, connId, 'schema', t, 'none', false, true);
+         this.audit(key, connId, 'schema', t, "", false, true);
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] BooleanLiteral
lib/manager.ts:180:52
-         this.audit(key, connId, 'schema', t, 'none', false, true);
+         this.audit(key, connId, 'schema', t, 'none', true, true);
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] BooleanLiteral
lib/manager.ts:180:59
-         this.audit(key, connId, 'schema', t, 'none', false, true);
+         this.audit(key, connId, 'schema', t, 'none', false, false);
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] BlockStatement
lib/manager.ts:194:27
-     ): Promise<QueryResult> {
-       const t = assertNonEmpty(table, 'table');
-       const capped = Math.max(1, Math.min(50, Math.floor(Number(limit) || 10)));
-       const { key, adapter } = await this.authorize(projectPath, connId, false);
-       try {
-         const result = await adapter.previewRows(t, capped, database);
-         this.audit(key, connId, 'preview', `PREVIEW ${t} LIMIT ${capped}`, 'none', false, true, undefined, result.rowCount);
-         return result;
-       } catch (e) {
-         this.audit(key, connId, 'preview', `PREVIEW ${t} LIMIT ${capped}`, 'none', false, false, this.errText(e));
-         throw this.toDriverError(e);
-       }
-     }
+     ): Promise<QueryResult> {}
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] StringLiteral
lib/manager.ts:195:37
-       const t = assertNonEmpty(table, 'table');
+       const t = assertNonEmpty(table, "");
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] MethodExpression
lib/manager.ts:196:20
-       const capped = Math.max(1, Math.min(50, Math.floor(Number(limit) || 10)));
+       const capped = Math.min(1, Math.min(50, Math.floor(Number(limit) || 10)));
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] MethodExpression
lib/manager.ts:196:32
-       const capped = Math.max(1, Math.min(50, Math.floor(Number(limit) || 10)));
+       const capped = Math.max(1, Math.max(50, Math.floor(Number(limit) || 10)));
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] ConditionalExpression
lib/manager.ts:196:56
-       const capped = Math.max(1, Math.min(50, Math.floor(Number(limit) || 10)));
+       const capped = Math.max(1, Math.min(50, Math.floor(true)));
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] ConditionalExpression
lib/manager.ts:196:56
-       const capped = Math.max(1, Math.min(50, Math.floor(Number(limit) || 10)));
+       const capped = Math.max(1, Math.min(50, Math.floor(false)));
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] LogicalOperator
lib/manager.ts:196:56
-       const capped = Math.max(1, Math.min(50, Math.floor(Number(limit) || 10)));
+       const capped = Math.max(1, Math.min(50, Math.floor(Number(limit) && 10)));
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] BooleanLiteral
lib/manager.ts:197:72
-       const { key, adapter } = await this.authorize(projectPath, connId, false);
+       const { key, adapter } = await this.authorize(projectPath, connId, true);
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] BlockStatement
lib/manager.ts:198:9
-       try {
-         const result = await adapter.previewRows(t, capped, database);
-         this.audit(key, connId, 'preview', `PREVIEW ${t} LIMIT ${capped}`, 'none', false, true, undefined, result.rowCount);
-         return result;
-       } catch (e) {
+       try {} catch (e) {
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] StringLiteral
lib/manager.ts:200:31
-         this.audit(key, connId, 'preview', `PREVIEW ${t} LIMIT ${capped}`, 'none', false, true, undefined, result.rowCount);
+         this.audit(key, connId, "", `PREVIEW ${t} LIMIT ${capped}`, 'none', false, true, undefined, result.rowCount);
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] StringLiteral
lib/manager.ts:200:42
-         this.audit(key, connId, 'preview', `PREVIEW ${t} LIMIT ${capped}`, 'none', false, true, undefined, result.rowCount);
+         this.audit(key, connId, 'preview', ``, 'none', false, true, undefined, result.rowCount);
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] StringLiteral
lib/manager.ts:200:74
-         this.audit(key, connId, 'preview', `PREVIEW ${t} LIMIT ${capped}`, 'none', false, true, undefined, result.rowCount);
+         this.audit(key, connId, 'preview', `PREVIEW ${t} LIMIT ${capped}`, "", false, true, undefined, result.rowCount);
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] BooleanLiteral
lib/manager.ts:200:82
-         this.audit(key, connId, 'preview', `PREVIEW ${t} LIMIT ${capped}`, 'none', false, true, undefined, result.rowCount);
+         this.audit(key, connId, 'preview', `PREVIEW ${t} LIMIT ${capped}`, 'none', true, true, undefined, result.rowCount);
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] BooleanLiteral
lib/manager.ts:200:89
-         this.audit(key, connId, 'preview', `PREVIEW ${t} LIMIT ${capped}`, 'none', false, true, undefined, result.rowCount);
+         this.audit(key, connId, 'preview', `PREVIEW ${t} LIMIT ${capped}`, 'none', false, false, undefined, result.rowCount);
Tests ran:
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    DbToolService: 浏览与审计 databases/tables/schema/preview 正常；preview limit 强制 ≤50


[Survived] BlockStatement
lib/manager.ts:216:41
-     ): Promise<QueryResult | NeedConfirm> {
-       const statement = assertNonEmpty(sql, 'sql');
-       return this.runGuarded({
-         projectPath,
-         connId,
-         op: 'query',
-         statement,
-         challengeId,
-         run: (adapter) => adapter.query(statement, params),
-         rowsAffected: (r) => r.rowCount,
-       });
-     }
+     ): Promise<QueryResult | NeedConfirm> {}
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 8 more tests!


[Survived] StringLiteral
lib/manager.ts:217:43
-       const statement = assertNonEmpty(sql, 'sql');
+       const statement = assertNonEmpty(sql, "");
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 8 more tests!


[Survived] ObjectLiteral
lib/manager.ts:218:28
-       return this.runGuarded({
-         projectPath,
-         connId,
-         op: 'query',
-         statement,
-         challengeId,
-         run: (adapter) => adapter.query(statement, params),
-         rowsAffected: (r) => r.rowCount,
-       });
+       return this.runGuarded({});
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 8 more tests!


[Survived] StringLiteral
lib/manager.ts:221:11
-         op: 'query',
+         op: "",
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 8 more tests!


[Survived] ArrowFunction
lib/manager.ts:224:12
-         run: (adapter) => adapter.query(statement, params),
+         run: () => undefined,
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 8 more tests!


[Survived] ArrowFunction
lib/manager.ts:225:21
-         rowsAffected: (r) => r.rowCount,
+         rowsAffected: () => undefined,
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 8 more tests!


[Survived] BlockStatement
lib/manager.ts:235:40
-     ): Promise<ExecResult | NeedConfirm> {
-       const stmt = assertNonEmpty(statement, 'statement');
-       return this.runGuarded({
-         projectPath,
-         connId,
-         op: 'execute',
-         statement: stmt,
-         challengeId,
-         run: (adapter) => adapter.execute(stmt, params),
-         rowsAffected: (r) => r.affectedRows,
-       });
-     }
+     ): Promise<ExecResult | NeedConfirm> {}
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 11 more tests!


[Survived] StringLiteral
lib/manager.ts:236:44
-       const stmt = assertNonEmpty(statement, 'statement');
+       const stmt = assertNonEmpty(statement, "");
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 11 more tests!


[Survived] ObjectLiteral
lib/manager.ts:237:28
-       return this.runGuarded({
-         projectPath,
-         connId,
-         op: 'execute',
-         statement: stmt,
-         challengeId,
-         run: (adapter) => adapter.execute(stmt, params),
-         rowsAffected: (r) => r.affectedRows,
-       });
+       return this.runGuarded({});
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 11 more tests!


[Survived] StringLiteral
lib/manager.ts:240:11
-         op: 'execute',
+         op: "",
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 11 more tests!


[Survived] ArrowFunction
lib/manager.ts:243:12
-         run: (adapter) => adapter.execute(stmt, params),
+         run: () => undefined,
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 11 more tests!


[Survived] ArrowFunction
lib/manager.ts:244:21
-         rowsAffected: (r) => r.affectedRows,
+         rowsAffected: () => undefined,
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 11 more tests!


[Survived] BlockStatement
lib/manager.ts:258:37
-     ): Promise<unknown | NeedConfirm> {
-       const src = assertNonEmpty(code, 'code');
-       const { key, mode, adapter } = await this.authorize(projectPath, connId, true);
-       const db = {
-         query: async (sql: string, params?: unknown[]): Promise<unknown> => {
-           const r = await this.query(key, connId, sql, params);
-           return this.unwrapForScript(r);
-         },
-         execute: async (statement: string, params?: unknown[]): Promise<unknown> => {
-           const r = await this.execute(key, connId, statement, params, challengeId);
-           return this.unwrapForScript(r);
-         },
-       };
-       // ro 连接：写句柄直接拒（authorize needWrite 已拦，此处为句柄级兜底）
-       if (mode !== 'rw') {
-         db.execute = async () => {
-           throw new DbToolError('READ_ONLY', '该连接对本项目为只读授权（ro），脚本不可执行写操作');
-         };
-       }
-       this.audit(key, connId, 'script', src.length > 200 ? src.slice(0, 200) + '…' : src, 'none', false, true);
-       const sandbox: Record<string, unknown> = {
-         db,
-         console: { log: (...a: unknown[]) => console.log('[db-script]', ...a), error: (...a: unknown[]) => console.error('[db-script]', ...a) },
-       };
-       try {
-         const p = vm.runInNewContext(`(async () => {\n${src}\n})()`, sandbox, {
-           timeout: 60_000,
-           filename: 'db-script.js',
-         }) as Promise<unknown>;
-         return await Promise.race([
-           p,
-           new Promise<never>((_, rej) => {
-             const t = setTimeout(() => rej(new DbToolError('SCRIPT_TIMEOUT', '脚本执行超时（60s）')), 60_000);
-             t.unref?.();
-           }),
-         ]);
-       } catch (e) {
-         if (e instanceof DbToolError) throw e;
-         throw this.toDriverError(e);
-       }
-     }
+     ): Promise<unknown | NeedConfirm> {}
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
  and 4 more tests!


[Survived] StringLiteral
lib/manager.ts:259:38
-       const src = assertNonEmpty(code, 'code');
+       const src = assertNonEmpty(code, "");
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
  and 4 more tests!


[Survived] BooleanLiteral
lib/manager.ts:260:78
-       const { key, mode, adapter } = await this.authorize(projectPath, connId, true);
+       const { key, mode, adapter } = await this.authorize(projectPath, connId, false);
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
  and 4 more tests!


[Survived] ObjectLiteral
lib/manager.ts:262:16
-       const db = {
-         query: async (sql: string, params?: unknown[]): Promise<unknown> => {
-           const r = await this.query(key, connId, sql, params);
-           return this.unwrapForScript(r);
-         },
-         execute: async (statement: string, params?: unknown[]): Promise<unknown> => {
-           const r = await this.execute(key, connId, statement, params, challengeId);
-           return this.unwrapForScript(r);
-         },
-       };
+       const db = {};
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    DbToolService: run_script 沙箱 沙箱内无 require/process/globalThis.fs 逃逸面
  and 3 more tests!


[Survived] BlockStatement
lib/manager.ts:263:75
-         query: async (sql: string, params?: unknown[]): Promise<unknown> => {
-           const r = await this.query(key, connId, sql, params);
-           return this.unwrapForScript(r);
-         },
+         query: async (sql: string, params?: unknown[]): Promise<unknown> => {},
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] BlockStatement
lib/manager.ts:267:83
-         execute: async (statement: string, params?: unknown[]): Promise<unknown> => {
-           const r = await this.execute(key, connId, statement, params, challengeId);
-           return this.unwrapForScript(r);
-         },
+         execute: async (statement: string, params?: unknown[]): Promise<unknown> => {},
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    DbToolService: run_script 沙箱 脚本内危险语句 → NEEDS_CONFIRMATION（challengeId 为空时生成新 challenge）


[Survived] ConditionalExpression
lib/manager.ts:273:9
-       if (mode !== 'rw') {
+       if (true) {
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    DbToolService: run_script 沙箱 沙箱内无 require/process/globalThis.fs 逃逸面
  and 3 more tests!


[Survived] ConditionalExpression
lib/manager.ts:273:9
-       if (mode !== 'rw') {
+       if (false) {
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    DbToolService: run_script 沙箱 沙箱内无 require/process/globalThis.fs 逃逸面
  and 3 more tests!


[Survived] EqualityOperator
lib/manager.ts:273:9
-       if (mode !== 'rw') {
+       if (mode === 'rw') {
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    DbToolService: run_script 沙箱 沙箱内无 require/process/globalThis.fs 逃逸面
  and 3 more tests!


[Survived] StringLiteral
lib/manager.ts:273:18
-       if (mode !== 'rw') {
+       if (mode !== "") {
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    DbToolService: run_script 沙箱 沙箱内无 require/process/globalThis.fs 逃逸面
  and 3 more tests!


[Survived] StringLiteral
lib/manager.ts:279:29
-       this.audit(key, connId, 'script', src.length > 200 ? src.slice(0, 200) + '…' : src, 'none', false, true);
+       this.audit(key, connId, "", src.length > 200 ? src.slice(0, 200) + '…' : src, 'none', false, true);
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    DbToolService: run_script 沙箱 沙箱内无 require/process/globalThis.fs 逃逸面
  and 3 more tests!


[Survived] ConditionalExpression
lib/manager.ts:279:39
-       this.audit(key, connId, 'script', src.length > 200 ? src.slice(0, 200) + '…' : src, 'none', false, true);
+       this.audit(key, connId, 'script', true ? src.slice(0, 200) + '…' : src, 'none', false, true);
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    DbToolService: run_script 沙箱 沙箱内无 require/process/globalThis.fs 逃逸面
  and 3 more tests!


[Survived] ConditionalExpression
lib/manager.ts:279:39
-       this.audit(key, connId, 'script', src.length > 200 ? src.slice(0, 200) + '…' : src, 'none', false, true);
+       this.audit(key, connId, 'script', false ? src.slice(0, 200) + '…' : src, 'none', false, true);
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    DbToolService: run_script 沙箱 沙箱内无 require/process/globalThis.fs 逃逸面
  and 3 more tests!


[Survived] EqualityOperator
lib/manager.ts:279:39
-       this.audit(key, connId, 'script', src.length > 200 ? src.slice(0, 200) + '…' : src, 'none', false, true);
+       this.audit(key, connId, 'script', src.length >= 200 ? src.slice(0, 200) + '…' : src, 'none', false, true);
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    DbToolService: run_script 沙箱 沙箱内无 require/process/globalThis.fs 逃逸面
  and 3 more tests!


[Survived] EqualityOperator
lib/manager.ts:279:39
-       this.audit(key, connId, 'script', src.length > 200 ? src.slice(0, 200) + '…' : src, 'none', false, true);
+       this.audit(key, connId, 'script', src.length <= 200 ? src.slice(0, 200) + '…' : src, 'none', false, true);
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    DbToolService: run_script 沙箱 沙箱内无 require/process/globalThis.fs 逃逸面
  and 3 more tests!


[Survived] StringLiteral
lib/manager.ts:279:89
-       this.audit(key, connId, 'script', src.length > 200 ? src.slice(0, 200) + '…' : src, 'none', false, true);
+       this.audit(key, connId, 'script', src.length > 200 ? src.slice(0, 200) + '…' : src, "", false, true);
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    DbToolService: run_script 沙箱 沙箱内无 require/process/globalThis.fs 逃逸面
  and 3 more tests!


[Survived] BooleanLiteral
lib/manager.ts:279:97
-       this.audit(key, connId, 'script', src.length > 200 ? src.slice(0, 200) + '…' : src, 'none', false, true);
+       this.audit(key, connId, 'script', src.length > 200 ? src.slice(0, 200) + '…' : src, 'none', true, true);
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    DbToolService: run_script 沙箱 沙箱内无 require/process/globalThis.fs 逃逸面
  and 3 more tests!


[Survived] BooleanLiteral
lib/manager.ts:279:104
-       this.audit(key, connId, 'script', src.length > 200 ? src.slice(0, 200) + '…' : src, 'none', false, true);
+       this.audit(key, connId, 'script', src.length > 200 ? src.slice(0, 200) + '…' : src, 'none', false, false);
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    DbToolService: run_script 沙箱 沙箱内无 require/process/globalThis.fs 逃逸面
  and 3 more tests!


[Survived] ObjectLiteral
lib/manager.ts:280:46
-       const sandbox: Record<string, unknown> = {
-         db,
-         console: { log: (...a: unknown[]) => console.log('[db-script]', ...a), error: (...a: unknown[]) => console.error('[db-script]', ...a) },
-       };
+       const sandbox: Record<string, unknown> = {};
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    DbToolService: run_script 沙箱 沙箱内无 require/process/globalThis.fs 逃逸面
  and 3 more tests!


[Survived] ObjectLiteral
lib/manager.ts:282:16
-         console: { log: (...a: unknown[]) => console.log('[db-script]', ...a), error: (...a: unknown[]) => console.error('[db-script]', ...a) },
+         console: {},
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    DbToolService: run_script 沙箱 沙箱内无 require/process/globalThis.fs 逃逸面
  and 3 more tests!


[Survived] ArrowFunction
lib/manager.ts:282:85
-         console: { log: (...a: unknown[]) => console.log('[db-script]', ...a), error: (...a: unknown[]) => console.error('[db-script]', ...a) },
+         console: { log: (...a: unknown[]) => console.log('[db-script]', ...a), error: () => undefined },
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    DbToolService: run_script 沙箱 沙箱内无 require/process/globalThis.fs 逃逸面
  and 3 more tests!


[Survived] ArrowFunction
lib/manager.ts:282:23
-         console: { log: (...a: unknown[]) => console.log('[db-script]', ...a), error: (...a: unknown[]) => console.error('[db-script]', ...a) },
+         console: { log: () => undefined, error: (...a: unknown[]) => console.error('[db-script]', ...a) },
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    DbToolService: run_script 沙箱 沙箱内无 require/process/globalThis.fs 逃逸面
  and 3 more tests!


[Survived] BlockStatement
lib/manager.ts:284:9
-       try {
-         const p = vm.runInNewContext(`(async () => {\n${src}\n})()`, sandbox, {
-           timeout: 60_000,
-           filename: 'db-script.js',
-         }) as Promise<unknown>;
-         return await Promise.race([
-           p,
-           new Promise<never>((_, rej) => {
-             const t = setTimeout(() => rej(new DbToolError('SCRIPT_TIMEOUT', '脚本执行超时（60s）')), 60_000);
-             t.unref?.();
-           }),
-         ]);
-       } catch (e) {
+       try {} catch (e) {
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    DbToolService: run_script 沙箱 沙箱内无 require/process/globalThis.fs 逃逸面
  and 3 more tests!


[Survived] ArrayDeclaration
lib/manager.ts:289:33
-         return await Promise.race([
-           p,
-           new Promise<never>((_, rej) => {
-             const t = setTimeout(() => rej(new DbToolError('SCRIPT_TIMEOUT', '脚本执行超时（60s）')), 60_000);
-             t.unref?.();
-           }),
-         ]);
+         return await Promise.race([]);
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    DbToolService: run_script 沙箱 沙箱内无 require/process/globalThis.fs 逃逸面
  and 2 more tests!


[Survived] BlockStatement
lib/manager.ts:291:40
-           new Promise<never>((_, rej) => {
-             const t = setTimeout(() => rej(new DbToolError('SCRIPT_TIMEOUT', '脚本执行超时（60s）')), 60_000);
-             t.unref?.();
-           }),
+           new Promise<never>((_, rej) => {}),
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    DbToolService: run_script 沙箱 沙箱内无 require/process/globalThis.fs 逃逸面
  and 2 more tests!


[Survived] ArrowFunction
lib/manager.ts:292:32
-             const t = setTimeout(() => rej(new DbToolError('SCRIPT_TIMEOUT', '脚本执行超时（60s）')), 60_000);
+             const t = setTimeout(() => undefined, 60_000);
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    DbToolService: run_script 沙箱 沙箱内无 require/process/globalThis.fs 逃逸面
  and 2 more tests!


[Survived] OptionalChaining
lib/manager.ts:293:11
-             t.unref?.();
+             t.unref();
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    DbToolService: run_script 沙箱 沙箱内无 require/process/globalThis.fs 逃逸面
  and 2 more tests!


[Survived] BlockStatement
lib/manager.ts:296:17
-       } catch (e) {
-         if (e instanceof DbToolError) throw e;
-         throw this.toDriverError(e);
-       }
+       } catch (e) {}
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 沙箱内无 require/process/globalThis.fs 逃逸面
    DbToolService: run_script 沙箱 脚本内危险语句 → NEEDS_CONFIRMATION（challengeId 为空时生成新 challenge）
  and 1 more test!


[Survived] ConditionalExpression
lib/manager.ts:297:11
-         if (e instanceof DbToolError) throw e;
+         if (true) throw e;
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 沙箱内无 require/process/globalThis.fs 逃逸面
    DbToolService: run_script 沙箱 脚本内危险语句 → NEEDS_CONFIRMATION（challengeId 为空时生成新 challenge）
  and 1 more test!


[Survived] ConditionalExpression
lib/manager.ts:297:11
-         if (e instanceof DbToolError) throw e;
+         if (false) throw e;
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 沙箱内无 require/process/globalThis.fs 逃逸面
    DbToolService: run_script 沙箱 脚本内危险语句 → NEEDS_CONFIRMATION（challengeId 为空时生成新 challenge）
  and 1 more test!


[Survived] BlockStatement
lib/manager.ts:304:76
-     auditTail(projectPath: string | undefined, limit?: number): AuditEntry[] {
-       const n = Math.max(1, Math.min(500, Math.floor(Number(limit) || 50)));
-       if (!projectPath || projectPath.trim() === '') return this.store.audit.tail(n);
-       const key = normalizeProjectKey(projectPath);
-       return this.store.audit.tail(n).filter((e) => e.projectPathKey === key);
-     }
+     auditTail(projectPath: string | undefined, limit?: number): AuditEntry[] {}
Tests ran:
    fenced 校验 无 Origin/Referer（本地脚本）放行
    handleDbToolRequest 直调（prefix 挂载形态 + trust） loopback 同端口 + prefix 路径 /dsh-db-tool/api/state → 200
    handleDbToolRequest 直调（prefix 挂载形态 + trust） trustedHosts 条目放行（非 loopback Host + 受信 Origin）
  and 3 more tests!


[Survived] MethodExpression
lib/manager.ts:305:15
-       const n = Math.max(1, Math.min(500, Math.floor(Number(limit) || 50)));
+       const n = Math.min(1, Math.min(500, Math.floor(Number(limit) || 50)));
Tests ran:
    fenced 校验 无 Origin/Referer（本地脚本）放行
    handleDbToolRequest 直调（prefix 挂载形态 + trust） loopback 同端口 + prefix 路径 /dsh-db-tool/api/state → 200
    handleDbToolRequest 直调（prefix 挂载形态 + trust） trustedHosts 条目放行（非 loopback Host + 受信 Origin）
  and 3 more tests!


[Survived] MethodExpression
lib/manager.ts:305:27
-       const n = Math.max(1, Math.min(500, Math.floor(Number(limit) || 50)));
+       const n = Math.max(1, Math.max(500, Math.floor(Number(limit) || 50)));
Tests ran:
    fenced 校验 无 Origin/Referer（本地脚本）放行
    handleDbToolRequest 直调（prefix 挂载形态 + trust） loopback 同端口 + prefix 路径 /dsh-db-tool/api/state → 200
    handleDbToolRequest 直调（prefix 挂载形态 + trust） trustedHosts 条目放行（非 loopback Host + 受信 Origin）
  and 3 more tests!


[Survived] ConditionalExpression
lib/manager.ts:305:52
-       const n = Math.max(1, Math.min(500, Math.floor(Number(limit) || 50)));
+       const n = Math.max(1, Math.min(500, Math.floor(true)));
Tests ran:
    fenced 校验 无 Origin/Referer（本地脚本）放行
    handleDbToolRequest 直调（prefix 挂载形态 + trust） loopback 同端口 + prefix 路径 /dsh-db-tool/api/state → 200
    handleDbToolRequest 直调（prefix 挂载形态 + trust） trustedHosts 条目放行（非 loopback Host + 受信 Origin）
  and 3 more tests!


[Survived] ConditionalExpression
lib/manager.ts:305:52
-       const n = Math.max(1, Math.min(500, Math.floor(Number(limit) || 50)));
+       const n = Math.max(1, Math.min(500, Math.floor(false)));
Tests ran:
    fenced 校验 无 Origin/Referer（本地脚本）放行
    handleDbToolRequest 直调（prefix 挂载形态 + trust） loopback 同端口 + prefix 路径 /dsh-db-tool/api/state → 200
    handleDbToolRequest 直调（prefix 挂载形态 + trust） trustedHosts 条目放行（非 loopback Host + 受信 Origin）
  and 3 more tests!


[Survived] LogicalOperator
lib/manager.ts:305:52
-       const n = Math.max(1, Math.min(500, Math.floor(Number(limit) || 50)));
+       const n = Math.max(1, Math.min(500, Math.floor(Number(limit) && 50)));
Tests ran:
    fenced 校验 无 Origin/Referer（本地脚本）放行
    handleDbToolRequest 直调（prefix 挂载形态 + trust） loopback 同端口 + prefix 路径 /dsh-db-tool/api/state → 200
    handleDbToolRequest 直调（prefix 挂载形态 + trust） trustedHosts 条目放行（非 loopback Host + 受信 Origin）
  and 3 more tests!


[Survived] ConditionalExpression
lib/manager.ts:306:9
-       if (!projectPath || projectPath.trim() === '') return this.store.audit.tail(n);
+       if (true) return this.store.audit.tail(n);
Tests ran:
    fenced 校验 无 Origin/Referer（本地脚本）放行
    handleDbToolRequest 直调（prefix 挂载形态 + trust） loopback 同端口 + prefix 路径 /dsh-db-tool/api/state → 200
    handleDbToolRequest 直调（prefix 挂载形态 + trust） trustedHosts 条目放行（非 loopback Host + 受信 Origin）
  and 3 more tests!


[Survived] ConditionalExpression
lib/manager.ts:306:9
-       if (!projectPath || projectPath.trim() === '') return this.store.audit.tail(n);
+       if (false) return this.store.audit.tail(n);
Tests ran:
    fenced 校验 无 Origin/Referer（本地脚本）放行
    handleDbToolRequest 直调（prefix 挂载形态 + trust） loopback 同端口 + prefix 路径 /dsh-db-tool/api/state → 200
    handleDbToolRequest 直调（prefix 挂载形态 + trust） trustedHosts 条目放行（非 loopback Host + 受信 Origin）
  and 3 more tests!


[Survived] LogicalOperator
lib/manager.ts:306:9
-       if (!projectPath || projectPath.trim() === '') return this.store.audit.tail(n);
+       if (!projectPath && projectPath.trim() === '') return this.store.audit.tail(n);
Tests ran:
    fenced 校验 无 Origin/Referer（本地脚本）放行
    handleDbToolRequest 直调（prefix 挂载形态 + trust） loopback 同端口 + prefix 路径 /dsh-db-tool/api/state → 200
    handleDbToolRequest 直调（prefix 挂载形态 + trust） trustedHosts 条目放行（非 loopback Host + 受信 Origin）
  and 3 more tests!


[Survived] ConditionalExpression
lib/manager.ts:306:25
-       if (!projectPath || projectPath.trim() === '') return this.store.audit.tail(n);
+       if (!projectPath || false) return this.store.audit.tail(n);
Tests ran:
    业务路由与确认流程 GET /api/audit 与 /api/state


[Survived] EqualityOperator
lib/manager.ts:306:25
-       if (!projectPath || projectPath.trim() === '') return this.store.audit.tail(n);
+       if (!projectPath || projectPath.trim() !== '') return this.store.audit.tail(n);
Tests ran:
    业务路由与确认流程 GET /api/audit 与 /api/state


[Survived] MethodExpression
lib/manager.ts:306:25
-       if (!projectPath || projectPath.trim() === '') return this.store.audit.tail(n);
+       if (!projectPath || projectPath === '') return this.store.audit.tail(n);
Tests ran:
    业务路由与确认流程 GET /api/audit 与 /api/state


[Survived] BooleanLiteral
lib/manager.ts:306:9
-       if (!projectPath || projectPath.trim() === '') return this.store.audit.tail(n);
+       if (projectPath || projectPath.trim() === '') return this.store.audit.tail(n);
Tests ran:
    fenced 校验 无 Origin/Referer（本地脚本）放行
    handleDbToolRequest 直调（prefix 挂载形态 + trust） loopback 同端口 + prefix 路径 /dsh-db-tool/api/state → 200
    handleDbToolRequest 直调（prefix 挂载形态 + trust） trustedHosts 条目放行（非 loopback Host + 受信 Origin）
  and 3 more tests!


[Survived] StringLiteral
lib/manager.ts:306:48
-       if (!projectPath || projectPath.trim() === '') return this.store.audit.tail(n);
+       if (!projectPath || projectPath.trim() === "Stryker was here!") return this.store.audit.tail(n);
Tests ran:
    业务路由与确认流程 GET /api/audit 与 /api/state


[Survived] MethodExpression
lib/manager.ts:308:12
-       return this.store.audit.tail(n).filter((e) => e.projectPathKey === key);
+       return this.store.audit.tail(n);
Tests ran:
    业务路由与确认流程 GET /api/audit 与 /api/state


[Survived] ArrowFunction
lib/manager.ts:308:44
-       return this.store.audit.tail(n).filter((e) => e.projectPathKey === key);
+       return this.store.audit.tail(n).filter(() => undefined);
Tests ran:
    业务路由与确认流程 GET /api/audit 与 /api/state


[Survived] ConditionalExpression
lib/manager.ts:308:51
-       return this.store.audit.tail(n).filter((e) => e.projectPathKey === key);
+       return this.store.audit.tail(n).filter((e) => true);
Tests ran:
    业务路由与确认流程 GET /api/audit 与 /api/state


[Survived] ConditionalExpression
lib/manager.ts:308:51
-       return this.store.audit.tail(n).filter((e) => e.projectPathKey === key);
+       return this.store.audit.tail(n).filter((e) => false);
Tests ran:
    业务路由与确认流程 GET /api/audit 与 /api/state


[Survived] EqualityOperator
lib/manager.ts:308:51
-       return this.store.audit.tail(n).filter((e) => e.projectPathKey === key);
+       return this.store.audit.tail(n).filter((e) => e.projectPathKey !== key);
Tests ran:
    业务路由与确认流程 GET /api/audit 与 /api/state


[Survived] BlockStatement
lib/manager.ts:315:5
-     } {
-       const grants = projectPath && projectPath.trim() !== '' ? this.grantsFor(projectPath) : [];
-       return {
-         connections: this.listConnections(),
-         grants,
-         auditTail: this.auditTail(projectPath, 50),
-       };
-     }
+     } {}
Tests ran:
    fenced 校验 无 Origin/Referer（本地脚本）放行
    handleDbToolRequest 直调（prefix 挂载形态 + trust） loopback 同端口 + prefix 路径 /dsh-db-tool/api/state → 200
    handleDbToolRequest 直调（prefix 挂载形态 + trust） trustedHosts 条目放行（非 loopback Host + 受信 Origin）
  and 1 more test!


[Survived] ConditionalExpression
lib/manager.ts:316:20
-       const grants = projectPath && projectPath.trim() !== '' ? this.grantsFor(projectPath) : [];
+       const grants = true ? this.grantsFor(projectPath) : [];
Tests ran:
    fenced 校验 无 Origin/Referer（本地脚本）放行
    handleDbToolRequest 直调（prefix 挂载形态 + trust） loopback 同端口 + prefix 路径 /dsh-db-tool/api/state → 200
    handleDbToolRequest 直调（prefix 挂载形态 + trust） trustedHosts 条目放行（非 loopback Host + 受信 Origin）
  and 1 more test!


[Survived] ConditionalExpression
lib/manager.ts:316:20
-       const grants = projectPath && projectPath.trim() !== '' ? this.grantsFor(projectPath) : [];
+       const grants = false ? this.grantsFor(projectPath) : [];
Tests ran:
    fenced 校验 无 Origin/Referer（本地脚本）放行
    handleDbToolRequest 直调（prefix 挂载形态 + trust） loopback 同端口 + prefix 路径 /dsh-db-tool/api/state → 200
    handleDbToolRequest 直调（prefix 挂载形态 + trust） trustedHosts 条目放行（非 loopback Host + 受信 Origin）
  and 1 more test!


[Survived] LogicalOperator
lib/manager.ts:316:20
-       const grants = projectPath && projectPath.trim() !== '' ? this.grantsFor(projectPath) : [];
+       const grants = projectPath || projectPath.trim() !== '' ? this.grantsFor(projectPath) : [];
Tests ran:
    fenced 校验 无 Origin/Referer（本地脚本）放行
    handleDbToolRequest 直调（prefix 挂载形态 + trust） loopback 同端口 + prefix 路径 /dsh-db-tool/api/state → 200
    handleDbToolRequest 直调（prefix 挂载形态 + trust） trustedHosts 条目放行（非 loopback Host + 受信 Origin）
  and 1 more test!


[Survived] EqualityOperator
lib/manager.ts:316:35
-       const grants = projectPath && projectPath.trim() !== '' ? this.grantsFor(projectPath) : [];
+       const grants = projectPath && projectPath.trim() === '' ? this.grantsFor(projectPath) : [];
Tests ran:
    业务路由与确认流程 GET /api/audit 与 /api/state


[Survived] ConditionalExpression
lib/manager.ts:316:35
-       const grants = projectPath && projectPath.trim() !== '' ? this.grantsFor(projectPath) : [];
+       const grants = projectPath && true ? this.grantsFor(projectPath) : [];
Tests ran:
    业务路由与确认流程 GET /api/audit 与 /api/state


[Survived] StringLiteral
lib/manager.ts:316:58
-       const grants = projectPath && projectPath.trim() !== '' ? this.grantsFor(projectPath) : [];
+       const grants = projectPath && projectPath.trim() !== "Stryker was here!" ? this.grantsFor(projectPath) : [];
Tests ran:
    业务路由与确认流程 GET /api/audit 与 /api/state


[Survived] MethodExpression
lib/manager.ts:316:35
-       const grants = projectPath && projectPath.trim() !== '' ? this.grantsFor(projectPath) : [];
+       const grants = projectPath && projectPath !== '' ? this.grantsFor(projectPath) : [];
Tests ran:
    业务路由与确认流程 GET /api/audit 与 /api/state


[Survived] ArrayDeclaration
lib/manager.ts:316:93
-       const grants = projectPath && projectPath.trim() !== '' ? this.grantsFor(projectPath) : [];
+       const grants = projectPath && projectPath.trim() !== '' ? this.grantsFor(projectPath) : ["Stryker was here"];
Tests ran:
    fenced 校验 无 Origin/Referer（本地脚本）放行
    handleDbToolRequest 直调（prefix 挂载形态 + trust） loopback 同端口 + prefix 路径 /dsh-db-tool/api/state → 200
    handleDbToolRequest 直调（prefix 挂载形态 + trust） trustedHosts 条目放行（非 loopback Host + 受信 Origin）


[Survived] ObjectLiteral
lib/manager.ts:317:12
-       return {
-         connections: this.listConnections(),
-         grants,
-         auditTail: this.auditTail(projectPath, 50),
-       };
+       return {};
Tests ran:
    fenced 校验 无 Origin/Referer（本地脚本）放行
    handleDbToolRequest 直调（prefix 挂载形态 + trust） loopback 同端口 + prefix 路径 /dsh-db-tool/api/state → 200
    handleDbToolRequest 直调（prefix 挂载形态 + trust） trustedHosts 条目放行（非 loopback Host + 受信 Origin）
  and 1 more test!


[Survived] BlockStatement
lib/manager.ts:342:93
-     private requireConn(connId: string): ReturnType<DbToolStore['connections']['testTarget']> {
-       try {
-         return this.store.connections.testTarget(connId);
-       } catch {
-         throw new DbToolError('NOT_FOUND', `连接不存在: ${connId}`);
-       }
-     }
+     private requireConn(connId: string): ReturnType<DbToolStore['connections']['testTarget']> {}
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
    DbToolService: 授权与只读 rw 连接 mode=rw 透传，DML 直接执行
  and 13 more tests!


[Survived] BlockStatement
lib/manager.ts:343:9
-       try {
-         return this.store.connections.testTarget(connId);
-       } catch {
+       try {} catch {
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
    DbToolService: 授权与只读 rw 连接 mode=rw 透传，DML 直接执行
  and 13 more tests!


[Survived] ConditionalExpression
lib/manager.ts:356:9
-       if (this.disposed) throw new DbToolError('INVALID_ARGUMENT', '服务已关闭');
+       if (true) throw new DbToolError('INVALID_ARGUMENT', '服务已关闭');
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
  and 22 more tests!


[Survived] BlockStatement
lib/manager.ts:355:76
-     ): Promise<{ key: string; mode: 'ro' | 'rw'; adapter: DatabaseAdapter }> {
-       if (this.disposed) throw new DbToolError('INVALID_ARGUMENT', '服务已关闭');
-       if (!projectPath || projectPath.trim() === '') {
-         throw new DbToolError('UNAUTHORIZED_PROJECT', '业务操作需要 projectPath（匿名项目仅可管理连接）');
-       }
-       const key = normalizeProjectKey(projectPath);
-       const mode = this.store.grants.check(key, connId);
-       if (!mode) throw new DbToolError('UNAUTHORIZED_PROJECT', `项目未授权该连接: ${connId}`);
-       if (needWrite && mode === 'ro') {
-         throw new DbToolError('READ_ONLY', '该连接对本项目为只读授权（ro），拒绝写操作');
-       }
-       const adapter = await this.adapterFor(connId, mode);
-       return { key, mode, adapter };
-     }
+     ): Promise<{ key: string; mode: 'ro' | 'rw'; adapter: DatabaseAdapter }> {}
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
  and 22 more tests!


[Survived] ConditionalExpression
lib/manager.ts:356:9
-       if (this.disposed) throw new DbToolError('INVALID_ARGUMENT', '服务已关闭');
+       if (false) throw new DbToolError('INVALID_ARGUMENT', '服务已关闭');
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
  and 22 more tests!


[Survived] ConditionalExpression
lib/manager.ts:357:9
-       if (!projectPath || projectPath.trim() === '') {
+       if (true) {
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
  and 22 more tests!


[Survived] ConditionalExpression
lib/manager.ts:357:9
-       if (!projectPath || projectPath.trim() === '') {
+       if (false) {
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
  and 22 more tests!


[Survived] LogicalOperator
lib/manager.ts:357:9
-       if (!projectPath || projectPath.trim() === '') {
+       if (!projectPath && projectPath.trim() === '') {
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
  and 22 more tests!


[Survived] BooleanLiteral
lib/manager.ts:357:9
-       if (!projectPath || projectPath.trim() === '') {
+       if (projectPath || projectPath.trim() === '') {
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
  and 22 more tests!


[Survived] ConditionalExpression
lib/manager.ts:357:25
-       if (!projectPath || projectPath.trim() === '') {
+       if (!projectPath || false) {
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
  and 22 more tests!


[Survived] EqualityOperator
lib/manager.ts:357:25
-       if (!projectPath || projectPath.trim() === '') {
+       if (!projectPath || projectPath.trim() !== '') {
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
  and 22 more tests!


[Survived] MethodExpression
lib/manager.ts:357:25
-       if (!projectPath || projectPath.trim() === '') {
+       if (!projectPath || projectPath === '') {
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
  and 22 more tests!


[Survived] BlockStatement
lib/manager.ts:357:52
-       if (!projectPath || projectPath.trim() === '') {
-         throw new DbToolError('UNAUTHORIZED_PROJECT', '业务操作需要 projectPath（匿名项目仅可管理连接）');
-       }
+       if (!projectPath || projectPath.trim() === '') {}
Tests ran:
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝


[Survived] StringLiteral
lib/manager.ts:358:29
-         throw new DbToolError('UNAUTHORIZED_PROJECT', '业务操作需要 projectPath（匿名项目仅可管理连接）');
+         throw new DbToolError("", '业务操作需要 projectPath（匿名项目仅可管理连接）');
Tests ran:
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝


[Survived] StringLiteral
lib/manager.ts:358:53
-         throw new DbToolError('UNAUTHORIZED_PROJECT', '业务操作需要 projectPath（匿名项目仅可管理连接）');
+         throw new DbToolError('UNAUTHORIZED_PROJECT', "");
Tests ran:
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝


[Survived] StringLiteral
lib/manager.ts:357:48
-       if (!projectPath || projectPath.trim() === '') {
+       if (!projectPath || projectPath.trim() === "Stryker was here!") {
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
  and 22 more tests!


[Survived] BooleanLiteral
lib/manager.ts:362:9
-       if (!mode) throw new DbToolError('UNAUTHORIZED_PROJECT', `项目未授权该连接: ${connId}`);
+       if (mode) throw new DbToolError('UNAUTHORIZED_PROJECT', `项目未授权该连接: ${connId}`);
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
  and 22 more tests!


[Survived] ConditionalExpression
lib/manager.ts:362:9
-       if (!mode) throw new DbToolError('UNAUTHORIZED_PROJECT', `项目未授权该连接: ${connId}`);
+       if (true) throw new DbToolError('UNAUTHORIZED_PROJECT', `项目未授权该连接: ${connId}`);
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
  and 22 more tests!


[Survived] StringLiteral
lib/manager.ts:362:38
-       if (!mode) throw new DbToolError('UNAUTHORIZED_PROJECT', `项目未授权该连接: ${connId}`);
+       if (!mode) throw new DbToolError("", `项目未授权该连接: ${connId}`);
Tests ran:
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 1 more test!


[Survived] ConditionalExpression
lib/manager.ts:362:9
-       if (!mode) throw new DbToolError('UNAUTHORIZED_PROJECT', `项目未授权该连接: ${connId}`);
+       if (false) throw new DbToolError('UNAUTHORIZED_PROJECT', `项目未授权该连接: ${connId}`);
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
  and 22 more tests!


[Survived] StringLiteral
lib/manager.ts:362:62
-       if (!mode) throw new DbToolError('UNAUTHORIZED_PROJECT', `项目未授权该连接: ${connId}`);
+       if (!mode) throw new DbToolError('UNAUTHORIZED_PROJECT', ``);
Tests ran:
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    DbToolService: 授权与只读 未授权项目 → UNAUTHORIZED_PROJECT；缺 projectPath 同样拒绝
    DbToolService: 授权与只读 未授权连接一概 UNAUTHORIZED_PROJECT（不泄露存在性）；已知项目下未知连接 remove → NOT_FOUND
  and 1 more test!


[Survived] ConditionalExpression
lib/manager.ts:363:9
-       if (needWrite && mode === 'ro') {
+       if (true) {
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
  and 18 more tests!


[Survived] ConditionalExpression
lib/manager.ts:363:9
-       if (needWrite && mode === 'ro') {
+       if (false) {
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
  and 18 more tests!


[Survived] LogicalOperator
lib/manager.ts:363:9
-       if (needWrite && mode === 'ro') {
+       if (needWrite || mode === 'ro') {
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
  and 18 more tests!


[Survived] ConditionalExpression
lib/manager.ts:363:22
-       if (needWrite && mode === 'ro') {
+       if (needWrite && true) {
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 14 more tests!


[Survived] BlockStatement
lib/manager.ts:363:37
-       if (needWrite && mode === 'ro') {
-         throw new DbToolError('READ_ONLY', '该连接对本项目为只读授权（ro），拒绝写操作');
-       }
+       if (needWrite && mode === 'ro') {}
Tests ran:
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
    DbToolService: 危险确认与 challenge ro 连接对危险语句也先报 READ_ONLY（授权优先于 guard）
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] EqualityOperator
lib/manager.ts:363:22
-       if (needWrite && mode === 'ro') {
+       if (needWrite && mode !== 'ro') {
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 14 more tests!


[Survived] StringLiteral
lib/manager.ts:364:29
-         throw new DbToolError('READ_ONLY', '该连接对本项目为只读授权（ro），拒绝写操作');
+         throw new DbToolError("", '该连接对本项目为只读授权（ro），拒绝写操作');
Tests ran:
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
    DbToolService: 危险确认与 challenge ro 连接对危险语句也先报 READ_ONLY（授权优先于 guard）
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] StringLiteral
lib/manager.ts:363:31
-       if (needWrite && mode === 'ro') {
+       if (needWrite && mode === "") {
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 14 more tests!


[Survived] StringLiteral
lib/manager.ts:364:42
-         throw new DbToolError('READ_ONLY', '该连接对本项目为只读授权（ro），拒绝写操作');
+         throw new DbToolError('READ_ONLY', "");
Tests ran:
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
    DbToolService: 危险确认与 challenge ro 连接对危险语句也先报 READ_ONLY（授权优先于 guard）
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] ObjectLiteral
lib/manager.ts:367:12
-       return { key, mode, adapter };
+       return {};
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
  and 17 more tests!


[Survived] BlockStatement
lib/manager.ts:370:83
-     private adapterFor(connId: string, mode: 'ro' | 'rw'): Promise<DatabaseAdapter> {
-       let p = this.adapterCache.get(connId);
-       if (!p) {
-         p = (async () => {
-           const rc = this.requireConn(connId);
-           const factory = await this.resolver(rc.meta.kind);
-           return factory(rc, { mode });
-         })().catch((e) => {
-           this.adapterCache.delete(connId);
-           throw e instanceof DbToolError ? e : this.toDriverError(e);
-         });
-         this.adapterCache.set(connId, p);
-       }
-       return p;
-     }
+     private adapterFor(connId: string, mode: 'ro' | 'rw'): Promise<DatabaseAdapter> {}
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
  and 17 more tests!


[Survived] BooleanLiteral
lib/manager.ts:372:9
-       if (!p) {
+       if (p) {
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
  and 17 more tests!


[Survived] ConditionalExpression
lib/manager.ts:372:9
-       if (!p) {
+       if (true) {
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
  and 17 more tests!


[Survived] BlockStatement
lib/manager.ts:372:13
-       if (!p) {
-         p = (async () => {
-           const rc = this.requireConn(connId);
-           const factory = await this.resolver(rc.meta.kind);
-           return factory(rc, { mode });
-         })().catch((e) => {
-           this.adapterCache.delete(connId);
-           throw e instanceof DbToolError ? e : this.toDriverError(e);
-         });
-         this.adapterCache.set(connId, p);
-       }
+       if (!p) {}
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
    DbToolService: 授权与只读 rw 连接 mode=rw 透传，DML 直接执行
  and 13 more tests!


[Survived] ConditionalExpression
lib/manager.ts:372:9
-       if (!p) {
+       if (false) {
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
  and 17 more tests!


[Survived] BlockStatement
lib/manager.ts:373:24
-         p = (async () => {
-           const rc = this.requireConn(connId);
-           const factory = await this.resolver(rc.meta.kind);
-           return factory(rc, { mode });
-         })().catch((e) => {
+         p = (async () => {})().catch((e) => {
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
    DbToolService: 授权与只读 rw 连接 mode=rw 透传，DML 直接执行
  and 13 more tests!


[Survived] ObjectLiteral
lib/manager.ts:376:28
-           return factory(rc, { mode });
+           return factory(rc, {});
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
    DbToolService: 授权与只读 rw 连接 mode=rw 透传，DML 直接执行
  and 13 more tests!


[Survived] CallExpression
lib/manager.ts:381:7
-         this.adapterCache.set(connId, p);
+         ;
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    DbToolService: 授权与只读 ro 连接 execute / run_script → READ_ONLY；query 可用；mode 透传工厂
    DbToolService: 授权与只读 rw 连接 mode=rw 透传，DML 直接执行
  and 13 more tests!


[Survived] BlockStatement
lib/manager.ts:395:32
-     }): Promise<T | NeedConfirm> {
-       const { projectPath, connId, op, statement, challengeId } = args;
-       const { key, adapter } = await this.authorize(projectPath, connId, op === 'execute');
-       const kind = adapter.kind;
-       const verdict = classifyStatement(kind, statement, op);
-       if (verdict.level === 'danger') {
-         if (!challengeId) {
-           const { id: cid } = this.challenges.create(statement);
-           this.audit(key, connId, op, statement, 'danger', false, false, 'NEEDS_CONFIRMATION');
-           return { needConfirmation: true, challengeId: cid, statement, danger: 'danger', reason: verdict.reason ?? '危险操作，需要用户确认' };
-         }
-         if (!this.challenges.consume(challengeId, statement)) {
-           this.audit(key, connId, op, statement, 'danger', false, false, 'INVALID_CHALLENGE');
-           throw new DbToolError('INVALID_CHALLENGE', '确认凭据无效（不存在、已使用、已过期或语句已变更），请重新发起');
-         }
-       }
-       try {
-         const result = await args.run(adapter);
-         this.audit(key, connId, op, statement, verdict.level as DangerLevel, verdict.level === 'danger', true, undefined, args.rowsAffected?.(result));
-         return result;
-       } catch (e) {
-         this.audit(key, connId, op, statement, verdict.level as DangerLevel, verdict.level === 'danger', false, this.errText(e));
-         throw this.toDriverError(e);
-       }
-     }
+     }): Promise<T | NeedConfirm> {}
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
  and 18 more tests!


[Survived] ConditionalExpression
lib/manager.ts:397:72
-       const { key, adapter } = await this.authorize(projectPath, connId, op === 'execute');
+       const { key, adapter } = await this.authorize(projectPath, connId, true);
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
  and 18 more tests!


[Survived] ConditionalExpression
lib/manager.ts:397:72
-       const { key, adapter } = await this.authorize(projectPath, connId, op === 'execute');
+       const { key, adapter } = await this.authorize(projectPath, connId, false);
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
  and 18 more tests!


[Survived] StringLiteral
lib/manager.ts:397:79
-       const { key, adapter } = await this.authorize(projectPath, connId, op === 'execute');
+       const { key, adapter } = await this.authorize(projectPath, connId, op === "");
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
  and 18 more tests!


[Survived] EqualityOperator
lib/manager.ts:397:72
-       const { key, adapter } = await this.authorize(projectPath, connId, op === 'execute');
+       const { key, adapter } = await this.authorize(projectPath, connId, op !== 'execute');
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 未授权项目 → UNAUTHORIZED_PROJECT
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
  and 18 more tests!


[Survived] ConditionalExpression
lib/manager.ts:401:9
-       if (verdict.level === 'danger') {
+       if (true) {
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 13 more tests!


[Survived] ConditionalExpression
lib/manager.ts:401:9
-       if (verdict.level === 'danger') {
+       if (false) {
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 13 more tests!


[Survived] EqualityOperator
lib/manager.ts:401:9
-       if (verdict.level === 'danger') {
+       if (verdict.level !== 'danger') {
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 13 more tests!


[Survived] StringLiteral
lib/manager.ts:401:27
-       if (verdict.level === 'danger') {
+       if (verdict.level === "") {
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
  and 13 more tests!


[Survived] BooleanLiteral
lib/manager.ts:402:11
-         if (!challengeId) {
+         if (challengeId) {
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 8 more tests!


[Survived] BlockStatement
lib/manager.ts:401:37
-       if (verdict.level === 'danger') {
-         if (!challengeId) {
-           const { id: cid } = this.challenges.create(statement);
-           this.audit(key, connId, op, statement, 'danger', false, false, 'NEEDS_CONFIRMATION');
-           return { needConfirmation: true, challengeId: cid, statement, danger: 'danger', reason: verdict.reason ?? '危险操作，需要用户确认' };
-         }
-         if (!this.challenges.consume(challengeId, statement)) {
-           this.audit(key, connId, op, statement, 'danger', false, false, 'INVALID_CHALLENGE');
-           throw new DbToolError('INVALID_CHALLENGE', '确认凭据无效（不存在、已使用、已过期或语句已变更），请重新发起');
-         }
-       }
+       if (verdict.level === 'danger') {}
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 8 more tests!


[Survived] ConditionalExpression
lib/manager.ts:402:11
-         if (!challengeId) {
+         if (true) {
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 8 more tests!


[Survived] ConditionalExpression
lib/manager.ts:402:11
-         if (!challengeId) {
+         if (false) {
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 8 more tests!


[Survived] BlockStatement
lib/manager.ts:402:25
-         if (!challengeId) {
-           const { id: cid } = this.challenges.create(statement);
-           this.audit(key, connId, op, statement, 'danger', false, false, 'NEEDS_CONFIRMATION');
-           return { needConfirmation: true, challengeId: cid, statement, danger: 'danger', reason: verdict.reason ?? '危险操作，需要用户确认' };
-         }
+         if (!challengeId) {}
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 8 more tests!


[Survived] StringLiteral
lib/manager.ts:404:48
-           this.audit(key, connId, op, statement, 'danger', false, false, 'NEEDS_CONFIRMATION');
+           this.audit(key, connId, op, statement, "", false, false, 'NEEDS_CONFIRMATION');
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 8 more tests!


[Survived] BooleanLiteral
lib/manager.ts:404:58
-           this.audit(key, connId, op, statement, 'danger', false, false, 'NEEDS_CONFIRMATION');
+           this.audit(key, connId, op, statement, 'danger', true, false, 'NEEDS_CONFIRMATION');
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 8 more tests!


[Survived] BooleanLiteral
lib/manager.ts:404:65
-           this.audit(key, connId, op, statement, 'danger', false, false, 'NEEDS_CONFIRMATION');
+           this.audit(key, connId, op, statement, 'danger', false, true, 'NEEDS_CONFIRMATION');
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 8 more tests!


[Survived] StringLiteral
lib/manager.ts:404:72
-           this.audit(key, connId, op, statement, 'danger', false, false, 'NEEDS_CONFIRMATION');
+           this.audit(key, connId, op, statement, 'danger', false, false, "");
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 8 more tests!


[Survived] ObjectLiteral
lib/manager.ts:405:16
-           return { needConfirmation: true, challengeId: cid, statement, danger: 'danger', reason: verdict.reason ?? '危险操作，需要用户确认' };
+           return {};
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 8 more tests!


[Survived] BooleanLiteral
lib/manager.ts:405:36
-           return { needConfirmation: true, challengeId: cid, statement, danger: 'danger', reason: verdict.reason ?? '危险操作，需要用户确认' };
+           return { needConfirmation: false, challengeId: cid, statement, danger: 'danger', reason: verdict.reason ?? '危险操作，需要用户确认' };
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 8 more tests!


[Survived] StringLiteral
lib/manager.ts:405:79
-           return { needConfirmation: true, challengeId: cid, statement, danger: 'danger', reason: verdict.reason ?? '危险操作，需要用户确认' };
+           return { needConfirmation: true, challengeId: cid, statement, danger: "", reason: verdict.reason ?? '危险操作，需要用户确认' };
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 8 more tests!


[Survived] BooleanLiteral
lib/manager.ts:407:11
-         if (!this.challenges.consume(challengeId, statement)) {
+         if (this.challenges.consume(challengeId, statement)) {
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 5 more tests!


[Survived] LogicalOperator
lib/manager.ts:405:97
-           return { needConfirmation: true, challengeId: cid, statement, danger: 'danger', reason: verdict.reason ?? '危险操作，需要用户确认' };
+           return { needConfirmation: true, challengeId: cid, statement, danger: 'danger', reason: verdict.reason && '危险操作，需要用户确认' };
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 8 more tests!


[Survived] ConditionalExpression
lib/manager.ts:407:11
-         if (!this.challenges.consume(challengeId, statement)) {
+         if (true) {
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 5 more tests!


[Survived] ConditionalExpression
lib/manager.ts:407:11
-         if (!this.challenges.consume(challengeId, statement)) {
+         if (false) {
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 5 more tests!


[Survived] BlockStatement
lib/manager.ts:407:61
-         if (!this.challenges.consume(challengeId, statement)) {
-           this.audit(key, connId, op, statement, 'danger', false, false, 'INVALID_CHALLENGE');
-           throw new DbToolError('INVALID_CHALLENGE', '确认凭据无效（不存在、已使用、已过期或语句已变更），请重新发起');
-         }
+         if (!this.challenges.consume(challengeId, statement)) {}
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    DbToolService: 危险确认与 challenge challenge 一次性：重放同 id → INVALID_CHALLENGE
  and 2 more tests!


[Survived] StringLiteral
lib/manager.ts:408:48
-           this.audit(key, connId, op, statement, 'danger', false, false, 'INVALID_CHALLENGE');
+           this.audit(key, connId, op, statement, "", false, false, 'INVALID_CHALLENGE');
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    DbToolService: 危险确认与 challenge challenge 一次性：重放同 id → INVALID_CHALLENGE
  and 2 more tests!


[Survived] BooleanLiteral
lib/manager.ts:408:58
-           this.audit(key, connId, op, statement, 'danger', false, false, 'INVALID_CHALLENGE');
+           this.audit(key, connId, op, statement, 'danger', true, false, 'INVALID_CHALLENGE');
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    DbToolService: 危险确认与 challenge challenge 一次性：重放同 id → INVALID_CHALLENGE
  and 2 more tests!


[Survived] BooleanLiteral
lib/manager.ts:408:65
-           this.audit(key, connId, op, statement, 'danger', false, false, 'INVALID_CHALLENGE');
+           this.audit(key, connId, op, statement, 'danger', false, true, 'INVALID_CHALLENGE');
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    DbToolService: 危险确认与 challenge challenge 一次性：重放同 id → INVALID_CHALLENGE
  and 2 more tests!


[Survived] StringLiteral
lib/manager.ts:408:72
-           this.audit(key, connId, op, statement, 'danger', false, false, 'INVALID_CHALLENGE');
+           this.audit(key, connId, op, statement, 'danger', false, false, "");
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    DbToolService: 危险确认与 challenge challenge 一次性：重放同 id → INVALID_CHALLENGE
  and 2 more tests!


[Survived] StringLiteral
lib/manager.ts:409:31
-           throw new DbToolError('INVALID_CHALLENGE', '确认凭据无效（不存在、已使用、已过期或语句已变更），请重新发起');
+           throw new DbToolError("", '确认凭据无效（不存在、已使用、已过期或语句已变更），请重新发起');
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    DbToolService: 危险确认与 challenge challenge 一次性：重放同 id → INVALID_CHALLENGE
  and 2 more tests!


[Survived] StringLiteral
lib/manager.ts:409:52
-           throw new DbToolError('INVALID_CHALLENGE', '确认凭据无效（不存在、已使用、已过期或语句已变更），请重新发起');
+           throw new DbToolError('INVALID_CHALLENGE', "");
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    DbToolService: 危险确认与 challenge challenge 一次性：重放同 id → INVALID_CHALLENGE
  and 2 more tests!


[Survived] BlockStatement
lib/manager.ts:413:9
-       try {
-         const result = await args.run(adapter);
-         this.audit(key, connId, op, statement, verdict.level as DangerLevel, verdict.level === 'danger', true, undefined, args.rowsAffected?.(result));
-         return result;
-       } catch (e) {
+       try {} catch (e) {
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 9 more tests!


[Survived] CallExpression
lib/manager.ts:415:7
-         this.audit(key, connId, op, statement, verdict.level as DangerLevel, verdict.level === 'danger', true, undefined, args.rowsAffected?.(result));
+         ;
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 9 more tests!


[Survived] ConditionalExpression
lib/manager.ts:415:76
-         this.audit(key, connId, op, statement, verdict.level as DangerLevel, verdict.level === 'danger', true, undefined, args.rowsAffected?.(result));
+         this.audit(key, connId, op, statement, verdict.level as DangerLevel, true, true, undefined, args.rowsAffected?.(result));
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 9 more tests!


[Survived] ConditionalExpression
lib/manager.ts:415:76
-         this.audit(key, connId, op, statement, verdict.level as DangerLevel, verdict.level === 'danger', true, undefined, args.rowsAffected?.(result));
+         this.audit(key, connId, op, statement, verdict.level as DangerLevel, false, true, undefined, args.rowsAffected?.(result));
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 9 more tests!


[Survived] EqualityOperator
lib/manager.ts:415:76
-         this.audit(key, connId, op, statement, verdict.level as DangerLevel, verdict.level === 'danger', true, undefined, args.rowsAffected?.(result));
+         this.audit(key, connId, op, statement, verdict.level as DangerLevel, verdict.level !== 'danger', true, undefined, args.rowsAffected?.(result));
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 9 more tests!


[Survived] StringLiteral
lib/manager.ts:415:94
-         this.audit(key, connId, op, statement, verdict.level as DangerLevel, verdict.level === 'danger', true, undefined, args.rowsAffected?.(result));
+         this.audit(key, connId, op, statement, verdict.level as DangerLevel, verdict.level === "", true, undefined, args.rowsAffected?.(result));
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 9 more tests!


[Survived] BooleanLiteral
lib/manager.ts:415:104
-         this.audit(key, connId, op, statement, verdict.level as DangerLevel, verdict.level === 'danger', true, undefined, args.rowsAffected?.(result));
+         this.audit(key, connId, op, statement, verdict.level as DangerLevel, verdict.level === 'danger', false, undefined, args.rowsAffected?.(result));
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 9 more tests!


[Survived] OptionalChaining
lib/manager.ts:415:121
-         this.audit(key, connId, op, statement, verdict.level as DangerLevel, verdict.level === 'danger', true, undefined, args.rowsAffected?.(result));
+         this.audit(key, connId, op, statement, verdict.level as DangerLevel, verdict.level === 'danger', true, undefined, args.rowsAffected(result));
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 9 more tests!


[Survived] BlockStatement
lib/manager.ts:424:48
-     private unwrapForScript(r: unknown): unknown {
-       if (r && typeof r === 'object' && (r as NeedConfirm).needConfirmation === true) {
-         const nc = r as NeedConfirm;
-         throw new DbToolError('NEEDS_CONFIRMATION', JSON.stringify(nc));
-       }
-       return r;
-     }
+     private unwrapForScript(r: unknown): unknown {}
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    DbToolService: run_script 沙箱 脚本内危险语句 → NEEDS_CONFIRMATION（challengeId 为空时生成新 challenge）
  and 1 more test!


[Survived] ConditionalExpression
lib/manager.ts:425:9
-       if (r && typeof r === 'object' && (r as NeedConfirm).needConfirmation === true) {
+       if (false) {
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    DbToolService: run_script 沙箱 脚本内危险语句 → NEEDS_CONFIRMATION（challengeId 为空时生成新 challenge）
  and 1 more test!


[Survived] ConditionalExpression
lib/manager.ts:425:9
-       if (r && typeof r === 'object' && (r as NeedConfirm).needConfirmation === true) {
+       if (true) {
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    DbToolService: run_script 沙箱 脚本内危险语句 → NEEDS_CONFIRMATION（challengeId 为空时生成新 challenge）
  and 1 more test!


[Survived] LogicalOperator
lib/manager.ts:425:9
-       if (r && typeof r === 'object' && (r as NeedConfirm).needConfirmation === true) {
+       if (r && typeof r === 'object' || (r as NeedConfirm).needConfirmation === true) {
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    DbToolService: run_script 沙箱 脚本内危险语句 → NEEDS_CONFIRMATION（challengeId 为空时生成新 challenge）
  and 1 more test!


[Survived] ConditionalExpression
lib/manager.ts:425:9
-       if (r && typeof r === 'object' && (r as NeedConfirm).needConfirmation === true) {
+       if (true && (r as NeedConfirm).needConfirmation === true) {
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    DbToolService: run_script 沙箱 脚本内危险语句 → NEEDS_CONFIRMATION（challengeId 为空时生成新 challenge）
  and 1 more test!


[Survived] LogicalOperator
lib/manager.ts:425:9
-       if (r && typeof r === 'object' && (r as NeedConfirm).needConfirmation === true) {
+       if (r || typeof r === 'object' && (r as NeedConfirm).needConfirmation === true) {
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    DbToolService: run_script 沙箱 脚本内危险语句 → NEEDS_CONFIRMATION（challengeId 为空时生成新 challenge）
  and 1 more test!


[Survived] ConditionalExpression
lib/manager.ts:425:14
-       if (r && typeof r === 'object' && (r as NeedConfirm).needConfirmation === true) {
+       if (r && true && (r as NeedConfirm).needConfirmation === true) {
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    DbToolService: run_script 沙箱 脚本内危险语句 → NEEDS_CONFIRMATION（challengeId 为空时生成新 challenge）
  and 1 more test!


[Survived] EqualityOperator
lib/manager.ts:425:14
-       if (r && typeof r === 'object' && (r as NeedConfirm).needConfirmation === true) {
+       if (r && typeof r !== 'object' && (r as NeedConfirm).needConfirmation === true) {
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    DbToolService: run_script 沙箱 脚本内危险语句 → NEEDS_CONFIRMATION（challengeId 为空时生成新 challenge）
  and 1 more test!


[Survived] StringLiteral
lib/manager.ts:425:27
-       if (r && typeof r === 'object' && (r as NeedConfirm).needConfirmation === true) {
+       if (r && typeof r === "" && (r as NeedConfirm).needConfirmation === true) {
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    DbToolService: run_script 沙箱 脚本内危险语句 → NEEDS_CONFIRMATION（challengeId 为空时生成新 challenge）
  and 1 more test!


[Survived] ConditionalExpression
lib/manager.ts:425:39
-       if (r && typeof r === 'object' && (r as NeedConfirm).needConfirmation === true) {
+       if (r && typeof r === 'object' && true) {
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    DbToolService: run_script 沙箱 脚本内危险语句 → NEEDS_CONFIRMATION（challengeId 为空时生成新 challenge）
  and 1 more test!


[Survived] EqualityOperator
lib/manager.ts:425:39
-       if (r && typeof r === 'object' && (r as NeedConfirm).needConfirmation === true) {
+       if (r && typeof r === 'object' && (r as NeedConfirm).needConfirmation !== true) {
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    DbToolService: run_script 沙箱 脚本内危险语句 → NEEDS_CONFIRMATION（challengeId 为空时生成新 challenge）
  and 1 more test!


[Survived] BooleanLiteral
lib/manager.ts:425:79
-       if (r && typeof r === 'object' && (r as NeedConfirm).needConfirmation === true) {
+       if (r && typeof r === 'object' && (r as NeedConfirm).needConfirmation === false) {
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 rw：脚本可 db.query / db.execute，结果返回
    DbToolService: run_script 沙箱 脚本内危险语句 → NEEDS_CONFIRMATION（challengeId 为空时生成新 challenge）
  and 1 more test!


[Survived] BlockStatement
lib/manager.ts:425:85
-       if (r && typeof r === 'object' && (r as NeedConfirm).needConfirmation === true) {
-         const nc = r as NeedConfirm;
-         throw new DbToolError('NEEDS_CONFIRMATION', JSON.stringify(nc));
-       }
+       if (r && typeof r === 'object' && (r as NeedConfirm).needConfirmation === true) {}
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 脚本内危险语句 → NEEDS_CONFIRMATION（challengeId 为空时生成新 challenge）


[Survived] StringLiteral
lib/manager.ts:427:29
-         throw new DbToolError('NEEDS_CONFIRMATION', JSON.stringify(nc));
+         throw new DbToolError("", JSON.stringify(nc));
Tests ran:
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
    DbToolService: run_script 沙箱 脚本内危险语句 → NEEDS_CONFIRMATION（challengeId 为空时生成新 challenge）


[Survived] BlockStatement
lib/manager.ts:442:11
-     ): void {
-       try {
-         this.store.audit.append({
-           projectPathKey, connId, action, statement, danger, confirmed, ok,
-           ...(error !== undefined ? { error } : {}),
-           ...(rowsAffected !== undefined ? { rowsAffected } : {}),
-         });
-       } catch {
-         // 审计失败不阻塞业务（best-effort）
-       }
-     }
+     ): void {}
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    连接管理与授权路由 grants PUT/GET/DELETE
  and 20 more tests!


[Survived] ObjectLiteral
lib/manager.ts:444:31
-         this.store.audit.append({
-           projectPathKey, connId, action, statement, danger, confirmed, ok,
-           ...(error !== undefined ? { error } : {}),
-           ...(rowsAffected !== undefined ? { rowsAffected } : {}),
-         });
+         this.store.audit.append({});
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    连接管理与授权路由 grants PUT/GET/DELETE
  and 20 more tests!


[Survived] BlockStatement
lib/manager.ts:443:9
-       try {
-         this.store.audit.append({
-           projectPathKey, connId, action, statement, danger, confirmed, ok,
-           ...(error !== undefined ? { error } : {}),
-           ...(rowsAffected !== undefined ? { rowsAffected } : {}),
-         });
-       } catch {
+       try {} catch {
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    连接管理与授权路由 grants PUT/GET/DELETE
  and 20 more tests!


[Survived] ConditionalExpression
lib/manager.ts:446:13
-           ...(error !== undefined ? { error } : {}),
+           ...(true ? { error } : {}),
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    连接管理与授权路由 grants PUT/GET/DELETE
  and 20 more tests!


[Survived] ConditionalExpression
lib/manager.ts:446:13
-           ...(error !== undefined ? { error } : {}),
+           ...(false ? { error } : {}),
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    连接管理与授权路由 grants PUT/GET/DELETE
  and 20 more tests!


[Survived] ObjectLiteral
lib/manager.ts:446:35
-           ...(error !== undefined ? { error } : {}),
+           ...(error !== undefined ? {} : {}),
Tests ran:
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
    业务路由与确认流程 改语句带原 challengeId → INVALID_CHALLENGE
    业务路由与确认流程 POST /api/script：正常脚本与 NEEDS_CONFIRMATION（message JSON 还原为扁平响应）
  and 8 more tests!


[Survived] ConditionalExpression
lib/manager.ts:447:13
-           ...(rowsAffected !== undefined ? { rowsAffected } : {}),
+           ...(false ? { rowsAffected } : {}),
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    连接管理与授权路由 grants PUT/GET/DELETE
  and 20 more tests!


[Survived] EqualityOperator
lib/manager.ts:446:13
-           ...(error !== undefined ? { error } : {}),
+           ...(error === undefined ? { error } : {}),
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    连接管理与授权路由 grants PUT/GET/DELETE
  and 20 more tests!


[Survived] ConditionalExpression
lib/manager.ts:447:13
-           ...(rowsAffected !== undefined ? { rowsAffected } : {}),
+           ...(true ? { rowsAffected } : {}),
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    连接管理与授权路由 grants PUT/GET/DELETE
  and 20 more tests!


[Survived] BlockStatement
lib/manager.ts:454:39
-     private errText(e: unknown): string {
-       return e instanceof Error ? e.message : String(e);
-     }
+     private errText(e: unknown): string {}
Tests ran:
    DbToolService: run_script 沙箱 沙箱内无 require/process/globalThis.fs 逃逸面
    DbToolService: run_script 沙箱 脚本语法错误 → DRIVER_ERROR 而非崩溃


[Survived] BlockStatement
lib/manager.ts:458:44
-     private toDriverError(e: unknown): Error {
-       if (e instanceof DbToolError) return e;
-       return new DbToolError('DRIVER_ERROR', this.errText(e));
-     }
+     private toDriverError(e: unknown): Error {}
Tests ran:
    DbToolService: run_script 沙箱 沙箱内无 require/process/globalThis.fs 逃逸面
    DbToolService: run_script 沙箱 脚本语法错误 → DRIVER_ERROR 而非崩溃


[Survived] EqualityOperator
lib/manager.ts:447:13
-           ...(rowsAffected !== undefined ? { rowsAffected } : {}),
+           ...(rowsAffected === undefined ? { rowsAffected } : {}),
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    连接管理与授权路由 POST /api/connections 创建 + PUT 更新 + DELETE 删除
    连接管理与授权路由 grants PUT/GET/DELETE
  and 20 more tests!


[Survived] ObjectLiteral
lib/manager.ts:447:42
-           ...(rowsAffected !== undefined ? { rowsAffected } : {}),
+           ...(rowsAffected !== undefined ? {} : {}),
Tests ran:
    handleDbToolRequest 直调（prefix 挂载形态 + trust） POST body 直打：query 路由经 handler 正常返回
    业务路由与确认流程 GET /api/databases、/api/tables、/api/schema、/api/preview
    业务路由与确认流程 危险 execute → NEEDS_CONFIRMATION；携 challengeId 重发放行；重放被拒
  and 11 more tests!


[Survived] ConditionalExpression
lib/manager.ts:459:9
-       if (e instanceof DbToolError) return e;
+       if (true) return e;
Tests ran:
    DbToolService: run_script 沙箱 沙箱内无 require/process/globalThis.fs 逃逸面
    DbToolService: run_script 沙箱 脚本语法错误 → DRIVER_ERROR 而非崩溃


[Survived] ConditionalExpression
lib/manager.ts:459:9
-       if (e instanceof DbToolError) return e;
+       if (false) return e;
Tests ran:
    DbToolService: run_script 沙箱 沙箱内无 require/process/globalThis.fs 逃逸面
    DbToolService: run_script 沙箱 脚本语法错误 → DRIVER_ERROR 而非崩溃


[Survived] StringLiteral
lib/manager.ts:460:28
-       return new DbToolError('DRIVER_ERROR', this.errText(e));
+       return new DbToolError("", this.errText(e));
Tests ran:
    DbToolService: run_script 沙箱 沙箱内无 require/process/globalThis.fs 逃逸面
    DbToolService: run_script 沙箱 脚本语法错误 → DRIVER_ERROR 而非崩溃


[Survived] BlockStatement
lib/manager.ts:485:92
-   function pick(args: ToolActionArgs, snake: 'conn_id' | 'challenge_id'): string | undefined {
-     return args[snake] ?? (snake === 'conn_id' ? args.connId : args.challengeId);
-   }
+   function pick(args: ToolActionArgs, snake: 'conn_id' | 'challenge_id'): string | undefined {}
Tests ran:
    handleToolAction（工具分发层） list_connections 返回脱敏 JSON 文本
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出）
  and 2 more tests!


[Survived] LogicalOperator
lib/manager.ts:486:10
-     return args[snake] ?? (snake === 'conn_id' ? args.connId : args.challengeId);
+     return args[snake] && (snake === 'conn_id' ? args.connId : args.challengeId);
Tests ran:
    handleToolAction（工具分发层） list_connections 返回脱敏 JSON 文本
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出）
  and 2 more tests!


[Survived] ConditionalExpression
lib/manager.ts:486:26
-     return args[snake] ?? (snake === 'conn_id' ? args.connId : args.challengeId);
+     return args[snake] ?? (true ? args.connId : args.challengeId);
Tests ran:
    handleToolAction（工具分发层） list_connections 返回脱敏 JSON 文本
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出）
  and 2 more tests!


[Survived] ConditionalExpression
lib/manager.ts:486:26
-     return args[snake] ?? (snake === 'conn_id' ? args.connId : args.challengeId);
+     return args[snake] ?? (false ? args.connId : args.challengeId);
Tests ran:
    handleToolAction（工具分发层） list_connections 返回脱敏 JSON 文本
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出）
  and 2 more tests!


[Survived] EqualityOperator
lib/manager.ts:486:26
-     return args[snake] ?? (snake === 'conn_id' ? args.connId : args.challengeId);
+     return args[snake] ?? (snake !== 'conn_id' ? args.connId : args.challengeId);
Tests ran:
    handleToolAction（工具分发层） list_connections 返回脱敏 JSON 文本
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出）
  and 2 more tests!


[Survived] BlockStatement
lib/manager.ts:497:20
-   ): Promise<string> {
-     const action = args.action;
-     const connId = pick(args, 'conn_id');
-     const challengeId = pick(args, 'challenge_id');
-     const j = (v: unknown) => JSON.stringify(v, null, 2);
-     try {
-       switch (action) {
-         case 'list_connections':
-           return j(service.listConnections());
-         case 'query': {
-           const r = await service.query(projectPath, requireConn(connId), assertArg(args.sql, 'sql'), args.params, challengeId);
-           return needConfirmText(r) ?? j(r);
-         }
-         case 'execute': {
-           const r = await service.execute(projectPath, requireConn(connId), assertArg(args.statement ?? args.sql, 'statement'), args.params, challengeId);
-           return needConfirmText(r) ?? j(r);
-         }
-         case 'schema': {
-           const conn = requireConn(connId);
-           if (args.table) return j(await service.schema(projectPath, conn, args.table, args.database));
-           if (args.database) return j(await service.tables(projectPath, conn, args.database));
-           return j(await service.databases(projectPath, conn));
-         }
-         case 'preview': {
-           const r = await service.preview(projectPath, requireConn(connId), assertArg(args.table, 'table'), args.limit, args.database);
-           return j(r);
-         }
-         case 'run_script': {
-           const r = await service.runScript(projectPath, requireConn(connId), assertArg(args.code, 'code'), challengeId);
-           return needConfirmText(r) ?? j(r);
-         }
-         default:
-           throw new DbToolError('INVALID_ARGUMENT', `未知 action: ${action}（可用: list_connections/query/execute/schema/preview/run_script）`);
-       }
-     } catch (e) {
-       const code = e instanceof DbToolError ? e.code : 'DRIVER_ERROR';
-       return j({ ok: false, error: e instanceof Error ? e.message : String(e), code });
-     }
-   }
+   ): Promise<string> {}
Tests ran:
    handleToolAction（工具分发层） list_connections 返回脱敏 JSON 文本
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出）
  and 2 more tests!


[Survived] StringLiteral
lib/manager.ts:486:36
-     return args[snake] ?? (snake === 'conn_id' ? args.connId : args.challengeId);
+     return args[snake] ?? (snake === "" ? args.connId : args.challengeId);
Tests ran:
    handleToolAction（工具分发层） list_connections 返回脱敏 JSON 文本
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出）
  and 2 more tests!


[Survived] StringLiteral
lib/manager.ts:499:29
-     const connId = pick(args, 'conn_id');
+     const connId = pick(args, "");
Tests ran:
    handleToolAction（工具分发层） list_connections 返回脱敏 JSON 文本
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出）
  and 2 more tests!


[Survived] StringLiteral
lib/manager.ts:500:34
-     const challengeId = pick(args, 'challenge_id');
+     const challengeId = pick(args, "");
Tests ran:
    handleToolAction（工具分发层） list_connections 返回脱敏 JSON 文本
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出）
  and 2 more tests!


[Survived] BlockStatement
lib/manager.ts:503:7
-     try {
-       switch (action) {
-         case 'list_connections':
-           return j(service.listConnections());
-         case 'query': {
-           const r = await service.query(projectPath, requireConn(connId), assertArg(args.sql, 'sql'), args.params, challengeId);
-           return needConfirmText(r) ?? j(r);
-         }
-         case 'execute': {
-           const r = await service.execute(projectPath, requireConn(connId), assertArg(args.statement ?? args.sql, 'statement'), args.params, challengeId);
-           return needConfirmText(r) ?? j(r);
-         }
-         case 'schema': {
-           const conn = requireConn(connId);
-           if (args.table) return j(await service.schema(projectPath, conn, args.table, args.database));
-           if (args.database) return j(await service.tables(projectPath, conn, args.database));
-           return j(await service.databases(projectPath, conn));
-         }
-         case 'preview': {
-           const r = await service.preview(projectPath, requireConn(connId), assertArg(args.table, 'table'), args.limit, args.database);
-           return j(r);
-         }
-         case 'run_script': {
-           const r = await service.runScript(projectPath, requireConn(connId), assertArg(args.code, 'code'), challengeId);
-           return needConfirmText(r) ?? j(r);
-         }
-         default:
-           throw new DbToolError('INVALID_ARGUMENT', `未知 action: ${action}（可用: list_connections/query/execute/schema/preview/run_script）`);
-       }
-     } catch (e) {
+     try {} catch (e) {
Tests ran:
    handleToolAction（工具分发层） list_connections 返回脱敏 JSON 文本
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出）
  and 2 more tests!


[Survived] ArrowFunction
lib/manager.ts:501:13
-     const j = (v: unknown) => JSON.stringify(v, null, 2);
+     const j = () => undefined;
Tests ran:
    handleToolAction（工具分发层） list_connections 返回脱敏 JSON 文本
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出）
  and 2 more tests!


[Survived] ConditionalExpression
lib/manager.ts:505:7
-         case 'list_connections':
-           return j(service.listConnections());
+         case 'list_connections':
Tests ran:
    handleToolAction（工具分发层） list_connections 返回脱敏 JSON 文本


[Survived] StringLiteral
lib/manager.ts:505:12
-         case 'list_connections':
+         case "":
Tests ran:
    handleToolAction（工具分发层） list_connections 返回脱敏 JSON 文本
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出）
  and 2 more tests!


[Survived] StringLiteral
lib/manager.ts:508:12
-         case 'query': {
+         case "": {
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出）
    handleToolAction（工具分发层） 缺参数 / 未知 action → INVALID_ARGUMENT 错误 JSON
  and 1 more test!


[Survived] ConditionalExpression
lib/manager.ts:508:7
-         case 'query': {
-           const r = await service.query(projectPath, requireConn(connId), assertArg(args.sql, 'sql'), args.params, challengeId);
-           return needConfirmText(r) ?? j(r);
-         }
+         case 'query':
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出）
    handleToolAction（工具分发层） 缺参数 / 未知 action → INVALID_ARGUMENT 错误 JSON


[Survived] BlockStatement
lib/manager.ts:508:21
-         case 'query': {
-           const r = await service.query(projectPath, requireConn(connId), assertArg(args.sql, 'sql'), args.params, challengeId);
-           return needConfirmText(r) ?? j(r);
-         }
+         case 'query': {}
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出）
    handleToolAction（工具分发层） 缺参数 / 未知 action → INVALID_ARGUMENT 错误 JSON


[Survived] StringLiteral
lib/manager.ts:509:93
-           const r = await service.query(projectPath, requireConn(connId), assertArg(args.sql, 'sql'), args.params, challengeId);
+           const r = await service.query(projectPath, requireConn(connId), assertArg(args.sql, ""), args.params, challengeId);
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出）


[Survived] ConditionalExpression
lib/manager.ts:513:7
-         case 'execute': {
-           const r = await service.execute(projectPath, requireConn(connId), assertArg(args.statement ?? args.sql, 'statement'), args.params, challengeId);
-           return needConfirmText(r) ?? j(r);
-         }
+         case 'execute':
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引


[Survived] LogicalOperator
lib/manager.ts:510:16
-           return needConfirmText(r) ?? j(r);
+           return needConfirmText(r) && j(r);
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引


[Survived] StringLiteral
lib/manager.ts:513:12
-         case 'execute': {
+         case "": {
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） 缺参数 / 未知 action → INVALID_ARGUMENT 错误 JSON
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] BlockStatement
lib/manager.ts:513:23
-         case 'execute': {
-           const r = await service.execute(projectPath, requireConn(connId), assertArg(args.statement ?? args.sql, 'statement'), args.params, challengeId);
-           return needConfirmText(r) ?? j(r);
-         }
+         case 'execute': {}
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引


[Survived] LogicalOperator
lib/manager.ts:514:85
-           const r = await service.execute(projectPath, requireConn(connId), assertArg(args.statement ?? args.sql, 'statement'), args.params, challengeId);
+           const r = await service.execute(projectPath, requireConn(connId), assertArg(args.statement && args.sql, 'statement'), args.params, challengeId);
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引


[Survived] StringLiteral
lib/manager.ts:514:113
-           const r = await service.execute(projectPath, requireConn(connId), assertArg(args.statement ?? args.sql, 'statement'), args.params, challengeId);
+           const r = await service.execute(projectPath, requireConn(connId), assertArg(args.statement ?? args.sql, ""), args.params, challengeId);
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引


[Survived] LogicalOperator
lib/manager.ts:515:16
-           return needConfirmText(r) ?? j(r);
+           return needConfirmText(r) && j(r);
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引


[Survived] StringLiteral
lib/manager.ts:525:12
-         case 'preview': {
+         case "": {
Tests ran:
    handleToolAction（工具分发层） 缺参数 / 未知 action → INVALID_ARGUMENT 错误 JSON
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] StringLiteral
lib/manager.ts:518:12
-         case 'schema': {
+         case "": {
Tests ran:
    handleToolAction（工具分发层） 缺参数 / 未知 action → INVALID_ARGUMENT 错误 JSON
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] ConditionalExpression
lib/manager.ts:530:7
-         case 'run_script': {
-           const r = await service.runScript(projectPath, requireConn(connId), assertArg(args.code, 'code'), challengeId);
-           return needConfirmText(r) ?? j(r);
-         }
+         case 'run_script':
Tests ran:
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] StringLiteral
lib/manager.ts:530:12
-         case 'run_script': {
+         case "": {
Tests ran:
    handleToolAction（工具分发层） 缺参数 / 未知 action → INVALID_ARGUMENT 错误 JSON
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] StringLiteral
lib/manager.ts:531:98
-           const r = await service.runScript(projectPath, requireConn(connId), assertArg(args.code, 'code'), challengeId);
+           const r = await service.runScript(projectPath, requireConn(connId), assertArg(args.code, ""), challengeId);
Tests ran:
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] BlockStatement
lib/manager.ts:530:26
-         case 'run_script': {
-           const r = await service.runScript(projectPath, requireConn(connId), assertArg(args.code, 'code'), challengeId);
-           return needConfirmText(r) ?? j(r);
-         }
+         case 'run_script': {}
Tests ran:
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] LogicalOperator
lib/manager.ts:532:16
-           return needConfirmText(r) ?? j(r);
+           return needConfirmText(r) && j(r);
Tests ran:
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] ConditionalExpression
lib/manager.ts:535:7
-         default:
-           throw new DbToolError('INVALID_ARGUMENT', `未知 action: ${action}（可用: list_connections/query/execute/schema/preview/run_script）`);
+         default:
Tests ran:
    handleToolAction（工具分发层） 缺参数 / 未知 action → INVALID_ARGUMENT 错误 JSON


[Survived] StringLiteral
lib/manager.ts:536:31
-           throw new DbToolError('INVALID_ARGUMENT', `未知 action: ${action}（可用: list_connections/query/execute/schema/preview/run_script）`);
+           throw new DbToolError("", `未知 action: ${action}（可用: list_connections/query/execute/schema/preview/run_script）`);
Tests ran:
    handleToolAction（工具分发层） 缺参数 / 未知 action → INVALID_ARGUMENT 错误 JSON


[Survived] StringLiteral
lib/manager.ts:536:51
-           throw new DbToolError('INVALID_ARGUMENT', `未知 action: ${action}（可用: list_connections/query/execute/schema/preview/run_script）`);
+           throw new DbToolError('INVALID_ARGUMENT', ``);
Tests ran:
    handleToolAction（工具分发层） 缺参数 / 未知 action → INVALID_ARGUMENT 错误 JSON


[Survived] BlockStatement
lib/manager.ts:538:15
-     } catch (e) {
-       const code = e instanceof DbToolError ? e.code : 'DRIVER_ERROR';
-       return j({ ok: false, error: e instanceof Error ? e.message : String(e), code });
-     }
+     } catch (e) {}
Tests ran:
    handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出）
    handleToolAction（工具分发层） 缺参数 / 未知 action → INVALID_ARGUMENT 错误 JSON
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] ObjectLiteral
lib/manager.ts:540:14
-       return j({ ok: false, error: e instanceof Error ? e.message : String(e), code });
+       return j({});
Tests ran:
    handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出）
    handleToolAction（工具分发层） 缺参数 / 未知 action → INVALID_ARGUMENT 错误 JSON
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] BooleanLiteral
lib/manager.ts:540:20
-       return j({ ok: false, error: e instanceof Error ? e.message : String(e), code });
+       return j({ ok: true, error: e instanceof Error ? e.message : String(e), code });
Tests ran:
    handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出）
    handleToolAction（工具分发层） 缺参数 / 未知 action → INVALID_ARGUMENT 错误 JSON
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] BlockStatement
lib/manager.ts:544:58
-   function requireConn(connId: string | undefined): string {
-     if (!connId || connId.trim() === '') throw new DbToolError('INVALID_ARGUMENT', '缺少必填参数: conn_id');
-     return connId;
-   }
+   function requireConn(connId: string | undefined): string {}
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出）
    handleToolAction（工具分发层） 缺参数 / 未知 action → INVALID_ARGUMENT 错误 JSON
  and 1 more test!


[Survived] ConditionalExpression
lib/manager.ts:545:7
-     if (!connId || connId.trim() === '') throw new DbToolError('INVALID_ARGUMENT', '缺少必填参数: conn_id');
+     if (true) throw new DbToolError('INVALID_ARGUMENT', '缺少必填参数: conn_id');
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出）
    handleToolAction（工具分发层） 缺参数 / 未知 action → INVALID_ARGUMENT 错误 JSON
  and 1 more test!


[Survived] ConditionalExpression
lib/manager.ts:545:7
-     if (!connId || connId.trim() === '') throw new DbToolError('INVALID_ARGUMENT', '缺少必填参数: conn_id');
+     if (false) throw new DbToolError('INVALID_ARGUMENT', '缺少必填参数: conn_id');
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出）
    handleToolAction（工具分发层） 缺参数 / 未知 action → INVALID_ARGUMENT 错误 JSON
  and 1 more test!


[Survived] LogicalOperator
lib/manager.ts:545:7
-     if (!connId || connId.trim() === '') throw new DbToolError('INVALID_ARGUMENT', '缺少必填参数: conn_id');
+     if (!connId && connId.trim() === '') throw new DbToolError('INVALID_ARGUMENT', '缺少必填参数: conn_id');
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出）
    handleToolAction（工具分发层） 缺参数 / 未知 action → INVALID_ARGUMENT 错误 JSON
  and 1 more test!


[Survived] BooleanLiteral
lib/manager.ts:545:7
-     if (!connId || connId.trim() === '') throw new DbToolError('INVALID_ARGUMENT', '缺少必填参数: conn_id');
+     if (connId || connId.trim() === '') throw new DbToolError('INVALID_ARGUMENT', '缺少必填参数: conn_id');
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出）
    handleToolAction（工具分发层） 缺参数 / 未知 action → INVALID_ARGUMENT 错误 JSON
  and 1 more test!


[Survived] ConditionalExpression
lib/manager.ts:545:18
-     if (!connId || connId.trim() === '') throw new DbToolError('INVALID_ARGUMENT', '缺少必填参数: conn_id');
+     if (!connId || false) throw new DbToolError('INVALID_ARGUMENT', '缺少必填参数: conn_id');
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出）
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] EqualityOperator
lib/manager.ts:545:18
-     if (!connId || connId.trim() === '') throw new DbToolError('INVALID_ARGUMENT', '缺少必填参数: conn_id');
+     if (!connId || connId.trim() !== '') throw new DbToolError('INVALID_ARGUMENT', '缺少必填参数: conn_id');
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出）
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] MethodExpression
lib/manager.ts:545:18
-     if (!connId || connId.trim() === '') throw new DbToolError('INVALID_ARGUMENT', '缺少必填参数: conn_id');
+     if (!connId || connId === '') throw new DbToolError('INVALID_ARGUMENT', '缺少必填参数: conn_id');
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出）
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] StringLiteral
lib/manager.ts:545:36
-     if (!connId || connId.trim() === '') throw new DbToolError('INVALID_ARGUMENT', '缺少必填参数: conn_id');
+     if (!connId || connId.trim() === "Stryker was here!") throw new DbToolError('INVALID_ARGUMENT', '缺少必填参数: conn_id');
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出）
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] StringLiteral
lib/manager.ts:545:62
-     if (!connId || connId.trim() === '') throw new DbToolError('INVALID_ARGUMENT', '缺少必填参数: conn_id');
+     if (!connId || connId.trim() === '') throw new DbToolError("", '缺少必填参数: conn_id');
Tests ran:
    handleToolAction（工具分发层） 缺参数 / 未知 action → INVALID_ARGUMENT 错误 JSON


[Survived] StringLiteral
lib/manager.ts:545:82
-     if (!connId || connId.trim() === '') throw new DbToolError('INVALID_ARGUMENT', '缺少必填参数: conn_id');
+     if (!connId || connId.trim() === '') throw new DbToolError('INVALID_ARGUMENT', "");
Tests ran:
    handleToolAction（工具分发层） 缺参数 / 未知 action → INVALID_ARGUMENT 错误 JSON


[Survived] BlockStatement
lib/manager.ts:549:66
-   function assertArg(v: string | undefined, label: string): string {
-     if (!v || v.trim() === '') throw new DbToolError('INVALID_ARGUMENT', `缺少必填参数: ${label}`);
-     return v;
-   }
+   function assertArg(v: string | undefined, label: string): string {}
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出）
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] ConditionalExpression
lib/manager.ts:550:7
-     if (!v || v.trim() === '') throw new DbToolError('INVALID_ARGUMENT', `缺少必填参数: ${label}`);
+     if (true) throw new DbToolError('INVALID_ARGUMENT', `缺少必填参数: ${label}`);
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出）
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] ConditionalExpression
lib/manager.ts:550:7
-     if (!v || v.trim() === '') throw new DbToolError('INVALID_ARGUMENT', `缺少必填参数: ${label}`);
+     if (false) throw new DbToolError('INVALID_ARGUMENT', `缺少必填参数: ${label}`);
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出）
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] LogicalOperator
lib/manager.ts:550:7
-     if (!v || v.trim() === '') throw new DbToolError('INVALID_ARGUMENT', `缺少必填参数: ${label}`);
+     if (!v && v.trim() === '') throw new DbToolError('INVALID_ARGUMENT', `缺少必填参数: ${label}`);
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出）
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] ConditionalExpression
lib/manager.ts:550:13
-     if (!v || v.trim() === '') throw new DbToolError('INVALID_ARGUMENT', `缺少必填参数: ${label}`);
+     if (!v || false) throw new DbToolError('INVALID_ARGUMENT', `缺少必填参数: ${label}`);
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出）
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] BooleanLiteral
lib/manager.ts:550:7
-     if (!v || v.trim() === '') throw new DbToolError('INVALID_ARGUMENT', `缺少必填参数: ${label}`);
+     if (v || v.trim() === '') throw new DbToolError('INVALID_ARGUMENT', `缺少必填参数: ${label}`);
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出）
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] EqualityOperator
lib/manager.ts:550:13
-     if (!v || v.trim() === '') throw new DbToolError('INVALID_ARGUMENT', `缺少必填参数: ${label}`);
+     if (!v || v.trim() !== '') throw new DbToolError('INVALID_ARGUMENT', `缺少必填参数: ${label}`);
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出）
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] MethodExpression
lib/manager.ts:550:13
-     if (!v || v.trim() === '') throw new DbToolError('INVALID_ARGUMENT', `缺少必填参数: ${label}`);
+     if (!v || v === '') throw new DbToolError('INVALID_ARGUMENT', `缺少必填参数: ${label}`);
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出）
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] BlockStatement
lib/manager.ts:554:53
-   function needConfirmText(r: unknown): string | null {
-     if (!(r && typeof r === 'object' && (r as NeedConfirm).needConfirmation === true)) return null;
-     const nc = r as NeedConfirm;
-     return JSON.stringify({
-       ok: false,
-       code: 'NEEDS_CONFIRMATION',
-       ...nc,
-       hint: `该操作危险（${nc.reason}）。请先向用户说明并征得确认，用户同意后携带 challenge_id=${nc.challengeId} 重试同一语句；challenge 一次性、5 分钟内有效、绑定本语句。`,
-     }, null, 2);
-   }
+   function needConfirmText(r: unknown): string | null {}
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] StringLiteral
lib/manager.ts:550:26
-     if (!v || v.trim() === '') throw new DbToolError('INVALID_ARGUMENT', `缺少必填参数: ${label}`);
+     if (!v || v.trim() === "Stryker was here!") throw new DbToolError('INVALID_ARGUMENT', `缺少必填参数: ${label}`);
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） 未授权在工具层也拒（错误 JSON 而非抛出）
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] BooleanLiteral
lib/manager.ts:555:7
-     if (!(r && typeof r === 'object' && (r as NeedConfirm).needConfirmation === true)) return null;
+     if (r && typeof r === 'object' && (r as NeedConfirm).needConfirmation === true) return null;
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] ConditionalExpression
lib/manager.ts:555:7
-     if (!(r && typeof r === 'object' && (r as NeedConfirm).needConfirmation === true)) return null;
+     if (false) return null;
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] ConditionalExpression
lib/manager.ts:555:9
-     if (!(r && typeof r === 'object' && (r as NeedConfirm).needConfirmation === true)) return null;
+     if (!(true)) return null;
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] ConditionalExpression
lib/manager.ts:555:7
-     if (!(r && typeof r === 'object' && (r as NeedConfirm).needConfirmation === true)) return null;
+     if (true) return null;
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] ConditionalExpression
lib/manager.ts:555:9
-     if (!(r && typeof r === 'object' && (r as NeedConfirm).needConfirmation === true)) return null;
+     if (!(false)) return null;
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] LogicalOperator
lib/manager.ts:555:9
-     if (!(r && typeof r === 'object' && (r as NeedConfirm).needConfirmation === true)) return null;
+     if (!(r || typeof r === 'object' && (r as NeedConfirm).needConfirmation === true)) return null;
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] LogicalOperator
lib/manager.ts:555:9
-     if (!(r && typeof r === 'object' && (r as NeedConfirm).needConfirmation === true)) return null;
+     if (!(r && typeof r === 'object' || (r as NeedConfirm).needConfirmation === true)) return null;
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] ConditionalExpression
lib/manager.ts:555:9
-     if (!(r && typeof r === 'object' && (r as NeedConfirm).needConfirmation === true)) return null;
+     if (!(true && (r as NeedConfirm).needConfirmation === true)) return null;
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] ConditionalExpression
lib/manager.ts:555:14
-     if (!(r && typeof r === 'object' && (r as NeedConfirm).needConfirmation === true)) return null;
+     if (!(r && true && (r as NeedConfirm).needConfirmation === true)) return null;
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] ConditionalExpression
lib/manager.ts:555:39
-     if (!(r && typeof r === 'object' && (r as NeedConfirm).needConfirmation === true)) return null;
+     if (!(r && typeof r === 'object' && true)) return null;
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] EqualityOperator
lib/manager.ts:555:14
-     if (!(r && typeof r === 'object' && (r as NeedConfirm).needConfirmation === true)) return null;
+     if (!(r && typeof r !== 'object' && (r as NeedConfirm).needConfirmation === true)) return null;
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] StringLiteral
lib/manager.ts:555:27
-     if (!(r && typeof r === 'object' && (r as NeedConfirm).needConfirmation === true)) return null;
+     if (!(r && typeof r === "" && (r as NeedConfirm).needConfirmation === true)) return null;
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] EqualityOperator
lib/manager.ts:555:39
-     if (!(r && typeof r === 'object' && (r as NeedConfirm).needConfirmation === true)) return null;
+     if (!(r && typeof r === 'object' && (r as NeedConfirm).needConfirmation !== true)) return null;
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] BooleanLiteral
lib/manager.ts:555:79
-     if (!(r && typeof r === 'object' && (r as NeedConfirm).needConfirmation === true)) return null;
+     if (!(r && typeof r === 'object' && (r as NeedConfirm).needConfirmation === false)) return null;
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引
    handleToolAction（工具分发层） run_script action 返回脚本结果；ro 被拒


[Survived] BooleanLiteral
lib/manager.ts:558:9
-       ok: false,
+       ok: true,
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引


[Survived] ObjectLiteral
lib/manager.ts:557:25
-     return JSON.stringify({
-       ok: false,
-       code: 'NEEDS_CONFIRMATION',
-       ...nc,
-       hint: `该操作危险（${nc.reason}）。请先向用户说明并征得确认，用户同意后携带 challenge_id=${nc.challengeId} 重试同一语句；challenge 一次性、5 分钟内有效、绑定本语句。`,
-     }, null, 2);
+     return JSON.stringify({}, null, 2);
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引


[Survived] StringLiteral
lib/manager.ts:559:11
-       code: 'NEEDS_CONFIRMATION',
+       code: "",
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引


[Survived] StringLiteral
lib/manager.ts:561:11
-       hint: `该操作危险（${nc.reason}）。请先向用户说明并征得确认，用户同意后携带 challenge_id=${nc.challengeId} 重试同一语句；challenge 一次性、5 分钟内有效、绑定本语句。`,
+       hint: ``,
Tests ran:
    handleToolAction（工具分发层） query 直通与 NEEDS_CONFIRMATION 带 hint 指引


[Survived] BlockStatement
lib/manager.ts:325:34
-     async dispose(): Promise<void> {
-       this.disposed = true;
-       this.challenges.dispose();
-       const pending = [...this.adapterCache.values()];
-       this.adapterCache.clear();
-       for (const p of pending) {
-         try {
-           const a = await p;
-           await a.close().catch(() => {});
-         } catch {
-           // 缓存里的加载失败项直接忽略
-         }
-       }
-     }
+     async dispose(): Promise<void> {}
Ran all tests for this mutant.

[Survived] BooleanLiteral
lib/manager.ts:326:21
-       this.disposed = true;
+       this.disposed = false;
Ran all tests for this mutant.

[Survived] CallExpression
lib/manager.ts:327:5
-       this.challenges.dispose();
+       ;
Ran all tests for this mutant.

[Survived] ArrayDeclaration
lib/manager.ts:328:21
-       const pending = [...this.adapterCache.values()];
+       const pending = [];
Ran all tests for this mutant.

[Survived] CallExpression
lib/manager.ts:329:5
-       this.adapterCache.clear();
+       ;
Ran all tests for this mutant.

[Survived] BlockStatement
lib/manager.ts:330:30
-       for (const p of pending) {
-         try {
-           const a = await p;
-           await a.close().catch(() => {});
-         } catch {
-           // 缓存里的加载失败项直接忽略
-         }
-       }
+       for (const p of pending) {}
Ran all tests for this mutant.

[Survived] BlockStatement
lib/manager.ts:331:11
-         try {
-           const a = await p;
-           await a.close().catch(() => {});
-         } catch {
+         try {} catch {
Ran all tests for this mutant.

Ran 4.36 tests per mutant on average.
-----------------|------------------|----------|-----------|------------|----------|----------|
                 | % Mutation score |          |           |            |          |          |
File             |  total | covered | # killed | # timeout | # survived | # no cov | # errors |
-----------------|--------|---------|----------|-----------|------------|----------|----------|
All files        |   2.02 |    2.23 |       21 |         0 |        922 |       97 |        0 |
 guard           |   4.87 |    5.22 |       13 |         0 |        236 |       18 |        0 |
  index.ts       |   4.87 |    5.22 |       13 |         0 |        236 |       18 |        0 |
 store           |   2.35 |    2.40 |        8 |         0 |        325 |        8 |        0 |
  audit.ts       |   0.00 |    0.00 |        0 |         0 |         34 |        0 |        0 |
  connections.ts |   0.63 |    0.63 |        1 |         0 |        157 |        2 |        0 |
  grants.ts      |   0.00 |    0.00 |        0 |         0 |         77 |        0 |        0 |
  index.ts       |   0.00 |    0.00 |        0 |         0 |          7 |        2 |        0 |
  io.ts          |   0.00 |    0.00 |        0 |         0 |         26 |        4 |        0 |
  normalize.ts   |  50.00 |   50.00 |        7 |         0 |          7 |        0 |        0 |
  secrets.ts     |   0.00 |    0.00 |        0 |         0 |         17 |        0 |        0 |
 manager.ts      |   0.00 |    0.00 |        0 |         0 |        361 |       71 |        0 |
-----------------|--------|---------|----------|-----------|------------|----------|----------|
[32m18:30:19 (6764) INFO HtmlReporter[39m Your report can be found at: file:///E:/Code/dsh/dsh-db-tool/reports/mutation/mutation.html
[32m18:30:19 (6764) INFO MutationTestExecutor[39m Done in 8 minutes and 47 seconds.
